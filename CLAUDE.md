# 生计 - 生活成本计算器

## 项目概述

这是一个全栈的生活成本计算器应用，旨在帮助用户记录商品价格、计算烹饪成本、优化生活开支。

### 核心功能
- 📝 商品价格记录 - 记录不同时间、不同地点的商品价格
- 🍳 菜谱成本计算 - 根据价格记录计算菜谱成本
- 🥗 营养成分分析 - 基于 USDA 数据库的营养分析
- 📍 地图与路线规划 - 集成地图服务，计算出行成本
- 📊 生活成本报告 - 生成每日、每周、每月报告
- 💰 多币种支持 - 支持多种货币
- 🌏 多单位转换 - 支持公制/市制/英制转换
- 🔐 用户认证 - 支持 JWT 认证和邀请码注册

## 系统架构

```
livecalc/
├── backend/              # 后端服务 (FastAPI)
│   ├── app/
│   │   ├── api/        # API 路由定义
│   │   ├── core/       # 核心配置与安全
│   │   ├── models/     # 数据库模型
│   │   ├── schemas/    # Pydantic 模式
│   │   ├── services/   # 业务逻辑层
│   │   └── utils/      # 工具函数
│   ├── alembic/        # 数据库迁移
│   └── tests/          # 测试用例
├── frontend/            # 前端应用 (Vue 3)
│   ├── src/
│   │   ├── api/       # API 客户端
│   │   ├── components/ # 可复用组件
│   │   ├── stores/    # Pinia 状态管理
│   │   ├── views/     # 页面视图
│   │   └── router/    # 路由配置
│   └── public/         # 静态资源
├── docker-compose.yml   # Docker 编排
└── README.md           # 项目文档
```

## 技术栈

### 后端 (FastAPI)
- **框架**: FastAPI - 现代化的 Python Web 框架
- **ORM**: SQLAlchemy - Python SQL 工具包和对象关系映射
- **迁移**: Alembic - 数据库迁移工具
- **认证**: Python-JOSE + Passlib - JWT 认证和密码哈希
- **任务调度**: APScheduler - 任务调度器
- **异步任务**: Celery（可选）- 分布式任务队列

### 前端 (Vue 3)
- **框架**: Vue 3 - 渐进式 JavaScript 框架
- **构建工具**: Vite - 新一代前端构建工具
- **状态管理**: Pinia - Vue 的官方状态管理库
- **路由**: Vue Router - 官方路由管理器
- **HTTP 客户端**: Axios - Promise 基础的 HTTP 客户端
- **地图**: Leaflet - 开源地图库
- **图表**: Chart.js - 简单灵活的图表库

### 数据库
- **开发**: SQLite - 轻量级嵌入式数据库
- **生产**: PostgreSQL / MySQL - 企业级数据库

### 容器化
- **容器**: Docker - 容器化平台
- **编排**: Docker Compose - 多容器应用编排
- **Web 服务器**: Nginx - 高性能 Web 服务器

## 模块说明

### 后端模块
- **auth** - 用户认证与授权
- **products** - 商品价格记录与历史追踪
- **locations** - 地点管理与地图服务
- **nutrition** - 营养数据库与匹配算法
- **recipes** - 菜谱管理与成本计算
- **reports** - 报告生成与统计分析

### 前端模块
- **auth** - 登录/注册页面
- **dashboard** - 仪表盘与概览
- **products** - 商品管理界面
- **recipes** - 菜谱管理界面
- **locations** - 地点与地图界面
- **reports** - 报告与统计界面

## API 端点

### 认证 API
- `GET /api/v1/auth/config` - 获取注册配置
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/auth/login` - 用户登录
- `POST /api/v1/auth/refresh` - 刷新令牌
- `GET /api/v1/auth/me` - 获取当前用户信息

### 核心 API
- `GET/POST /api/v1/products/` - 商品价格记录
- `GET/POST /api/v1/locations/` - 地点管理
- `GET/POST /api/v1/nutrition/` - 营养数据
- `GET/POST /api/v1/recipes/` - 菜谱管理
- `GET/POST /api/v1/reports/` - 报告统计

## 开发规范

### Python 代码规范
- 使用 Black 进行代码格式化
- 使用 isort 进行导入排序
- 使用 Flake8 进行代码检查
- 使用 MyPy 进行类型检查

### JavaScript/TypeScript 代码规范
- 使用 Prettier 进行代码格式化
- 使用 ESLint 进行代码检查
- 遵循 Vue 3 最佳实践

### Git 工作流
- 使用 Conventional Commits 提交规范
- 保持主分支稳定
- 功能开发在特性分支进行
- 通过 Pull Request 进行代码审查

## 环境变量

### 后端
- `DATABASE_URL` - 数据库连接字符串
- `SECRET_KEY` - 应用密钥
- `JWT_SECRET_KEY` - JWT 签名密钥
- `REGISTRATION_REQUIRE_INVITE_CODE` - 是否需要邀请码注册

### 前端
- `VITE_API_URL` - 后端 API 地址（默认为 `/api/v1`）

## 开发情况

本项目为 monorepo 项目，包含前端和后端。

### 前端

技术栈：TypeScrPt + Vue + Vite

目录：`frontend`，所有前端相关操作均在此目录下进行。

开发 URL：`http://localhost:5173`

通常会打开浏览器调试。如有需要，可以使用 Chrome 开发者工具 MCP 查看页面情况，操作页面。由于一般情况下已经打开了页面，所以不要使用 Playwright。开发者在 Windows 下使用 Edge 浏览器，在 Linux 下使用 Chromium 浏览器。

响应式设计。开发时要兼顾不同地图引擎和桌面、移动端的体验。

目前需要考虑的地图引擎如下：

- 高德地图
- 百度地图（分为 GL 版本和 Legacy 版本，前者常用，后者只在一些特殊场景下使用）
- 腾讯地图
- Leaflet：目前支持高德地图、百度地图、腾讯地图、天地图、OpenStreetMap。

所有前端的修改都必须确保构建通过。

### 后端

技术栈：Python + FastAPI

目录：`backend`，所有后端相关操作均在此目录下进行，并且使用虚拟环境。

开发时使用 `uv` 管理。

虚拟环境：根目录下的 `.venv` 下的环境。

所有后端的修改都必须确保无语法错误。

### 数据库

数据库：`backend/.env` 文件中指定。一般情况下为 `backend/data/livecalc.db`。

数据库操作优先使用相应的 MCP。

开发过程中不要自行修改数据库，除非开发者明确允许此操作。

表结构需要变动时，除了维护 alembic 外，还需要提供对应的 SQL 脚本，包括一下数据库引擎的版本：

- SQLite
- MySQL
- PostgreSQL（未启用 PostGIS 支持）
- PostgreSQL（启用 PostGIS 支持）（如与 PostGIS 无关，则不需要此项）

### 测试

所有操作均需确保无语法层面上的报错，构建、编译通过。

不要在对话中启动服务，因为我已经启动了自动重载的前端、后端服务。

### 记录要点

当某项开发工作完成、告一段落或有关键性进展时，需要自动记录要点。用户要求记录要点时，也要记录。

要点按照以下的索引记录。

注意：为了节约 token，即便用户要求记录到 CLAUDE.md，也要按照下面的索引记录。

## Tips

> 来自 Claude Code 的 Inspect 工具。

### Overall

Before writing any fix, trace the full data flow for this bug: from database query → serialization → API response → frontend rendering. Show me what each layer returns at each step, and identify exactly which layer the bug is in. Do NOT propose a fix until you've completed this trace.

Before writing any SQL, run \d tablename or SELECT column_name, data_type FROM information_schema.columns WHERE table_name='tablename' to verify the exact column names and types. Then write your SQL using only confirmed column names.

Use a subagent to explore and map out the full codebase structure for feature, including all files, functions, and data flows involved. Return a detailed map. Then I'll review it before we start implementing step by step.

### Code Quality

Always verify column names, field names, and API paths against the actual codebase before writing SQL or making edits. Never guess — use Grep or Read to confirm the correct name.

### Debugging

When fixing bugs, always trace the full data pipeline end-to-end before proposing a fix. For backend→API→frontend flows, check: (1) DB query/storage, (2) serialization (_to_response or schema), (3) API response shape, (4) frontend consumption.

Before creating new data mappings or lookups, always check if the data already exists in related tables (e.g., entity_unit_overrides, existing product/ingredient records). Use Grep to search for existing implementations.

### Database

When working with PostgreSQL, never use JSON LIKE queries or naive/aware datetime comparisons without explicitly checking compatibility. Prefer ilike for text, and always ensure datetime objects are timezone-aware before comparison.

For boolean columns in SQL INSERT/UPDATE statements, always use true/false (SQL literals), not 0/1 (integers). Python False does not automatically map correctly in all ORMs.

### Architecture

When the user reports a permission/error issue, do not just add a permission check or toggle on the endpoint. First understand the intended UX flow — some operations should not exist as separate endpoints at all (e.g., image deletion should be part of recipe update, not a standalone DELETE).

## Testing

After editing Python files that may be cached (__pycache__, .pyc), remind the user to restart the server or clear cache before testing, to avoid false negatives where the fix appears not to work.

## 项目索引

本项目文档已模块化拆分，按需加载以提高性能。详细信息请查看 `./cc` 目录下的对应文件。

所有与 Claude Code 相关的文档，都放在 `./cc` 目录下。并且，在这里描述文档内容，以便索引。

如：部署说明：详见 [DEPLOYMENT.md](cc/DEPLOYMENT.md) 和 [QUICKSTART.md](cc/QUICKSTART.md)；开发时的规则，详见 [DEV_RULE.md](cc/DEV_RULE.md)

### 最新修复记录
- 菜谱成本区间 avg 超 max 修复（半成品成本区间递归）：蛋炒饭(280) 成本趋势 avg=15.80 超 max=13.24。根因是 `cc55a18` 的**口径撕裂**——[calculate_recipe_cost_range_trend](backend/app/services/recipe_service.py#L1508) 里 avg 走完整 5 层降级链（[calculate_recipe_cost_as_of](backend/app/services/recipe_service.py)）、min/max 手写只覆盖 3 层（缺 name_match + 半成品推导），半成品米饭(ing 179, `serving_weight=400`, 菜谱 185 产出) 在 min/max 贡献 0、avg 贡献 3.66 → avg 顶过 max。方案双轨收敛单轨：新增递归 [calculate_recipe_cost_range_as_of](backend/app/services/recipe_service.py#L1443) 每食材产 (min,max,avg) 三档、菜谱三档各自求和，**凸组合数学保证** min≤avg≤max 恒成立；trend 逐天循环删双轨改单轨。5 层降级链 1:1 映射（direct/fallback/name_match/半成品递归/contains 聚合），Task 7 抽纯函数 [_ppgs_to_range](backend/app/services/recipe_service.py)（一组每克单价→min/max/avg，过滤≤0 脏数据）。**Task 7b 意外挖出 standard_quantity 数据脏**：规范审查深查 r280/90 天炸出 24 违规，根因 [_direct_cost_range_ppg](backend/app/services/recipe_service.py#L1250) avg 用 resolve 加权路径（依赖 `standard_quantity`，product 2 该字段脏 original=30 斤未转 g 应 15000g，致单价放大 50 倍 545元/kg）、min/max 用逐条 original 口径，av_ppg=0.278>max(ppgs)=0.012 → avg 超 max 23 倍；放弃 av_ppg 覆盖改三档同源 `_ppgs_to_range`（逐条 original 口径对数据脏鲁棒），24 违规消除。**续修 single 版对齐**（用户发现成本估算 15.80 ≠ 趋势 avg 13.38）：single 版 [calculate_recipe_cost_as_of](backend/app/services/recipe_service.py) 的 direct/fallback/name_match 三层全对齐 range 版——direct 换 `_direct_cost_range_ppg`（原 `resolve_direct_weighted_for_cost` 依赖 standard_quantity）、fallback 换 `_fallback_cost_range_ppg`（原 `_get_ingredient_fallback` 取单条记录致火腿 4.05 vs 1.61）、name_match 换 `_name_match_cost_range_ppg`；单位转换段补 direct gram-based 分支。收尾（最终审查无 Critical/High）：降级链 5 层全部三档同源、contains 层 avg 对齐 direct 同源范式、删孤儿函数 `_std_unit_price_to_per_gram`（32 行死码）、trend `except Exception` 加 logger.debug。验证 py_compile + 单测 5p + 全量回归 452 passed + 端到端 r280/r5/r10/r50/r100 90天 + r280 365天 TOTAL violations=0 + 成本估算=趋势 avg=13.38 完全一致。边界遗留：fallback drift（RANGE 聚合 vs SINGLE 首商品）已通过共享 `_fallback_cost_range_ppg` 消除。教训：min/max/avg 必须同源同链；验证窗口要够大（执行者只测 30 天靠规范审查 90 天才炸出 24 违规）；single 版和 range 版必须共享同一套基础函数否则必然漂移。详见 [BUGFIX_半成品成本区间avg超max.md](cc/BUGFIX_半成品成本区间avg超max.md)
- 部署 nginx 图片 404 + 图标 403 修复（NAS all-in-one 部署两个独立根因）：①图片 `/api/v1/images/*.jpg` 全 404（连 307 都没有）根因是 [default.conf.template](deploy/nginx/default.conf.template) 的静态资源长缓存正则 `~* \.(...|jpg|png|svg|ico|...)$` **优先级高于**普通前缀 `location /api/`（nginx 优先级：正则 > 普通前缀），把 `/api/v1/images/*.jpg`、`/api/v1/static/*.jpg` 全截走去 `/usr/share/nginx/html` 下找文件 → 404，请求**根本没到后端** serve_image（[main.py:579](backend/app/main.py#L579) 必返 307）；对比铁证 `/api/v1/auth/config`（非图片后缀）200 JSON 而 `/api/v1/images/*.jpg` 404。修复 `location /api/` → `location ^~ /api/`（前缀命中后不再查正则），一行加 `^~`。②`/logo.svg`、`/favicon.ico`、`/pwa-*.png`、`/apple-touch-*`、`/maskable-*`（全是 `frontend/public/` 源文件）全 403 而同目录 vite 生成的 `/assets/*` 200，根因是 public 源文件从 **Windows 构建上下文** COPY 进 Linux 容器后权限不可读，nginx try_files stat 命中（存在）→ static 模块 open 读失败 → 403（非 404，因 try_files 的 =404 只在 stat 失败触发）；vite 容器内新生成的 assets 是 node umask 022→644 正常。修复 [Dockerfile](Dockerfile) frontend-builder `RUN npm run build && chmod -R a+rX dist`（a+r 文件可读、a+X 大写只给目录加遍历位，放 builder 两 target 都受益）。两处都藏在容器化部署配置里，dev 走 vite proxy 按前缀转发不看后缀、不经过这套 nginx 故从未暴露，必须 prod nginx 端到端验证媒体链路。宿主头像文件存在、DB `storage_configurations` backend=local 配置无误，纯被 nginx 挡在前面。详见 [BUGFIX_部署nginx图片图标404_403.md](cc/BUGFIX_部署nginx图片图标404_403.md)
- 粘贴导入 autocomplete 并发风暴修复：粘贴一两百行价格「解析并匹配」后半部分大量 unmatched。systematic-debugging 定位双层瓶颈——主因 [PasteImportDialog.doParse:256](frontend/src/components/prices/PasteImportDialog.vue#L256) `Promise.all(okRows.map(r => tryAutoMatch(r)))` 零并发限制齐发 autocomplete，200 行瞬间 200 请求；放大器 [product_autocomplete](backend/app/api/products_entity.py#L815) 全表扫描 851 活跃商品 + joinedload 原料 + Python 逐行子串遍历（O(N×M) 不走索引），SQLite（`.env` 确认）并发读弱；vite proxy HTTP/1.1 浏览器并发~6 排队 + 前端 axios 10s 超时（[client.ts:5](frontend/src/api/client.ts#L5)）→ 后排请求干等超时 → [tryAutoMatch catch:310](frontend/src/components/prices/PasteImportDialog.vue#L310) 静默吞保持 unmatched。讽刺的是同文件 [doImport:450](frontend/src/components/prices/PasteImportDialog.vue#L450) 导入阶段有 `CONCURRENCY=5` 分批、解析阶段却裸奔。修复对齐 doImport 范式：doParse 改 `for slice + Promise.all` 每批 5 并发（MATCH_CONCURRENCY=5），tryAutoMatch 内部 try/catch 必 resolve 故 Promise.all 不会被单行失败带崩；200 行约 2-3s 完成。后端 autocomplete 被 4 处共用（QuickFillView/PricesView/PasteImportDialog×2）未动。纯前端单文件改动，build 通过（26.03s，precache 131 entries），手动验证待做（DevTools Network 看每批 5 并发不再超时）。教训：`Promise.all(map)` 是并发风暴经典坑，限流要覆盖所有批量调用点；后端全表扫+Python 遍历是潜伏放大器（量小无感量大崩）。详见 [BUGFIX_粘贴导入autocomplete超时.md](cc/BUGFIX_粘贴导入autocomplete超时.md)
- S3 全量导出补图片打包（从 feat/local 搬运）：feat/local 分支（94c3fd6）发现主分支「全量导出 zip 无图片」并修复，因两分支差距大（feat/local 领先 46 提交、94c3fd6 还混入纯前端 local 模式代码 `frontend/src/api/local/*` 与 CLAUDE.md 改动）无法直接 cherry-pick，改用 `git checkout 94c3fd6 -- <指定文件>` 单文件挑取 6 个（packaging.py + 测试 + 4 文档），坚决不带 frontend/local 与 CLAUDE.md。铁证：master packaging.py blob 与 94c3fd6 改动前完全相同（`f67ef97a3f6…`，feat/local 在该文件仅此一提交动过）→ 整文件覆盖即纯应用 fix，零冲突零杂质。核心改动 [packaging.py](backend/app/services/export/packaging.py)：外链图 `ThreadPoolExecutor` 并发下载（admin 去 query 下原图、普通用户原样瘦身图）、storage key 图 `storage.get()` 直读 S3 bytes 打包（图物理在 S3、DB 存 storage key 是「全量无图」根因，原 `_collect_image_files` 只找本地图故漏）、`build_export_zip` tmpdir `try/finally shutil.rmtree` 清理；依赖 `get_storage()`/`StorageBackend.get(key)->bytes` master 已确认存在（[base.py:16](backend/app/services/storage/base.py#L16)）。py_compile 过；未跑测试（feat/local 纯前端分支从未运行此 backend fix），建议 `pytest backend/tests/services/test_export_packaging.py` + 实测一次 S3 全量导出确认图片进 zip。⚠️ 附带文档 `cc/BUGFIX_local模式数据流修复.md` 主体 #1~#7 为 local 模式（纯前端 IndexedDB）笔记（master 无此模式、引用的 frontend/src/api/local/* master 全无），仅文末附录「云模式全量导出诊断」与本次 fix 相关，事后可裁剪。commit 84fd09f，未 push。详见 [FEATURE_外链图打包导出.md](cc/FEATURE_外链图打包导出.md)
- 粘贴导入展开错位修复：移动端快速填写「粘贴导入」展开待处理项时视口剧烈下跳、当前行被挤出可视区。七轮拉锯定位真凶——前几轮 `.paste-preview-table { table-layout: fixed }` 加在 Vuetify v-table **外层 div** 上根本没生效（`table-layout` 必须作用在 `<table>` 元素，要用 `:deep(table)` 穿透），fixed 失效→列宽 auto→autocomplete 面板撑宽名称列→挤窄其他列致内容换行→**reflow 把当前行挤出可视区**（reflow 不触发 scroll 事件，故 overflow-anchor/rAF 对冲 scrollTop 全打空；诊断一度被「用户手动滚动惯性」日志误导）。关键转折是全量枚举诊断「展开后无任何 scroll 事件」+ 用户两次坚持列宽变化。最终修复 4 点：①`.paste-preview-table :deep(table) { table-layout: fixed }` 让 fixed 真生效（列宽钉死不 reflow）②列宽重分配（图标28+价格56+数量48+单位44=176，移动端名称列~150px）③展开态 `colspan="5"` 整行撑满表格宽度（给面板充足空间避免截断）+ 按钮 flex-wrap ④展开态顶部加商品名+价格摘要行。撤掉所有 scroll 对抗 hack，overflow-anchor:none 留作双保险。详见 [BUGFIX_粘贴导入展开错位.md](cc/BUGFIX_粘贴导入展开错位.md)
- local 模式数据流修复（菜谱/原料/商品详情 7 项）：纯前端本地模式（`VITE_STORAGE_MODE=local`）详情页数据对不上/缺失批量修复——根因同构，local handler 移植时与「云模式返回格式 + 前端消费字段名 + DB 存储字段名」三方契约没对齐。① 菜谱 NRV 与数量口径拧着（[recipes.ts:351](frontend/src/api/local/handlers/recipes.ts#L351) 用了 aggregator 基于「每100g菜谱」的 nrv_pct，改 `calcNRV(name, n.amount)` 基于每份，对齐云模式 [nutrition.py:1506](backend/app/api/nutrition.py#L1506)）；② 原料价格记录越界（[products.ts listRecords:118](frontend/src/api/local/handlers/products.ts#L118) 不认 `ingredient_id` → 全表返回，加按原料商品集过滤）；③ 相关菜谱空（[nutrition.ts:321](frontend/src/api/local/handlers/nutrition.ts#L321) 返回裸 recipe_ingredients，改为 `{items:[菜谱详情]}` 含 is_public）；④ 食材单位全变「斤」（云 seed 克=3 vs 本地 斤=3，[exportImport.ts:375](frontend/src/api/local/handlers/exportImport.ts#L375) 单菜谱分支漏传 resolveLocalUnitId 致 cloudId 直接保留，补传后按名重映射；存量错数据需清空 livecalc IndexedDB 重导）；⑤ 食材成本空（[costCalculator](frontend/src/api/local/business/costCalculator.ts) 五处 push + [recipes.ts:203](frontend/src/api/local/handlers/recipes.ts#L203) cost_breakdown 都没透传 recipe_ingredient_id，前端按它匹配失败）；⑥ 相关菜谱全标「未发布」（#3 漏返 is_public，`!undefined` 触发 chip）；⑦ 层级关系空（[hierarchy.ts:5](frontend/src/api/local/handlers/hierarchy.ts#L5) 返回 `{parents,children,relations}`，前端要 `{parent_relations,child_relations}` 含 parent_name/child_name/relation_type，对齐云模式 [ingredient_hierarchy.py:181](backend/app/api/ingredient_hierarchy.py#L181)）。附云导出诊断：全量导出「数据少」非 bug（git 实锤本次未动后端；当前代码+SQLite 库复现 `build_export_zip('full')` 完整 merchants 8/price_records 2375/hierarchy 124；22:00 ZIP 少是当时后端连了另一较空的库；图片外链跳过是设计行为，另起特性「外链图打包」）。build 通过、dev HMR 实测全验证。local 库实为 `livecalc`（[database.ts:154](frontend/src/api/local/database.ts#L154)）非设计文档的 `livecalc_local`（空壳遗物）。详见 [BUGFIX_local模式数据流修复.md](cc/BUGFIX_local模式数据流修复.md)
- 天地图「地图类型」死选项清理：后台「地图配置」天地图折叠面板的「地图类型」下拉（矢量/影像/地形）切换后地图毫无变化，是误导用户的死选项。trace 全链路确认 `config.map_api_keys.tianditu.type` 无人消费——[TiandituEngine.ts:34](frontend/src/utils/map/engines/TiandituEngine.ts#L34) 硬编码 `TianDiTu.Normal.Map` 矢量底图只读 token 不读 type、[mapAdapters.ts:101](frontend/src/utils/mapAdapters.ts#L101) 另一套也写死，无论 UI 选 vec/img/ter 永远矢量层。KISS 删 [MapSettingsView.vue:159](frontend/src/views/admin/MapSettingsView.vue#L159) 的 v-select + 加注释；**保留 type 默认值 'vec' 不动**（config 默认值/MapApiKeys 接口/mapTypes.ts 全留），让 config 结构不变（TS 类型/保存 payload/后端透传/向后兼容全不受影响），不扩大到接口与后端 schema。单文件 template 改动，build 通过（precache 128 entries 不变），无表结构变更。详见 [BUGFIX_天地图地图类型死选项.md](cc/BUGFIX_天地图地图类型死选项.md)
- 商家列表筛选样式与页宽微调：地图禁用态三处体验问题。①桌面端「清除筛选」按钮多余 ②搜索框与筛选项间距太窄（ga-2=8px 挤）③页宽比其他列表页宽。FilterBar 是公共组件被 5 页面共用（Merchants/Prices/Recipes/Products/Ingredients），桌面 inline 清除按钮（[FilterBar.vue:332](frontend/src/components/common/FilterBar.vue#L332)）其他 4 页面仍需要不能硬删 → 加可选 prop `hideClearButton?: boolean`（默认 false 向后兼容）按钮 `v-if` 加 `&& !hideClearButton`，[MerchantsView](frontend/src/views/data/MerchantsView.vue) B 分支传 `:hide-clear-button="isDesktop"`（移动 modal 清除按钮不受影响，prop 对移动端天然无效）+ 外层 `ga-2`→`ga-3`（12px）；页宽根因是其他列表页都靠 [responsive.css:54](frontend/src/assets/css/responsive.css#L54) `list-grid-container`（max-width:1600px;margin:0 auto）约束，商家管理 B 分支用裸 `<div class="w-100">` 无约束 >1600px 宽屏撑满 → 加 `list-grid-container` 类（`w-100` 撑满+max-width 限制+margin auto 在 flex 容器 main-content 居中，移动端窄屏不触发正常撑满），地图启用态（左列表+右地图）要铺满不动。其他 4 页面 prop 默认 false 零影响。属 [[FEATURE_禁用地图总开关]] 扫尾。build 通过（27.16s/15.12s，precache 128 entries 不变），无表结构变更。详见 [BUGFIX_商家列表筛选样式.md](cc/BUGFIX_商家列表筛选样式.md)
- 导入权限越权修复：管理员导出 full 包、普通用户导入后突破所有限制（建管理员专属黑名单分组、菜谱直接公共绕过发布审核、克隆管理员的隐私数据如价格记录/家地点/个人黑名单）。根因三层缺口——①入口 [import_api.py:33](backend/app/api/import_api.py#L33) `POST /data/upload` 用 `get_current_user`（保留普通用户导入迁移能力，设计如此）②闸门 [api_service.py](backend/app/services/importer/api_service.py) 只拦 HowToCook 不拦 EXPORT ③主体 [export.py](backend/app/services/importer/importers/export.py) `ExportImporter` 全文无 `is_admin` 判定，仅依赖 `self.user_id` 全数据裸写（菜谱 `is_public=True` 硬编码、admin-only 数据直写、私人数据改归属克隆）。brainstorming 五决策（两者兼顾/共享字典不存在直写新建 auto 级/私人数据 full 跳过·mine 恢复/营养 is_verified 降级 False/mine 菜谱恢复原 public）→ 给 ExportImporter 注入 `is_admin` + 读 manifest `scope`，按「角色×scope×数据桶」三维分流：admin-only（黑名单分组/单位换算/商品条码）普通用户跳过、营养 `is_verified` 强制 False、菜谱 full 强制私有/mine 恢复、地点/个人黑名单/订阅 full 跳过；价格记录查重导入（重复跳过，`recorded_at` 缺失不参与查重）且非自己的 `purchase` 降为 `price`（不计支出），全部计入新增 `result.skipped` 统计；管理员全量直写不变、入口不动（权限收口在 importer 内部）。附带修三个数据丢失 bug：商品 barcode/image_url/custom_nutrition_data 字段补读、entity_unit_overrides 表补导入（导出端有、导入端完全无）、`Product.tags` list 绑 String 列崩（对齐 `serialize_tags` 序列化，[BUGFIX_数据导入过程无进度反馈.md](cc/BUGFIX_数据导入过程无进度反馈.md) 曾提及的潜伏隐患被 Task 7 测试逼出）。前端 [ImportUploadDialog](frontend/src/components/import/ImportUploadDialog.vue) 区分 displayStats/skippedItems 展示跳过摘要。TDD 全程红绿，[test_export_importer_permissions.py](backend/tests/services/test_export_importer_permissions.py) 21 例全过（含端到端综合：full 包普通用户一次导入所有越权点 + mine 包 roundtrip）、后端 compileall + 前端 build 通过（48.86s）。无表结构变更。教训：旁路通道（导入/导出/批量 API）要和正门 UI 同一套权限，漏一个全破；`import_all` 整体 try/except 吞子步骤异常是隐形 bug 温床（tags 崩致商品静默建不出）；TDD 红绿逼出潜伏 bug。详见 [BUGFIX_导入权限越权.md](cc/BUGFIX_导入权限越权.md)
- 黑名单分组导入缺失修复：数据导出 zip 里**已含**黑名单三表（`blacklist_groups.json`/`user_ingredient_blacklist.json`/`blacklist_group_subscriptions.json`），导入后却全不回来。三层证据定位——①导出端完整且工作正常（实测 `build_export_zip(admin,'full')` 产出 `blacklist_groups:13`、errors 空；[packaging.py:314-385](backend/app/services/export/packaging.py#L314) 收集 + [serializers.py:437-470](backend/app/services/export/serializers.py#L437) 三序列化函数 + manifest/zip 全有；`.is_active` 看似可疑实继承自 [AuditMixin](backend/app/core/base_model.py#L17) 正常），②**确凿缺口在导入端** [export.py](backend/app/services/importer/importers/export.py) 完全无黑名单代码，`import_all` 调用链不消费三个 json 默默忽略，③附带诊断当前库 `blacklist_group_ingredients` 表空（[allergen_seed.py](backend/app/services/allergen_seed.py) ingredient_names 与实际原料名对不上、seed 时 found_ids 全空但建了 13 空壳分组，seed 匹配问题非本次范围）。修复单文件补导入端：import 四模型 + `ReferenceMapping` 加 `blacklist_groups` 映射 + `import_all` 在 price_records 后 images 前接入三步（带 cb 阶段进度）+ 三方法 + `_match_blacklist_group_by_name` 辅助。**关键设计**：分组按 name 去重（已存在只登记映射不重建、避免撞 `BlacklistGroup.name` 唯一约束）；三关系表（`BlacklistGroupIngredient`/`UserIngredientBlacklist`/`BlacklistGroupSubscription`）统一「查含软删→复活 or 新建」范式（先查不过滤 is_active，存在则复活，对齐 [API add_ingredients_to_group](backend/app/api/blacklist_groups.py#L146)，不存在才 add），否则会撞 unique 约束；`ingredient_id`/`group_id` 旧→新映射 + name 兜底。验证 py_compile + 临时 SQLite 库往返测试全断言通过（新建/去重/软删复活/id 映射/name 兜底全验证）。无表结构变更，导出端零动。教训：「导出了却导不回」要两端都端到端跑（看 json 内容 + 导入 stats）；软删+unique 表导入去重必须「查含软删」。详见 [BUGFIX_黑名单分组导入缺失.md](cc/BUGFIX_黑名单分组导入缺失.md)
- 数据导入过程无进度反馈修复：个人中心「数据导入」（[ImportUploadDialog](frontend/src/components/import/ImportUploadDialog.vue)）上传合法 ZIP 后一直转圈、无反馈，但上传非法包（快速 failed）反而能弹错误提示。systematic-debugging 三层证据定位根因——后端任务 success 未卡（DB 实测 id=1 29s 完成、stats 完整）+ failed 路径前端正常弹 alert（证明 watch 链路工作）+ progress 全程恒 `{}`（真凶）。不是「完成后没提示」是「过程无进度」用户没等到结束。根因三层断链：① [`_run_import_task`](backend/app/services/importer/api_service.py) 设 `running` 时**不写初始 progress** ② 四个 `import_from_*` 收到 `progress_callback` 却**不透传**给 `importer.import_all` ③ 两个 importer 的 `import_all(self, collection)` 签名**不接受 `progress_callback`**（[howtocook.py](backend/app/services/importer/importers/howtocook.py) 4 阶段 / [export.py](backend/app/services/importer/importers/export.py) 16 阶段零回调）。修复 4 文件（KISS，纯后端，前端零改动自动受益）：[models.py](backend/app/services/importer/models.py) 基类 `import_all` 加 `progress_callback` 参数；两个 importer `import_all` 加 `cb` 兜底 + 阶段边界 cb，大循环方法（原料/菜谱/商品/价格记录）加 `cb` 参数 + 循环周期 cb（复用既有 idx%100/50 logger 节流点；export 原料/商品每 100、菜谱每 25、价格记录每 200）；[api_service.py](backend/app/services/importer/api_service.py) 设 running 时写初始 progress「正在准备导入…」+ 四个 `import_from_*` 透传（`result = importer.import_all(collection)` 用 `replace_all` 一次覆盖 git/upload/upload_path 三处）。验证：py_compile + 单元（cb 调 13 次含「导入原料 100/150」数字滚动 + ERRORS 空 success）+ DB 0.3s 轮询演变（pending→running「初始化」→终态）+ failed 前端弹 alert。无表结构变更。附带发现未修的潜在隐患：`Product.tags` 缺字段时 list 绑定 SQLite 崩（`MutableDict` 未覆盖 tags 列，对齐 aliases 待修）+ `ProductRecord.original_unit_id` NOT NULL（缺字段崩），用户真实系统导出包字段完整不触发。教训：「一直转圈无反馈」要区分**过程无进度** vs **完成无提示**（failed 能弹 alert 是判 watch 正常的关键）；`py_compile` 过 ≠ 依赖装了（不执行 import）；venv 在项目根 `d:\code\livecalc\.venv`（`VIRTUAL_ENV` 指向、后端服务用）非 `backend/.venv`（uv 默认空壳，缺 sqlalchemy）。详见 [BUGFIX_数据导入过程无进度反馈.md](cc/BUGFIX_数据导入过程无进度反馈.md)
- 数据导出 full 200 但 ERR_FAILED（XHR blob 累积失败，fetch 流式读修复）：个人中心「数据导出」全量失败，后端无报错，DevTools 200+「No data found」+ 前端 `net::ERR_FAILED 200 (OK)`，mine 无事。systematic-debugging 5 轮，前 3 轮全栽在「curl 不是浏览器」：①假流式 StreamingResponse→Response→②真分块 chunked——curl 测后端/proxy 都「完整」，用户失败；③dev 绕过 proxy 直连后端 + CORS expose_headers——curl 跨域测完整，**用户浏览器仍失败**（URL 已变 8000 绕过 proxy 生效，证明非 proxy 单独所致）；④**关键突破**：curl 到极限，换 edge-devtools 浏览器视角测——`fetch+getReader` 流式读成功（received 104800319 err null）、`XHR responseType:blob`（axios 用）失败（error status 0 14.5s，复现）、`response.blob()` 失败（Failed to fetch）、`fetch+getReader+手动 new Blob` 成功（blobSize 104800319）。**真凶**：浏览器内部累积 ~100MB blob 的路径（XHR blob/response.blob()）失败，fetch 流式读（JS 主动消费）+ 手动 new Blob 成功；附带 Vite proxy 对带 Accept-Encoding 大响应流式吞字节（dev 经 proxy 叠加）。**最终修复**：前端 [ProfileView doExport](frontend/src/views/profile/ProfileView.vue) 弃 axios（XHR blob）改 `fetch + resp.body.getReader() 流式读 chunks + 手动 new Blob`，dev 绕过 proxy 直连后端（`directHost = import.meta.env.DEV ? VITE_DEV_BACKEND_URL||'http://localhost:8000' : ''`）、prod 同源 Nginx，删 axios import；后端 [main.py CORS](backend/app/main.py) 加 `expose_headers=["Content-Disposition"]`；export.py 真分块保留。edge-devtools 验证 fetch+new Blob 成功、build 38.23s 过。无表结构变更。教训：**curl 测不出浏览器 XHR/blob 累积失败**（curl 无 XHR 引擎），浏览器侧问题必须用浏览器 DevTools（edge-devtools MCP）测；大文件下载避免 axios/XHR responseType:blob，用 fetch 流式读+手动 new Blob 或 `<a download>` 原生下载；多轮修复失败要质疑假设层次（前几轮在响应/编码/proxy 打转，真因在浏览器 blob 累积，差一抽象层）。详见 [BUGFIX_导出假流式中断.md](cc/BUGFIX_导出假流式中断.md)
- 地图配置多选失效与默认地图不联动修复：后台「地图配置」页（[MapSettingsView.vue](frontend/src/views/admin/MapSettingsView.vue)）三个连锁问题——①「启用的地图服务」chip-group 点一个后再点只换值、无法多选 ②「默认地图」下拉写死全部 5 个、不随启用列表联动 ③ 保存报 422 `body.available_maps: list_type`（请求体传 `"amap"` 字符串）。根因全在前端，后端 [admin.py:46](backend/app/api/admin.py#L46) `available_maps: List[str]` 无误——①③ [MapSettingsView.vue:38](frontend/src/views/admin/MapSettingsView.vue#L38) `<v-chip-group v-model="config.available_maps" column>` 漏 `multiple`，Vuetify 3 chip-group 默认单选，选中把数组替成单值字符串（字符串也有 `.includes` 致 `:color` 选中态视觉正常、更具迷惑性）；② `availableMaps=ref(allMaps)` 固定全部 + default_map 无回退。修复 4 处单文件改动：v-chip-group 加 `multiple`；`availableMaps` 改 computed 按启用列表过滤；抽 `ensureDefaultMapValid`（默认地图不在启用列表回退首个）+ `watch(() => [...config.available_maps], ...)` 联动 + fetchConfig 拉取后兜底调一次（防库里脏数据，`[...]` 展开 防御 Vuetify 内部 mutate 数组）；import 补 computed/watch。后端零动（422 把脏请求挡在门外、库里始终合法数组）。build 通过（37.95s，precache 128 entries 不变），待手动核验多选/联动/保存 200。无表结构变更。详见 [BUGFIX_地图配置多选与联动.md](cc/BUGFIX_地图配置多选与联动.md)
- PWA dev 模式 workbox glob 空匹配警告消除：dev 启动终端打印 `One of the glob patterns doesn't match any files`（dev-dist）。根因 [vite.config.ts](frontend/vite.config.ts) `workbox.globPatterns` 配的是 build 产物类型（js/css/html/png/svg/webmanifest/woff2），本意扫 build 的 dist；但 `devOptions.enabled:true` 下 vite-plugin-pwa 在 dev 也跑一遍 workbox glob 扫 dev-dist，而 dev 模式 vite 走内存、dev-dist 仅含 sw.js + workbox-*.js（均被默认 globIgnores 排除）→ 匹配必然空 → workbox-build 对空匹配 pattern warn。良性噪音（dev precache 本无意义、SW 注册/manifest 都正常）。解法用 vite-plugin-pwa 官方开关 `devOptions.suppressWarnings:true`（源码 [index.js:579](frontend/node_modules/vite-plugin-pwa/dist/index.js#L579) 实锤：dev-dist 补一个空 suppress-warnings.js + 把 **dev 分支**用 globPatterns 临时改成 `["suppress-warnings.js"]` 命中它，仅作用 dev 分支、build 的 globPatterns 与 dist precache 清单零影响），比条件化 `globPatterns:[]` 之类 hack 干净（KISS）。验证 build 35.92s 通过、`precache 128 entries` 不变（证实 build 完全不受影响）；dev 需手动重启 dev 服务生效（vite 对 config 改动不一定自动重启），重启后 dev-dist 多出空 suppress-warnings.js、警告消失。单文件改动，无表结构变更。详见 [BUGFIX_PWA_dev_glob警告.md](cc/BUGFIX_PWA_dev_glob警告.md)
- 地图两个下拉移动端样式统一：[MerchantMapView.vue](frontend/src/components/map/MerchantMapView.vue) 移动端「选择地图」和「选择地点」两个 v-select 紧挨却样式不一致（地图下拉被 CSS 缩小、地点下拉默认大小；且地图下拉内联 `max-width:120px` 盖过 CSS 的 80px 致缩小 CSS 静默失效）。抽公共 class `map-control-select` 两个 v-select 共用：桌面 max-width 150px、移动统一 80px + font 0.7rem + `:deep(.v-field__input)` padding 4px 8px/min-height 28px；删两个 v-select 内联宽度（地图 `style:120px` / 地点 `:style maxWidth`）避免与 CSS 打架；删冗余 `:deep(.mobile-layer-selector)` 重复块与 `display:none/block`（v-if/v-else-if 已控渲染）。无表结构变更，build 通过（13.14s）。详见 [BUGFIX_地图下拉样式统一.md](cc/BUGFIX_地图下拉样式统一.md)（续修：首版统一 80px 致地点下拉「全部商家」截断，地点下拉加 `place-select` 修饰 class、移动端单独 120px——风格统一但宽度按内容差异化）
- 移动端详情页导航栏溢出菜单：移动端窄屏各详情页 app-bar 右侧按钮过多（菜谱4/原料·商品·商家各2）挤掉标题致省略号、看不出页面。方案：桌面端（isDesktop=mdAndUp ≥960px）原样平铺，移动端（!isDesktop）全部收进 `v-menu` 三点溢出菜单（mdi-dots-vertical + v-list），刷新也收进（用户拍板——原料/商品/商家只有 2 按钮，留刷新则「刷新+三点」仍 2 按钮、收纳失效，全收进才能真正瘦成单个三点按钮）。loading 放 activator（v-list-item 不支持 loading）：`<v-btn :loading="anyLoading">` 任一异步进行时三点转圈、不用打开菜单就知道在忙；[RecipeDetail.vue](frontend/src/views/recipes/RecipeDetail.vue) 加 `anyLoading = computed(() => publishing.value || loading.value || deleting.value)`（排在 publishing 声明后防 TDZ）。沿用四页既有 `useMobileDrawerControl` 的 isDesktop，不引新断点；v-menu 写法参考 [QuickFillView.vue:82](frontend/src/views/prices/QuickFillView.vue#L82)。RecipeDetail 删除两态（可删 mdi-delete / 不可删 mdi-delete-off-outline+tooltip 互斥分支）合并成一个「删除菜谱」菜单项：可删 `v-list-item @click="showDeleteDialog = true"` / 不可删 `v-else disabled + subtitle`「已发布菜谱不可删除，请联系管理员」，语义对齐桌面 tooltip；发布 `v-if=canPublish` / `:disabled=!recipe` 迁 v-list-item，`base-color=success/error` 保配色。四页统一模板：`<template #append>` 内 `<template v-if="isDesktop">` 包原按钮组 + `<v-menu v-else>` 三点菜单。不抽公共组件（四页按钮集差异大，内联 + 写法一致性收敛）。[RecipeAnalysisView](frontend/src/views/recipes/RecipeAnalysisView.vue) 仅 1 按钮不动。无表结构变更，纯前端，build 通过（15.55s）。chrome-devtools 连不上 Edge 9222 待开发者手动核<960px 四页单三点按钮/菜单可用/刷新转圈。详见 [FEATURE_移动端详情页溢出菜单.md](cc/FEATURE_移动端详情页溢出菜单.md)
- 商家管理列表项按钮收纳（所有端）：[MerchantsView.vue](frontend/src/views/data/MerchantsView.vue) 列表项 append 原 4 小按钮（收藏/定位/编辑/删除）平铺、桌面端也挤。所有端统一收（非仅移动端），定位（高频、看坐标禁用态）留外面，收藏/编辑/删除 进 v-menu 三点菜单（mdi-dots-vertical，size=small）。关键：菜单项 `@click` 不需 `.stop`——v-menu 内容 teleport 到 overlay 不冒泡到外层 list-item 触发跳详情（原 v-btn 用 `.stop` 是因它在 list-item DOM 内）；收藏项 `:base-color` 已收藏 error。同属按钮收纳轮次，详情见同篇 [FEATURE_移动端详情页溢出菜单.md](cc/FEATURE_移动端详情页溢出菜单.md) 续节。无表结构变更，build 通过（13.03s）。
- 改密码输错原密码致 401 死循环转圈修复：个人中心修改密码输错原密码点确定一直转圈，后台频繁 `PUT /auth/me/account` 401「当前密码错误」，无前端提示。根因 [client.ts](frontend/src/api/client.ts) 响应拦截器 401 处理只对 `/auth/login` 排除 refresh（[BUGFIX_登录401刷新冲掉错误](cc/BUGFIX_登录401刷新冲掉错误.md) 的特例），其它 401 一律 refresh+重放；`/auth/me/account` 的「当前密码错误」是业务校验型 401，refresh 救不了（token 没坏），重放照样 401，拦截器又无「已重试」刹车 → 无限 refresh→重放→401 死循环，`return api.request(error.config)` 永不 reject 致 [ProfileView saveChangePassword](frontend/src/views/profile/ProfileView.vue#L764) catch 收不到错误、`savingPassword` 永真转圈。修复加 `_retry` 单次重试标志位（axios 社区标准范式）：条件 `if (!isLoginRequest && !(error.config as any)?._retry)`、重放前 `(error.config as any)._retry = true`，二次 401 跳过 refresh 透传 extractErrorDetail + reject，UI 显示「修改失败：当前密码错误」。四场景走查（token 过期能救/密码错只 refresh 一次报错/login 不影响/token 过期+密码错最终报密码错）。教训：已登录用户业务校验型 401 端点（改密码、改邮箱等）都踩同款死循环，单次重试标志位是根治范式，比逐端点加白名单省心。纯前端单文件改动，build 通过（13.69s）。详见 [BUGFIX_改密码401死循环转圈.md](cc/BUGFIX_改密码401死循环转圈.md)
- 登录失败页面刷新看不到错误修复：输错密码页面快速刷新、错误提示一闪而过。根因 [client.ts](frontend/src/api/client.ts) 响应拦截器 401 处理无差别——登录请求(/auth/login)的 401（凭证错误）在用户未登录时 refreshToken 为空，命中 else 分支 `window.location.href='/login'`，可用户本就在 /login，等于整页刷新，把 [Login.vue:111](frontend/src/views/auth/Login.vue#L111) catch 刚设的 errorMessage（读 response.data.detail 显示 v-alert）冲掉。修复 401 拦截器加 `isLoginRequest = error.config?.url?.includes('/auth/login')` 判断，登录请求 401 跳过 token 刷新/跳登录，直接走 extractErrorDetail + reject（带 userMessage）给调用方显示；Login.vue catch 逻辑本就正确无需改。普通 tab + PWA 都受益。无表结构变更，build 通过。详见 [BUGFIX_登录401刷新冲掉错误.md](cc/BUGFIX_登录401刷新冲掉错误.md)
- Edge PWA 点击/刷新唤起外部浏览器定性（非应用 bug）：Edge 安装的 PWA 里点侧边菜单或刷新把焦点甩给 Edge 主窗口（新建空白窗口 about:blank 无标签 / 激活已有窗口不打开页面），应用内 SPA 跳转本身正常。排查四连排除 + 二分 + 运行时 hook：① grep src 无 window.open/target=_blank ② dist manifest scope '/' 正常 ③ 二分注释 display_override(WCO)/protocol_handlers 仍唤起（排除）④ dist 的 window.open 在 echarts chunk 是 openLink（format.js Tc 函数）但项目 graphic 无 link 配置=库死代码不调用 ⑤ Console hook window.open 点击无调用 ⑥ bubble hook 确认 `<a>` 点击 defaultPrevented=true（拦截器/Router 正常 preventDefault 非漏拦；capture 阶段读 defaultPrevented 必 false 无信息量是踩过的坑）⑦ 关键线索"刷新也唤起"说明非点击/`<a>` 特有 ⑧ **Chrome 对照：Chrome 装 PWA 完全正常** → 实锤 Edge PWA standalone 窗口管理特性/bug，非应用代码。结论：Edge 特有行为代码改不动，从 edge://apps 应用设置绕或忽略，部署后 Chrome/其他浏览器正常。附：main.ts 加全局 click 拦截器（站内 `<a href="/...">` preventDefault + router.push 兜底，覆盖 v-list-item/v-btn/router-link，Chrome 也受益）保留。教训：PWA 点链接开外部先 Chrome 对照判浏览器特有行为再查代码。详见 [DEBUG_PWA唤起Edge定性.md](cc/DEBUG_PWA唤起Edge定性.md)

- 主题自动模式 + 地图切换按钮深色修复：① 主题三态化（浅色/深色/自动跟随系统）。根因 [useTheme.ts](frontend/src/composables/useTheme.ts) 早已写好完整三态（light/dark/system + localStorage 持久化 + matchMedia 监听 + 应用 Vuetify）却是死代码（全项目零引用），实际在跑的 [AppLayout](frontend/src/components/layout/AppLayout.vue)/[ProfileView](frontend/src/views/profile/ProfileView.vue)/DesktopNav 3 处 inline 二态逻辑不持久化、不监听系统、刷新回浅色，且 [index.html](frontend/index.html) 无 head bootstrap 刷新必闪白。方案：复活 useTheme.ts 并单例化（themeMode/prefersDark/matchMedia 监听/actualTheme computed/toggle/setTheme 全提模块顶层只初始化一次，useThemeToggle 仅绑 Vuetify 一次，vuetifyBound 标志位防重复）作唯一主题来源；index.html head 加内联 style（html[data-theme] body 背景）+ script（读 localStorage + prefers-color-scheme 解析、写 window.`__INITIAL_THEME__` + html data-theme）防 FOUC；[vuetify.ts](frontend/src/plugins/vuetify.ts) defaultTheme 读 `__INITIAL_THEME__`；useTheme 运行时同步 data-theme 驱动 body 防闪背景跟随切换。AppLayout/ProfileView 接入 useThemeToggle 删 inline；ProfileView「主题切换」项按用户拍板改原地 v-btn-toggle 三态（浅色/深色/自动，w-100 三等分，v-model 绑单例 themeMode ref 写入即持久化+应用），侧边栏改三态图标按钮组（仅图标，对齐个人中心；桌面 rail 收起模式退化为单图标、单击 useTheme.ts toggleTheme 三态循环 浅色→深色→自动，避免窄条挤三按钮）。② 商家地图切换按钮组深色文字太浅：病灶 [MerchantMapView.vue:1045](frontend/src/components/map/MerchantMapView.vue#L1045) `.desktop-layer-selector { background: white }` 硬编码白底，未选中按钮 `variant=text` 文字跟随 on-surface（深色 #E3E3DB 浅灰）落白底看不清，改 `rgb(var(--v-theme-surface))` 跟随主题（与该文件 L1074 .map-loading-overlay 写法对齐）；选中按钮绿底本就正常不动；移动端 v-select 自带 surface 底不受影响。无表结构变更，纯前端 6 文件改动 + 删除死代码 DesktopNav.vue，build 通过（28.38s / 14.97s）。DesktopNav.vue 死代码已删（用户确认）。详见 [FEATURE_主题自动模式与地图按钮深色修复.md](cc/FEATURE_主题自动模式与地图按钮深色修复.md)
- 待审提示从个人中心挪至后台首页「提议审核台」入口：其他用户提交提议后，管理员个人中心「我的提议」菜单项多出「N 条待审」chip——该提示属审核台职责、不应在此（且原 [ProfileView loadPendingCount](frontend/src/views/profile/ProfileView.vue) 漏传 scope='mine'，管理员走全表语义，亮的还是他人 pending，同 commit `72fdead` 同源漏网）。用户拍板「换地方」而非「修 scope」：删个人中心 badge（[ProfileView](frontend/src/views/profile/ProfileView.vue) 删 chip 模板 + loadPendingCount + pendingProposalCount ref + onMounted 调用 + listProposals import），挪到 [AdminDashboard:81](frontend/src/views/admin/AdminDashboard.vue#L81)「提议审核台」入口 v-list-item 的 append（与原个人中心 badge 形态位置完全对称），新增 loadPendingCount 调 `listProposals('pending', 100)` **不传 scope** 取全表待审数（审核台管理员视角，正该看全表，与原 bug 语义相反）。审核台页内 [ProposalsView](frontend/src/views/admin/ProposalsView.vue) 不动（默认筛选即「待审」、列表已显条数）。姐姐首轮曾误采「补 scope=mine 保留原位」方案，用户澄清「原先的删掉、审核台该加的没加」后纠正。两文件纯前端改动，无表结构变更，build 通过（25.35s）。详见 [BUGFIX_待审提示挪到审核台入口.md](cc/BUGFIX_待审提示挪到审核台入口.md)
- 我的提议混入他人提议修复：admin/123456 登录后「我的提议」列表出现非管理员添加的内容。根因 [list_proposals](backend/app/api/proposals.py#L194) 按角色分流——管理员分支不过滤 proposer_id 看全表，而「我的提议」与审核台共用 `GET /proposals`、前端不传区分参数，admin 的「我的提议」= 全部提议（含他人 pending）。admin 直写 [apply_as_admin](backend/app/services/proposals/service.py#L177) 留痕（proposer_id=admin、status=applied）在「我的提议」正常，混入的是别的普通用户提交的 pending。修复 GET /proposals 加 `scope=mine|all` 参数（mine 时无论角色都按 proposer_id 过滤、不传保持原行为向后兼容），前端 [listProposals](frontend/src/api/proposals.ts) 透传 scope + [MyProposalsView:226](frontend/src/views/profile/MyProposalsView.vue#L226) 传 `mine`，审核台 [ProposalsView](frontend/src/views/admin/ProposalsView.vue) 不动。用户拍板 admin「我的提议」= proposer_id 为自己的全部（含直写留痕）。无表结构变更。验证：TDD 红绿（[test_proposals_scope.py](backend/tests/test_proposals_scope.py) 3 passed：admin 默认全部/admin mine 只自己/普通用户 mine 只自己）+ proposals 回归 71 passed（1 failed 经 git stash 回基线 `a5d695a` 证实为预存商家执行器 bug，无关）+ py_compile + build 31.59s。详见 [BUGFIX_我的提议混入他人提议.md](cc/BUGFIX_我的提议混入他人提议.md)
- 邮件模板初始化漏接修复：`email_templates` 表为空、审核通知邮件全被静默跳过（[email_service.send_template_async:67](backend/app/services/email_service.py#L67) 查不到模板只打 warning 不发邮件）。根因与 [FEATURE_过敏原seed接入初始化](cc/FEATURE_过敏原seed接入初始化.md) 同构——3 条默认模板（proposal_submitted/approved/rejected）数据只在 alembic [20260704_0001:47-80](backend/alembic/versions/20260704_0001_add_email_config_tables.py#L47) 的 `op.execute("INSERT...")` 和手动 SQL 脚本里，但项目走 create_all 不跑 alembic（create_all 只建表不插数据），而 [main.py](backend/app/main.py) lifespan 零接入邮件模板 seed（grep `email_template`/`smtp`/`template` 全文零匹配，`init_default_data` 只塞单位/分类、lifespan 只接了 `ensure_allergen_groups`）。修复照搬过敏原 seed 模式：抽 [email_template_seed.py](backend/app/services/email_template_seed.py) 作唯一数据源（`EMAIL_TEMPLATES` 常量从 alembic 原样抽取），lifespan 在 ensure_allergen_groups 之后接 `ensure_email_templates`（try/except 失败不阻断启动）。幂等策略用「按 key 逐个补齐」而非过敏原的「count>0 跳过」——模板可被管理员经 `PUT /admin/email-config/templates/{key}` 编辑，按 key 补齐既保护已编辑、又能在中途失败后补齐缺失项。注意 body_html 必须用普通字面量（隐式拼接）不能用 f-string——模板内 `${var}` 是 `string.Template` 语法（与 send_template_async 的 safe_substitute 对齐），f-string 会误解析报错。无表结构变更（表早由 create_all 建，业务数据非 schema 变更，无需新 alembic/SQL），3 单测 passed（空表创建/补齐不覆盖管理员编辑/双调幂等）+ py_compile。后端 reload 后 lifespan 自动往 PG 插 3 条。详见 [BUGFIX_邮件模板seed接入初始化.md](cc/BUGFIX_邮件模板seed接入初始化.md)
- 菜谱详情删除按钮禁用逻辑写反：管理员在菜谱详情页删已发布菜谱，按钮被禁用（tooltip「已发布菜谱不可删除，请联系管理员」），普通用户反而按钮亮着（虽点后端 403 提示）。根因 [RecipeDetail.vue:26](frontend/src/views/recipes/RecipeDetail.vue#L26) 删除按钮 `v-if="!isPublished || !canManage"` 把「可用条件」写成「未发布 或 非管理员」，正好反了——管理员+已发布落 v-else 禁用、普通用户+已发布落 v-if 可用；后端 [recipes.py delete_recipe:628-638](backend/app/api/recipes.py#L628) 权限规则其实正确（管理员任意删；普通用户删别人/删已发布含自己发布的均 403），纯前端判断与后端拧着。修复 `v-if` 翻正为 `canManage || !isPublished`（管理员或未发布→可用按钮；普通用户+已发布→v-else 禁用+tooltip），顺带把 [canManage](frontend/src/views/recipes/RecipeDetail.vue#L562) 注释写实（变量仅反映是否管理员，原注释「是否可管理（删除/撤回）」易被误读成「能否删除」——正是本次 bug 的认知温床）。`canManage` 全文件仅此一处使用、零连带副作用；无独立「撤回」按钮故无同款连带。无表结构变更，build 通过（31.40s）。详见 [BUGFIX_菜谱删除按钮禁用逻辑.md](cc/BUGFIX_菜谱删除按钮禁用逻辑.md)
- 推荐页配图路径残缺修复：午餐「瘦肉土豆片」配图消失。根因是后端实际连 **PostgreSQL**（非 CLAUDE.md 所说 SQLite——见自动记忆 [backend-uses-postgresql]），PG 里 177 条有图菜谱 `images` 全是残缺路径 `images/xxx.jpg`（缺 `/static/` 前缀和 `recipes/` 段；SQLite 旧库反而规范，两库不同步）——源头是 [json_recipe_import_service _download_image](backend/app/services/json_recipe_import_service.py#L72) 失败时 fallback 存残缺仓库相对路径，PG 空库重导时临时目录无图全走 fallback；前端 [MealCard recipeImage](frontend/src/components/meals/MealCard.vue) 旧 `startsWith('/')` 判断不成立、又独缺其余 4 处 `getImageUrl` 都有的「远程仓库」兜底分支，致残缺路径直出浏览器 404。三管齐下：①批量 UPDATE PG 177 条 images 规范化（备份 [recipe_images_backup_20260708.json](backend/data/recipe_images_backup_20260708.json)，残缺归零）；②导入失败跳过不存残缺路径；③抽 [utils/image.ts resolveImageUrl](frontend/src/utils/image.ts)（三条分支含远程兜底），MealCard/RecipesView/RecipeDetail/ImageManager/RecipeEditDiff 5 处 DRY 统一（附带把硬编码 `/api/v1` 改 `VITE_API_URL`）。验证：API 返回规范路径 + 图片直链 200（254KB）+ py_compile + build 32.34s。遗留：id=249 图片文件缺失（路径已规范、物理文件不在盘，需重新获取）。无表结构变更。详见 [BUGFIX_配图路径残缺.md](cc/BUGFIX_配图路径残缺.md)
- 今日推荐滚动条 + 老三样：① `DailyMealsView` `.daily-meals-view` 设 `min-height: 100vh`（[:134](frontend/src/views/meals/DailyMealsView.vue#L134)），但容器在 `<v-main>` 内、头顶压 app-bar，100vh 超出可用高度 → 内容再少也溢出竖向滚动条（全局仅此一处用此写法）。② [meal_recommender _pick_best_recipe:201](backend/app/services/meal_recommender.py#L201) 是确定性 argmax（`if score > best_score`），用户目标/菜谱池/营养成本计算天天相同 → 三餐最高分菜谱永远同一道。修复：删 `min-height: 100vh`；`_pick_best_recipe` 改「按分数加权随机」——`weight = score + 0.05`（epsilon 防 score=0 全零、也给低分微机会），`random.choices` 选取、不放大幂次让高分占优但不垄断（用户选「更刺激」档）；refresh 路径自动受益（已 exclude 当前餐，必换不同菜）。无表结构变更，py_compile + build（33.98s）通过。注意当天已缓存推荐不自动重算（generate 先查 existing 直返），看新随机效果需点各餐「换一个」或等次日。详见 [BUGFIX_今日推荐滚动条与随机性.md](cc/BUGFIX_今日推荐滚动条与随机性.md)
- 原料价格区间 1000 倍 + 最新价标签错位修复：`/data/ingredients/142` 显示「¥9.19/个」+「区间 ¥0.01-¥0.01/千克」（单位打架、min==max==0.01）。根因两 bug：① 后端 [nutrition.py latest-price:941](backend/app/api/nutrition.py#L941) 按 `default_mass_unit_id`（质量偏好=kg）折算=9.19 元/kg，前端 [loadLatestPrice:3119](frontend/src/views/ingredients/IngredientDetail.vue#L3119) 没取 `response.unit`，[模板:214](frontend/src/views/ingredients/IngredientDetail.vue#L214) 硬贴 `priceUnitName`（=「个」价格偏好）标签错位（综合价 [:296](frontend/src/views/ingredients/IngredientDetail.vue#L296) 同款）；② [chartData:2961](frontend/src/views/ingredients/IngredientDetail.vue#L2961) `defaultUnit=massUnitName.value` 取单位**名「千克」**，但 [unitFactors:2983](frontend/src/views/ingredients/IngredientDetail.vue#L2983) key 全是**缩写**（'kg':1000 无「千克」键）→ toFactor=undefined→1 → 单价 ×(1/500) 少 1000 倍，三条记录 0.00598~0.0116 toFixed(2) 全=0.01。与 [VOLUME_MASS_CONVERSION_1000X](cc/VOLUME_MASS_CONVERSION_1000X_BUG.md) 同源。修复 [IngredientDetail.vue](frontend/src/views/ingredients/IngredientDetail.vue) 3 处：解构加 `massUnit`、最新价+综合价 `priceUnitName`→`massUnitName`（replace_all 两处，与后端 mass 口径/区间 chartUnit 一致）、chartData `defaultUnit` 改 `massUnit.value?.abbreviation ?? '斤'`（堵 1000 倍 + 注释 name/abbr 陷阱）。ProductDetail 确认无同款（其 chartData 用 `standard_quantity`+`convertFromJin` abbr 路径，正确，也解释了为何只原料页中招——两页 chartData 不同构、原料页是劣化版）。无表结构变更，build 通过（34.22s），手算修复后区间 5.98-11.60 元/kg、9.19 落区间内自洽。遗留：后端 latest-price 按 mass 归一化不直观（想按「元/个」需改 target 口径+piece_weight，原料 142 piece_weight=None），未动；原料页 chartData 长远应统一为商品页 standard_quantity 路径（DRY）。详见 [BUGFIX_原料价格区间1000倍与标签错位.md](cc/BUGFIX_原料价格区间1000倍与标签错位.md)
- 原料详情页「默认单位」弃用清理：`/data/ingredients/142` 基本信息显示「默认单位:个」、编辑表单设「斤」却无效。根因三层语义错位——展示行标签「默认单位」却绑 `overlaidDefaultUnitName`（[IngredientDetail.vue:1953](frontend/src/views/ingredients/IngredientDetail.vue#L1953) 定义为 `priceUnitName`，即用户级价格偏好；当前用户 billding `default_price_unit_id=18`=「个」）；编辑字段 `basicEditForm.default_unit_id` 是死字段（后端 [ingredient_extended.py](backend/app/api/ingredient_extended.py) grep `default_unit` 零匹配、ingredients 表 PRAGMA 已无此列，保存 payload 被静默忽略，打开恒 `|| jinUnitId` 回退「斤」）；属 [FEATURE_用户级默认单位配置](cc/FEATURE_用户级默认单位配置.md) 前端基本信息区扫尾遗漏（当时只清了 priceForm）。修复 [IngredientDetail.vue](frontend/src/views/ingredients/IngredientDetail.vue) 单文件：删展示行 v-list-item + 删编辑表单 v-autocomplete + 价格显示两处 `overlaidDefaultUnitName||'斤'`→`priceUnitName`（已含 fallback）+ 清理 `overlaidDefaultUnitName` computed / `Ingredient` 接口 `default_unit_id?` / `basicEditForm.default_unit_id` / startEditBasicInfo / saveBasicInfo payload / `jinUnitId` 死代码（含 loadUnits 找斤赋值）。无表结构变更，grep 零残留，build 通过（IngredientDetail chunk 91.45 kB）。备注：原料级默认单位概念已迁用户级偏好，原料不应再有此属性；用户想要「斤」显示应去个人中心改「价格单位」偏好。详见 [BUGFIX_原料默认单位弃用清理.md](cc/BUGFIX_原料默认单位弃用清理.md)
- 商品加权价格体验与口径修补：加权机制上线后 4 个问题。①别名 hint 与全局权重文本重叠（别名框去 persistent-hint + 权重区 mt 分隔）②普通用户不该看到全局权重框（去掉只读框，全局栏整块不渲染，仅管理员见）③权重数字框改滑块（参考关系强度 v-slider；min=0 因 0=排除有意义；「我的覆盖」用 v-switch 开关+滑块表达「留空=用全局」，对应后端有无覆盖记录语义）④原料详情页「各商家价格/价格趋势」未加权。总原则「权重只插手平均值语义」，min/max/count/迷你折线不动。后端新增 `GET /nutrition/ingredients/{id}/product-weights` 批量生效权重端点（供趋势加权 avg，免 N 次 my-weight）+ 改 [nutrition.py _lookup_merchant_prices](backend/app/api/nutrition.py#L1062) 同商家多商品按权加权（复用 [_resolve_weight](backend/app/services/ingredient_price_service.py#L97)，查询/单位折算/子食材回退链不动）；前端 [IngredientDetail chartData](frontend/src/views/ingredients/IngredientDetail.vue#L2965) avg 按日×商品加权（只改 avg）+ [PriceTrendChart](frontend/src/components/charts/PriceTrendChart.vue) 加 avgLabel prop 原料页传「加权平均」（商品页默认「平均」）+ 各商家列表底部加「商品加权综合价」行（复用 latestPrice）；[ProductDetail](frontend/src/views/products/ProductDetail.vue)/[IngredientDetail](frontend/src/views/ingredients/IngredientDetail.vue) 两处权重编辑区同改滑块+开关。无表结构变更。验证：后端 5 测试 passed（test_ingredient_product_weights 3 + test_ingredient_merchant_weighted 2）+ py_compile；前端 build 通过。续修（用户反馈）：原料页改商品权重后价格不刷新（[saveProduct](frontend/src/views/ingredients/IngredientDetail.vue#L2061)/[deleteProduct](frontend/src/views/ingredients/IngredientDetail.vue#L2134) 保存后没重拉加权数据）→ 两处成功分支后 reload latestPrice/merchantPrices/productWeights（chartData 跟 productWeights 自动重算），覆盖编辑/新增/删除三场景。详见 [BUGFIX_加权价格体验修补.md](cc/BUGFIX_加权价格体验修补.md)
- 原料详情页 TDZ 崩溃修复：访问 `/data/ingredients/:id` 白屏，控制台报 `Cannot access 'priceUnitName' before initialization at setup (IngredientDetail.vue:2093)`。根因是 ES 模块 `const` TDZ——[IngredientDetail.vue](frontend/src/views/ingredients/IngredientDetail.vue) 中 `const { energyUnit, priceUnitName, massUnitName } = useUserUnits()` 声明在原 2145 行，但 [editPriceForm](frontend/src/views/ingredients/IngredientDetail.vue#L2090) 的 `ref({ ... unit: priceUnitName.value ... })` 在原 2093 行 setup 执行时立即求值（ref 参数是普通对象字面量非懒求值），此时 priceUnitName 未声明 → TDZ 抛错整个 setup 崩；同款引用的 overlaidDefaultUnitName（原 1876）因裹在 computed 懒求值里侥幸没炸（这也是报错只在 2093 的原因）。属「用户级默认单位配置」（[FEATURE_用户级默认单位配置.md](cc/FEATURE_用户级默认单位配置.md)）Phase F 手工替代失败 subagent 时漏的声明顺序。对比 [ProductDetail.vue](frontend/src/views/products/ProductDetail.vue) 同款改造声明（1401）在使用（1529）前未炸，佐证单纯顺序问题。修复：把 useUserUnits() 解构前置到 overlaidDefaultUnitName（1876）之前（现 1875），NUTRIENT_DEFINITIONS 留原位（依赖 energyUnit 已在声明后），三处使用点（1879/2096/3918）全在声明之后；useUserUnits 是纯 composable 仅依赖 Pinia store 提前安全。纯前端单文件 ~3 行，无表结构变更，build 通过（IngredientDetail chunk 88.19 kB 正常）。详见 [BUGFIX_原料详情页TDZ单位变量.md](cc/BUGFIX_原料详情页TDZ单位变量.md)
- 菜谱列表漏导入菜谱（NULL 三值逻辑缝隙）：审计外键修复把 howtocook 导入 Recipe 改 `user_id=None`（见下条 BUGFIX_audit_field_pg_fk_violation）后，PG 注册用户看菜谱列表为空。根因是 [recipes.py:179](backend/app/api/recipes.py#L179) 公共菜谱子查询 `Recipe.user_id != current_user.id`——导入菜谱 user_id=None，SQL 三值逻辑下 `NULL != me` 为 NULL 非 True，公共分支 AND 塌空，`union_all` 两边（「我的」`user_id==me` 也 False）都落空。修复：公共分支加 `or_(user_id != me, user_id.is_(None))` 显式接纳无主菜谱。验证 py_compile + 走查（导入菜谱命中、别人私有仍不可见、我的不重复）+ 确认详情页 [:375](backend/app/api/recipes.py#L375) 本就 `or_(user_id==me, source!=None)` 无缝隙；grep 确认全库仅此一处 `Recipe.user_id != me`。NULL 三值逻辑是 SQL 标准，三库一致受益。详见 [BUGFIX_recipe_list_null_seam.md](cc/BUGFIX_recipe_list_null_seam.md)

- JSON 列 != 比较致 PG UndefinedFunction：审计外键那关过后 PG 启动导入又崩 `操作符不存在: json <> unknown`（`ingredients.aliases != '[]'`）。根因是 `Ingredient.aliases = Column(JSON)`（[nutrition.py:28](backend/app/models/nutrition.py#L28)），三处「按别名查找」查询用 `Ingredient.aliases != "[]"`（[enhanced_recipe_import_service.py:67](backend/app/services/enhanced_recipe_import_service.py#L67)、[json_recipe_import_service.py:66](backend/app/services/json_recipe_import_service.py#L66)、[howtocook.py:298](backend/app/services/importer/importers/howtocook.py#L298)）——SQLite JSON 存文本直接比 OK、PG json 类型无 `<>` 字面量操作符必崩、MySQL 又另一套。关键：三处 Python 层 `if c.aliases and name in c.aliases` 已对空 list 兜底，SQL 的 `!= "[]"` 是冗余预过滤。修复：删 SQL 比较、保留 `isnot(None)`（IS NOT NULL 三库通用）+ 行内注释，避开 JSON 列跨库 SQL 比较整片雷区（比 cast(Text)/json_array_length/改 jsonb 都干净）。验证 py_compile + grep 确认无残留且无其他 JSON 空值比较。遗留：[ingredient_matcher.py:359](backend/app/services/ingredient_matcher.py#L359) `aliases.op('?')` 用 PG json `?` 操作符（PG 支持、SQLite 不支持），当前 PG 未触发，待 SQLite 端遇到再处理。详见 [BUGFIX_json_col_pg_not_equal.md](cc/BUGFIX_json_col_pg_not_equal.md)

- 审计字段外键致 PG 空库导入违反外键：boolean 那关修好后 `create_all` 过了，启动 lifespan 导入初始数据又崩 `products_created_by_fkey`（`(created_by)=(1)没有在表users中出现`）。根因是 AuditMixin 的 created_by/updated_by 带 `ForeignKey("users.id")`（[base_model.py:13](backend/app/core/base_model.py#L13)、[:30](backend/app/core/base_model.py#L30)）+ 启动导入写死 `user_id=1`（[main.py:236](backend/app/main.py#L236)/[:243](backend/app/main.py#L243)/[:270](backend/app/main.py#L270)、[howtocook.py:149](backend/app/services/importer/importers/howtocook.py#L149)/[:207](backend/app/services/importer/importers/howtocook.py#L207)）；SQLite 外键默认 OFF + 开发库恰有 user 1 故从未暴露，PG 空库外键强制立即触发。崩在 products 不在 ingredients，因 howtocook 导入 Ingredient 不设 created_by（NULL）、建同名 Product 才 `created_by=1`。用户拍板「约定创建者、修改者填 0、不搞这方面的外键」。修复：base_model.py 去 created_by/updated_by 外键（grep 确认无 relationship 依赖，零误伤，清理 ForeignKey import）+ howtocook Recipe 删 user_id（公共菜谱无作者，Recipe.user_id 外键保留）+ main.py 导入 `user_id=0` + 批量建商品 `created_by=0`。边界：Recipe.user_id（[recipe.py:15](backend/app/models/recipe.py#L15) 有 relationship）、invite_code.created_by（[:34](backend/app/models/invite_code.py#L34) creator relationship）、ProductRecord/UserPlace.user_id 等业务归属字段外键全保留——只动审计字段。验证 py_compile + PG DDL 编译（created_by/updated_by 无 REFERENCES、`Recipe.user_id FOREIGN KEY` 保留）。无需 SQL 脚本（新库 create_all 即生效、SQLite 开发库 foreign_keys OFF 不校验；既有 PG/MySQL 库按需补 DROP CONSTRAINT）。详见 [BUGFIX_audit_field_pg_fk_violation.md](cc/BUGFIX_audit_field_pg_fk_violation.md)

- PostgreSQL boolean server_default 不兼容修复：切 PG 后 `Base.metadata.create_all` 崩 `字段 "is_active" 类型是 boolean, 但默认表达式类型是 integer`（[SQL: ... is_active BOOLEAN DEFAULT 1 NOT NULL]）。根因是 entity_densities/entity_unit_overrides 两表 `is_active` 用 `server_default=sa.text("1")`——SQLite 认（boolean 即 integer 别名）、PG 严格类型校验拒整数默认值（[entity_density.py:24](backend/app/models/entity_density.py#L24)、[entity_unit_override.py:28](backend/app/models/entity_unit_override.py#L28)）；同款 bug 殃及 email_config alembic（use_tls/enabled）+ entity 软删 alembic 两处（[20260704_0001](backend/alembic/versions/20260704_0001_add_email_config_tables.py#L27)、[20260629_c0e1d2f3a4b5](backend/alembic/versions/20260629_c0e1d2f3a4b5_entity_unit_density_soft_delete.py#L175)）。修复全改 `sa.text("true")`/`sa.text("false")`——三库通吃（PG boolean 字面量、SQLite 3.23+ true 当 1 别名、MySQL BOOL=TINYINT(1) true=1），与项目 [task_templates.py:60-62](backend/app/services/agent/task_templates.py#L60) 既定规矩（`WHERE col=true`、禁用 1/0）一致，当初两处漏看自家规矩。验证：py_compile + 三方言 DDL 编译均产 `DEFAULT true` + SQLite 3.50.4 实测 DEFAULT true 解析为 1（Agent 不带 is_active 列的原始 INSERT 兜得住，无回归）。SQL 脚本未动（PG 脚本本就 true/false、SQLite/MySQL 脚本用 0/1 是各自引擎正确惯例）。非表结构变更（字段定义未变，仅 server_default SQL 文本修正），无需新 alembic/SQL 脚本。详见 [BUGFIX_boolean_server_default_pg.md](cc/BUGFIX_boolean_server_default_pg.md)
- 粘贴导入别名匹配缝隙：快速填写「粘贴导入」粘 `青茄` 未匹配到商品「青茄子」（其别名恰为「青茄」）。根因是后端 [products_entity.py:799](backend/app/api/products_entity.py#L799) autocomplete 按名字子串优先判定（`search_lower in product.name.lower()`，「青茄」是「青茄子」子串→标 match_type=name），别名分支是 elif 不再下沉；前端 [PasteImportDialog.vue](frontend/src/components/prices/PasteImportDialog.vue) tryAutoMatch Priority 1 要求 name 且完全相等（「青茄子」≠「青茄」）、Priority 3 只认 `match_type==='alias'`，结果卡缝隙落 unmatched。取证：DB 确认 product 403 aliases=`["青茄"]`、ingredient 212「茄子」别名空、[product_entity.py:18](backend/app/models/product_entity.py#L18) `aliases=Column(JSON)` 别名机制本身没坏、后端返回体已带 aliases 数组（[:827](backend/app/api/products_entity.py#L827)）。修复：前端 Priority 3 兼容自判 `it.match_type==='alias' || (Array.isArray(it.aliases) && it.aliases.includes(row.name))`，不动后端通用搜索语义（autocomplete 被商品搜索框/快速填写/内联搜索共用，改其语义波及面大）。续修：`翅中` 未匹配原料「鸡翅中」（别名 `["翅中"]`）——同根因但别名在 ingredient 上且后端未返原料别名，后端 [:828](backend/app/api/products_entity.py#L828) 加 `ingredient_aliases` 字段 + 前端 Priority 4 同理自判。build + py_compile 通过，静态走查「青茄子/青茄/茄/翅中/鸡翅中/翅根/翅」七种输入无误伤。纯前端 + 后端返回体 +1 字段（向后兼容），无表结构变更。详见 [BUGFIX_粘贴导入别名匹配.md](cc/BUGFIX_粘贴导入别名匹配.md)
- 菜谱推荐长事务导致 database is locked：POST /products 创建价格记录报 `database is locked`（35 秒超时）。根因 [generate_recommendations](backend/app/services/meal_recommender.py) 后台生成循环内 `db.add+db.flush`（持 SQLite 写锁）后继续 `_pick_best_recipe`（扫候选菜谱池 + 算营养/成本，慢计算）、最后才 `db.commit`，写锁持有整个 3 餐循环；并发写（POST /products）等 busy_timeout 30s 超时（[database.py](backend/app/core/database.py) `timeout=30`）。修复改「先算后写」——循环内只 `_pick_best_recipe` 收集 picks（读不持锁）、循环后批量 `db.add+commit`（写锁毫秒级），主分支 + 黑名单重生成分支两处；`refresh_meal_recommendation` 已正确（先 pick 后写）无需改。写锁持有从「整个循环」缩到「批量写」。既有 bug（菜谱推荐后台生长事务），非审核框架改动引入（POST /products 不走审核）。详见 [BUGFIX_菜谱推荐长事务锁.md](cc/BUGFIX_菜谱推荐长事务锁.md)
- 共享转型 schema 漂移修复：后端启动报 `no such column: units.is_standard`。根因是开发库 [livecalc.db](backend/data/livecalc.db) 由 `Base.metadata.create_all()` 建库、**从未走 alembic**（无 `alembic_version` 表），create_all 只建新表、不碰「加列/改约束」的 ALTER，致迁移 [20260627_0002_shared_data_transform.py](backend/alembic/versions/20260627_0002_shared_data_transform.py) 三处变更漂移——`units.is_standard`、`recipes.is_public` 缺列（SELECT 必崩）、`merchants.user_id` 仍 NOT NULL（语义已改可空）。同批两新表（user_merchant_favorites/product_merchant_price_summary）因 create_all 建表机制已落地，反证只漏 ALTER 老表。`alembic upgrade head` 走不通（无版本表 + create_table 撞已存在表）。修复：直接补开发库（备份 `livecalc.db.bak_20260629_103713` 后），两列 ADD COLUMN，`merchants.user_id` 改 nullable 用 alembic `batch_alter_table` 离线执行（SQLite 需表重建，交 alembic 免手写易错 SQL，不依赖 alembic_version）。验证三处生效 + merchants 空表数据完整 + 索引保留 + 模拟 ORM SELECT 跑通。纯开发库欠账（alembic 迁移/模型本就正确未改）。配套：因项目不用 alembic，完善 [20260627_shared_transform_sqlite.sql](backend/scripts/sql/20260627_shared_transform_sqlite.sql) 为可直接手动执行（放开 is_public/is_standard 加列 + 补 merchants.user_id 官方表重建流程，不依赖 alembic；MySQL/PG 本就可直接执行），模拟旧结构测试库 executescript 实跑验证表重建零损（数据/9 列/索引全保留）+ 加列/建表/回填全对。附带记录前序报错：`Settings`（pydantic）`extra=forbid` 拒绝 `backend/.env` 混入的 `VITE_DATA_REPO_IMAGE_BASE`/`VITE_REQUEST_TIMEOUT`/`VITE_LONG_REQUEST_TIMEOUT`（前端变量），用户已删 `.env` 中这三项解决，`extra→ignore` 的根因修法未动。详见 [BUGFIX_共享转型表结构漂移.md](cc/BUGFIX_共享转型表结构漂移.md)
- 共享写端点分流改造（Task 2.5b）：P0 阶段共享数据写端点统一限管理员（`get_current_admin_user`），P1 框架 + P2 业务执行器就绪后升级为分流——管理员 `apply_as_admin`（经框架执行器 apply 立即生效 + change_proposals 留痕 status=applied）/ 普通用户 `submit`（按治理总表策略：auto_approve 立即 / manual 待审）。改造 5 文件：[ingredient_hierarchy.py](backend/app/api/ingredient_hierarchy.py) 食材合并 + 层级 create/update/delete（注意 delete 改软删 is_active=False 支持回滚，原硬删）；[units.py](backend/app/api/units.py) 单位 create/update/delete（执行器 validate 拒改标准单位，delete 软删）；[nutrition.py](backend/app/api/nutrition.py) 食材营养写入（抽 `_build_structured_nutrients` 共用构造，补空→auto 覆盖→manual）；[merchants.py](backend/app/api/merchants.py) 商家 update/delete（delete 不再拒绝有价格商家，改软停用+ProductRecord.merchant_id 置 NULL）；[ingredient_extended.py](backend/app/api/ingredient_extended.py) 硬删除保留管理员直写（高危不可逆，执行器软删语义不符）。未进分流（同模式待续）：units 换算/覆盖/密度（执行器未覆盖多表）、nutrition 商品营养/correct（写 Product.custom_nutrition_data / IngredientNutritionMapping，与 NutritionData 执行器语义不符）、products_entity 条码/别名。测试：P0 改造 + 新增 8 条分流测试（[test_shared_data.py](backend/tests/test_shared_data.py) 管理员直写 + 普通提议各一类，覆盖 ingredient.merge/unit.create/hierarchy.create/merchant.update）；P0 加 autouse fixture 显式 `register_all()`（TestClient 不触发 main.py lifespan）。`pytest tests/test_permissions_p0.py tests/test_proposals_framework.py tests/test_shared_data.py` 46 passed；全量回归 16 failed/368 passed（失败数与基线一致——全是预存 auth/import/locations/recipes/reports 等无关失败，通过数从 360 升到 368）。py_compile 全通过，无表结构变更。详见 [BUGFIX_共享写端点分流改造.md](cc/BUGFIX_共享写端点分流改造.md)
- 食材合并路由重复修复：启动报 `Duplicate Operation ID merge_ingredients_api_v1_ingredients_merge_post`。根因是已删的 `ingredient_merge.py` 与 [ingredient_hierarchy.py:329](backend/app/api/ingredient_hierarchy.py#L329) 都注册了 `POST /api/v1/ingredients/merge` 且函数同名 `merge_ingredients` → operation ID 撞车；更隐蔽的是 [main.py](backend/app/main.py) 里 ingredient_merge.router 先注册，Starlette 按序匹配先注册胜出，实际生效的是宽松版（仅 401），hierarchy 后来加的 `is_admin`（403）管理员校验形同虚设。修复：删除孤立的早期遗留文件 ingredient_merge.py（整个文件仅此一个重复路由），保留 hierarchy 完整实现（含 merge-history/merge-status 配套），同步清理 main.py 的 import 与 include_router；schema 文件 app/schemas/ingredient_merge.py 被 nutrition.py 引用故保留。权限规则经需求澄清细化（非「仅管理员」）：管理员可合并任意食材；普通用户只能把自己创建的源食材合并进自己创建的目标食材（源与目标 created_by 均须为本人，系统/历史数据 created_by 为 NULL 一并挡住），实现于 merge_ingredients（查涉及食材 created_by 逐个比对，非本人则 403 并列出食材名），created_by 由 AuditMixin 提供无需改表；合并仍会迁移食材的所有引用（含他人菜谱），本次只卡所有权不限制跨用户引用迁移。py_compile 通过、grep 确认无遗留引用，无表结构变更。详见 [BUGFIX_食材合并路由重复.md](cc/BUGFIX_食材合并路由重复.md)
- 快速填写排序写入失败修复：「按填写顺序排序」从未生效、历史商品始终按拼音序。根因是 [merchants.py](backend/app/api/merchants.py) `save_product_orders`（`POST /merchants/{id}/product-orders`）upsert 循环只在请求开始查一次当天已有记录塞进 `existing` 字典，本轮 `db.add` 的新对象没登记回去；粘贴导入时 `product_ids` 含重复 pid（error.log 实测 622 出现两次）→ 第二次遇同 pid 仍走 add → 同 `(user,merchant,product,date)` UNIQUE 冲突 → IntegrityError 500。前端 `saveAll`/`onPasteImported` 用 try/catch+console.warn 静默吞错，故 `user_merchant_product_orders` count 恒 0、`get_merchant_product_prices` 算不出 `custom_sort_score`（全 undefined）、前端全回退「分类+拼音」分支。取证：error.log 实锤 `[parameters:(1,1,622,'2026-06-27',26)]` + TestClient 复现 `[325,78,325,1,30]`→500；而小量不重复 pid 与 DB 直写均 200（排除表/鉴权/路由/序列化）。修复：加 `seen` 字典跟踪本轮记录、`existing` 改以 pid 为键（本请求内 user/merchant/date 固定），重复 pid 时更新 sort_order 不再重复 add。TestClient 验证 200 + 去重（325 仅一条、sort_order=最后位置）+ 同日 upsert 不新增。无表结构变更，无需 SQL/alembic。前端配套：手动 `saveAll` 原按 `visibleHistoryRows`（显示顺序=拼音序）收集 savedProductIds，经确认改为按填写顺序——[QuickFillView.vue](frontend/src/views/prices/QuickFillView.vue) `FillRow` 加 `filledAt`（首次填上有效价的时间戳），价格框 `@update:model-value`→新增 `onPriceChange` 记时，`saveAll` 按 `filledAt` 升序排序收集 savedProductIds；粘贴导入本就是文本顺序=填写顺序无需改。前端 `npm run build` 通过。详见 [BUGFIX_快速填写排序写入失败.md](cc/BUGFIX_快速填写排序写入失败.md)
- 连接池耗尽修复：后端报 `QueuePool limit of size 10 overflow 20 reached`（栈顶 `resolve_user_from_token` 是受害者非凶手——它是每请求第一个借连接的点，池一空先炸）。根因是「长持有并发叠加」打爆 30 连接池：`run_agent_loop` 主 db 每会话占 1 连接达数分钟、SSE `stream_session` 的 `Depends(get_db)` 整个流期间白占连接（鉴权后进 event_gen 再没用过）、`resolve_user_from_token` 的 `next(get_db())` 反模式异常路径归还依赖 GC 且正常路径因 generator 立即被 GC 而借两次连接。三处 `SessionLocal()` 使用点排查均无永久泄漏（都有 try/finally+close）。修复三板斧：(1) [security.py](backend/app/core/security.py) `resolve_user_from_token` 改 `SessionLocal()`+try/finally 消除反模式；(2) [agent_api.py](backend/app/api/agent_api.py) `stream_session` 去掉 `Depends(get_db)` 改短命鉴权 session（捕获 `initial_status` 标量避免碰 detached `sess`）；(3) [database.py](backend/app/core/database.py) SQLite 池扩容 size 15/overflow 30。静态验证 py_compile+import+grep 通过，单测因 .venv 未装 pytest 未跑，运行时待观察。详见 [BUGFIX_连接池耗尽.md](cc/BUGFIX_连接池耗尽.md)
- 邀请码过期时间无法指定修复：后台「创建/编辑邀请码」的「过期时间」栏空白点不动。根因三叠：(1) 该栏用 `<v-date-input>`，Vuetify 3.12.4 里属 labs 组件，项目 [vuetify.ts](frontend/src/plugins/vuetify.ts) 只注册 `vuetify/components` 稳定版、未注册 labs（`vite-plugin-vuetify` 的 autoImport 只管样式不碰组件注册），运行时 `Failed to resolve component` 渲染空壳，且该组件只到「天」承载不了时间点；(2) 后端 `is_expired` 用 `naive < aware(now utc)`，SQLite 读回的 `DateTime(timezone=True)` 实为 naive（参 [products.py:459](backend/app/api/products.py#L459) 也要手动转 naive）→ 修好前端即触发 TypeError（此前因 expires_at 全 NULL 短路没崩）；(3) `_serialize_invite_code` 用 `strftime` 抹了时区，前端 [formatToLocalDateTimeShort](frontend/src/utils/timezone.ts)（要求输入带时区）显示偏。修复按「后端统一 UTC、前端按本地时区」：前端 `v-date-input`→`v-text-field type=datetime-local`（复用 PricesView 既有模式 + 新增局部 `toDatetimeLocalValue` 回填 + `getLocalDateTimeString` 做 min）、提交 `new Date(local).toISOString()`；模型加 `ensure_utc`（naive 视作 UTC）改 `is_expired` 防 TypeError；序列化 `expires_at`/`created_at` 改 `ensure_utc(dt).isoformat()` 带 `+00:00`。新增 [test_invite_code.py](backend/tests/models/test_invite_code.py) 10 例（含 naive/aware 防 TypeError）。build 通过 + 单测 10 passed；UI 实测待管理员账号。详见 [BUGFIX_邀请码过期时间.md](cc/BUGFIX_邀请码过期时间.md)
- 邀请码 schema 漂移修复：创建邀请码报 `no such column: invite_codes.used_count`（[invite_codes.py:47](backend/app/api/invite_codes.py#L47) 查询触发）。根因是开发库 schema 与 `alembic_version` 漂移——0002 迁移（[20260622_0002](backend/alembic/versions/20260622_0002_add_system_config_and_enhance_invite_codes.py)）里 system_config 建表落地了，但 invite_codes 的 batch_alter_table（used→used_count + max_uses）没落地，而版本号已盖到 0003，故 `alembic upgrade head` 无效。模型/迁移/SQL 脚本本身正确，纯环境漂移。不改代码，直接补两列（RENAME used→used_count + ADD max_uses，与已提交的 [sqlite 脚本](backend/scripts/sql/20260622_user_and_invite_code_enhancement_sqlite.sql) 一致），操作前备份；ORM 层验证查询/插入/属性全通。详见 [BUGFIX_邀请码schema漂移.md](cc/BUGFIX_邀请码schema漂移.md)
- 注册校验对齐与错误透传：注册 `username="a"` 前端放过、后端 422 拒，且报错只显示「请求参数验证失败」不指明字段。根因全在前端 Register.vue：username/email 只校验非空（缺后端要求的 3-50 字符 / 邮箱格式）+ catch 读笼统 `detail` 没用拦截器拼好的 `userMessage`（后端其实已在 `errors` 数组返回字段详情、`extractErrorDetail` 也已正确解析）。纯前端修复：补 username 3-50 + email 格式正则、catch 改读 `error.userMessage`，后端零改动。build 通过 + MCP 实测 `username="a"` 被前端拦（中文提示「用户名需 3-50 个字符」+ 无 `POST /auth/register`）。详见 [BUGFIX_注册校验对齐与错误透传.md](cc/BUGFIX_注册校验对齐与错误透传.md)
- 地图 SDK 点击标记偏移修复：商家管理添加/修改商家用各地图 SDK（高德/百度/腾讯）选点时点击地图，标记偏离点击处（火星偏移）、保存坐标也错。根因是三个 SDK 引擎（`AMapSDKEngine`/`BaiduSDKEngine`/`TencentSDKEngine`）违背「与调用方传地图坐标系原始坐标」契约（Leaflet 引擎都遵守），内部 click/markerDragend（百度/腾讯连 init center）多做一次 →WGS84 转换，致 `MapPicker` 双重转换且把 WGS84 当地图坐标画到偏移底图。修复：三个 SDK 引擎统一为「传啥用啥」契约，去掉引擎内所有 `convertCoordinate`，转换职责回归调用方，清未用 import。仅影响 MapPicker，MerchantMapView 不受影响。详见 [BUGFIX_地图SDK点击标记偏移.md](cc/BUGFIX_地图SDK点击标记偏移.md)
- 本地路径导入改用 .env 配置：数据维护中心「从本地路径导入」不再在页面手填目录路径，改为直接复用 `.env` 的 `DATA_LOCAL_PATH`（与启动导入共用同一来源，消除配置漂移）。后端 `trigger_local_import` 去掉 `local_path` 参数改读 `settings.data_local_path`（未配置/目录不存在各一 400）、新增只读 `GET /data/local-path-config`；前端删输入框换三态只读展示、未配置禁用按钮、点击不再传参。详见 [BUGFIX_本地路径导入改用env配置.md](cc/BUGFIX_本地路径导入改用env配置.md)
- 移动端优化和功能修复：详见 [BUGFIX_移动端优化和功能修复.md](cc/BUGFIX_移动端优化和功能修复.md) - 记录了移动端输入框放大、菜谱搜索功能、菜谱成本更新和商品自动完成功能的修复
- None值处理修复：详见 [NONE_VALUE_FIXES.md](cc/NONE_VALUE_FIXES.md) - 修复了在计算菜谱成本和营养素时未将None值视为0的问题
- 地图配置持久化：实现了地图配置的数据库存储功能，解决了地图API密钥等配置重启后丢失的问题。详细信息请见 [MAP_CONFIG_PERSISTENCE.md](cc/MAP_CONFIG_PERSISTENCE.md)
- 鸡蛋价格显示异常修复：修复了在原料管理中价格显示异常的问题，确保价格历史列表与价格趋势图表的显示逻辑一致。详细信息请见 [EGG_PRICE_DISPLAY_FIX.md](cc/EGG_PRICE_DISPLAY_FIX.md)
- 默认单位改为斤：商品和原料的默认单位统一改为「斤」，涉及后端 API 创建接口、导入服务和前端表单默认值。详见 [DEFAULT_UNIT_JIN.md](cc/DEFAULT_UNIT_JIN.md)
- quantity_range 成本计算修复：修复了使用 quantity_range（用量区间）的食材成本显示为 0 的问题。在成本计算函数中新增 `_get_effective_quantity()` 辅助函数，当 quantity 为 None 时从 quantity_range 取平均值计算成本。详见 [QUANTITY_RANGE_COST_FIX.md](cc/QUANTITY_RANGE_COST_FIX.md)
- 成本计算单位转换 & substitutable 回退：修复成本计算中价格记录单位（如"个"）与菜谱用量单位（如"克"）不同时直接相乘导致成本异常偏高的问题；同时让 substitutable（可替代）关系也能作为价格/营养回退源。详见 [BUGFIX_UNIT_CONVERSION_IN_COST.md](cc/BUGFIX_UNIT_CONVERSION_IN_COST.md)

- 快速填写移动端三处修复：新增商品搜索下拉与 iOS 地址栏重叠（菜单改 `location: 'top'`）、单位选择器闪退且只有斤/个（`v-select`+`@blur` 改 `v-menu`+`v-list`，`loadUnits` 加 `is_common` 过滤和 `Array.isArray` 兼容）、批量保存缓慢（串行改并发 `Promise.allSettled`，并发数 5，加进度显示）。详见 [BUGFIX_快速填写移动端修复.md](cc/BUGFIX_快速填写移动端修复.md)
- 旧版数据库迁移：从 livecalc_bak.db 迁移用户、商家、商品和价格记录（475 条）到新数据库，含兼容 SQLite/MySQL/PostgreSQL 的 SQL 脚本。详见 [DATA_MIGRATION_BAK_DB.md](cc/DATA_MIGRATION_BAK_DB.md)
- 成本数据延迟加载优化：菜谱管理列表和详情页的成本/营养/趋势数据改为异步延迟加载，先渲染基础数据再后台补计算数据。列表取消 `include_cost` 参数，详情页拆分加载流程，趋势图覆盖层不销毁图表 DOM。详见 [LAZY_LOADING_COST_DATA.md](cc/LAZY_LOADING_COST_DATA.md)
- 原料删除后无法同名重建修复：去掉 ingredients.name 的数据库唯一约束（改普通索引），创建/更新接口重名检查增加 is_active 过滤，与 Product 模型对齐；附带将 alembic.ini 注释 ASCII 化以修复 Python 3.14 下的 GBK 编码加载问题。详见 [BUGFIX_原料同名重建.md](cc/BUGFIX_原料同名重建.md)
- 启动导入仅在数据库初始化时进行：菜谱/原料/营养等初始数据的导入改为仅首次初始化时执行，lifespan 外层统一判断「已有 `source=json_repo` 菜谱则跳过」，修复本地路径 `import_from_local_dir` 每次重启重复遍历数据文件、重复执行营养增量导入的问题（判据与远程路径短路逻辑一致）。详见 [BUGFIX_启动导入仅初始化时进行.md](cc/BUGFIX_启动导入仅初始化时进行.md)
- USDA 下载 SSL 握手失败修复：四组对照定位根因——USDA 的 WAF 按 TLS 指纹 + UA 概率性切断非浏览器客户端，原 downloader 的 `USDA_UA` 是半截（缺 `Chrome/... Safari/... Edg/...`）被当 bot 切断率飙升（同库同窗口：完整 UA 4/6、半截 1/6），叠加网络窗口剧烈波动（好窗口单次近 100%、差窗口近 0%）。核心修复：UA 改完整 Edge 浏览器串（对齐参考项目 HowToCook_json_organizer）；纯 Python 兜底：httpx 并发重试（每轮 5 握手任一成功即返、全失败指数退避等窗口恢复）+ 可选 `usda_http_proxy`。不依赖 curl/curl_cffi（跨系统不可靠/Py3.14 库错误）。实测 fetch 3/3、foundation 363 条、sr_legacy 7793 条均成功。详见 [BUGFIX_USDA_DOWNLOAD_SSL_RETRY.md](cc/BUGFIX_USDA_DOWNLOAD_SSL_RETRY.md)
- USDA 匹配后营养不显示修复：原料匹配 USDA 后详情页营养仍为空，根因是 matcher 写入的 NutritionData 为 source=usda_manual_match 但未置 is_verified，而 NutritionCalculator 仅取 source='custom' 或 is_verified=True 的记录故查不到（商品走 custom_nutrition_data 不受影响）。修复：match_ingredient 写入时置 is_verified=True（source 保留追溯）；附带将 UsdaMatchDialog 的浏览器原生 confirm() 改为 Vuetify 二级模态确认对话框。续修：NRV 未计算——matcher 写入缺 nrp_pct/standard，提取模块级 calc_nrv_pct 共享函数补算中国 GB 标准 NRV%（商品复用、DRY）；USDA 搜索改 AND（空格分词每词都须命中，每词中/英任一子串即可）。详见 [BUGFIX_USDA匹配后营养不显示.md](cc/BUGFIX_USDA匹配后营养不显示.md)
- USDA 下载/上传任务纳入任务列表：数据维护页「任务列表」原本只合并 import_tasks + agent_sessions，而下载/上传走 usda_tasks 表未接入，点击后列表无记录。将 usda_tasks 作为第三来源（`_kind: 'usda'`）合并进 mergedTasks，复刻 agent「拿 id→入列→轮询」机制；后端 `/usda/download`、`/usda/upload` 端点预建 UsdaTask 返回 task_id、新增 `/usda/tasks` 列表与 `/usda/tasks/{id}` 单条端点；前端下载/上传后入列轮询、onMounted 恢复历史、模板三分支展示，usda 任务不显示取消按钮（后台无法取消）。详见 [BUGFIX_USDA下载任务列表.md](cc/BUGFIX_USDA下载任务列表.md)
- 营养素翻译被 100 行护栏误伤修复：USDA 维护页「未映射营养素名翻译」（openai）卡 running、name_zh 未写入，根因是 `run_agent_loop` 默认 `safe_row_threshold=100` 把单名批量 UPDATE（实测 SFA 16:0 未译 6398 行、全表单名最大 15600）误判 dangerous → unattended 跳过，Agent 被迫 LIMIT 分批甚至卡死。修复：`nutrient_task.py` 新增 `_SAFE_ROW_THRESHOLD=50000` 传给 `run_agent_loop`，放行单名批量同时兜底全表误更新（65 万行仍触发）；另三条老路径（`translate/task.py` 食材翻译、`inferrer` 模糊量/密度）一并覆盖 `_SAFE_ROW_THRESHOLD=5000`（按各表行数取值，同根因预防性修复）；续修：该任务 prompt 模板漏写「安全 SQL 自动执行」说明致 Agent 误报「请审批后执行」（实测 48 条 UPDATE 已全 auto_executed），已补全模板并约束 Agent 措辞。详见 [BUGFIX_营养素翻译行数护栏.md](cc/BUGFIX_营养素翻译行数护栏.md)
- 成本趋势并行加载优化：菜谱详情成本趋势切大跨度区间（季/年/全部）时，分批加载由串行 `while await`（每批 90 天一批接一批）改为并行。拆出无副作用 `fetchCostHistoryBatch` + 并行合并核心 `loadCostHistoryParallel`（各段 offset 不重叠 → `Promise.all` 并发，后端纯读无竞态）；固定区间一次性并行到底，「全部」分波次并行（每波 4 批 ≈360 天，末批空即终止）；任务间仍走 `costHistoryQueue` 串行队列避免区间切换竞态。「年」约 4×、「全部」约 4× 提速。详见 [PERF_成本趋势并行加载.md](cc/PERF_成本趋势并行加载.md)

### 功能实现记录
- 本地模式快速填写排序：本地 `getMerchantProductPrices`（[merchants.ts](frontend/src/api/local/handlers/merchants.ts)）此前只返回原始 product_records，未 JOIN products→ingredients→ingredient_categories，前端 QuickFillView 缺 product_name/category_sort_order 等字段，商品全塌进同一 sort_order=999999 桶、显示顺序退化为记录插入顺序。修复：handler 补齐 product_name/category_id/category_display_name/category_sort_order，并算 custom_sort_score（近3天加权 今3/昨2/前1，与云端 [get_merchant_product_prices](backend/app/api/merchants.py) 对齐）。配套 [database.ts](frontend/src/api/local/database.ts) 新增 user_merchant_product_orders 表（DB v2→v3 自动升级）+ [proxy.ts](frontend/src/api/local/proxy.ts) 注册 POST /merchants/:id/product-orders（前端保存时已调，此前本地 404 静默）。续修：新表在已有 v2 库需升级才存在，HMR 下 dbInstance 持有旧 v2 连接时 getAll 抛 NotFoundError 拖垮整个列表（「加载历史商品失败」），加 safeGetAll 降级（表不存在返回空、custom_sort_score 暂为 undefined 回退默认分类+拼音）；用户硬刷新后升级触发，排序学习生效。纯前端 IndexedDB，无表结构/后端变更，build 通过。
- 粘贴导入复制模板：快速填写「粘贴导入价格」对话框加「复制模板」按钮（[PasteImportDialog](frontend/src/components/prices/PasteImportDialog.vue)），点击弹出二级 v-dialog 展示该商家**全部历史商品**清单（每行「商品名 」+ 尾随空格，按页面顺序不分类别）+ 复制按钮。范围用户拍板含近 1h 已填隐藏的（模板=完整清单语义，隐藏只是 UI 反重复）。三处改动：①新建通用 [utils/clipboard.ts](frontend/src/utils/clipboard.ts) `copyText`（`navigator.clipboard` 优先 + `execCommand('copy')` 降级，兼容 http 明文部署 / NAS 直连，填补 [InviteCodesView:481](frontend/src/views/admin/InviteCodesView.vue#L481) 裸调无降级的缺口但未动它 YAGNI）②[QuickFillView](frontend/src/views/prices/QuickFillView.vue) 加 `historyProductNames` computed（基于全量 `historyRows` 非过滤的 `visibleHistoryRows`、复用已排序顺序 DRY）传 prop ③PasteImportDialog 加按钮 + 二级对话框（多根 fragment，Vuetify 各自 teleport、z-index 自管理）+ `templateText` computed / `copyTemplate`（成功「已复制」2s）+ 等宽预览样式。模板格式 `names.map(n => n + ' ').join('\n')`。纯前端 3 文件，后端零动，无表结构变更，build 22.50s 通过（precache 131 entries）。详见 [FEATURE_粘贴导入复制模板.md](cc/FEATURE_粘贴导入复制模板.md)
- 外链图打包导出：云模式全量导出对 OSS CDN 外链图（http(s)://）原一律跳过（[_collect_image_files._handle:72](backend/app/services/export/packaging.py#L72) 跳 http）致 ZIP 无图。改 [packaging.py](backend/app/services/export/packaging.py) 三处：① 新增 `_download_remote_images(image_urls, is_admin)`（[packaging.py:63](backend/app/services/export/packaging.py#L63)，ThreadPoolExecutor 并发8/超时30s，admin 去 `?imageslim` query 下原图、普通用户原样瘦身图，basename 用 `_UNSAFE_FILENAME` 清洗 + `name_2.ext` 去重，失败计 failed 返回 (downloaded, failed, tmpdir)）；② 改造 `_collect_image_files`（[packaging.py:117](backend/app/services/export/packaging.py#L117)，签名加 user 返回 (files, tmpdir)，收集去重外链→下载→成功的把 JSON 里 URL 改写为 `images/<basename>` 相对路径并打包（recipe.images 用 idx 改 list、product.image_url 直赋）、失败保留外链计 failed、本地图照旧，manifest image_summary 含 downloaded/failed/skipped_local_missing/skipped_remote(兼容)）；③ `build_export_zip`（[packaging.py:530](backend/app/services/export/packaging.py#L530)）`_collect_image_files` 传 user 解包 (image_files, img_tmpdir)、zip 写入 try/finally 包裹、finally `shutil.rmtree` 清理临时目录，19 条 writestr 逐行保留。④ Task 4 扩展（用户反馈「图都在 S3 导出没图」查出）：DB `Recipe.images` 实存 **storage key**（`recipes/xxx` 非 http 外链、图物理在 S3），Task 1-3 的 http 外链下载对它不触发（STATIC_DIR 查不到→skip）；补 `_collect` 第三分支——本地图缺失时 `storage.get(key)`（[s3.py:98](backend/app/services/storage/s3.py#L98) `get_object`，不经 CDN）直读 S3 bytes 打包到 `images/<key>`、JSON 改写（统一归 images/，避免与 `recipes/*.json` 混杂）、天然原图（无 UA 拦截/无 query 问题，admin/普通一致），`_collect` 顶部 `get_storage()` 预加载、缺失计 `s3_missing`；manifest `image_summary` 增 `s3_downloaded/s3_missing`。全量端到端实测当前 sqlite 库 **340 张 S3 图打包**（s3_downloaded=340，images/ 339 条去重）、`recipes/` 下 0 图片纯 json、zip 99.9MB（含图）、2 张 S3 缺失、46s。full + mine 都打包。8 单测（mock requests）+ 既有回归 16 passed、py_compile 过。**当前 sqlite 库无外链**（Recipe.images 相对路径 + STATIC_DIR 文件缺失，skipped_local_missing=342），要在「有外链图的库」（22:00 那个 PG/MySQL，images 存 `https://static.a4ding.com/...`）导出才触发真实外链下载打包；代码已就绪、单测 mock 覆盖下载/改写/失败逻辑。local 导入端 [exportImport.ts:396](frontend/src/api/local/handlers/exportImport.ts#L396) 现成兼容（相对路径图解包）前端零改动、云库 DB 不动。spec [2026-07-27-export-pack-remote-images-design.md](docs/superpowers/specs/2026-07-27-export-pack-remote-images-design.md) + plan [2026-07-27-export-pack-remote-images.md](docs/superpowers/plans/2026-07-27-export-pack-remote-images.md)。subagent-driven 3 task TDD 落地、逐 task controller 读码审查。详见 [FEATURE_外链图打包导出.md](cc/FEATURE_外链图打包导出.md)
- 头像裁剪与存储配置增强：OSS PutObject 兼容修复（payload_signing_enabled + checksum 控制）+ PicGo 三字段（base_path/custom_domain/url_suffix，DB 存逻辑 key）+ 国家级中文化（COUNTRY_NAMES_ZH + re-seed）+ 头像裁剪（vue-advanced-cropper 512×512）+ 向导按钮去重/url_style 提示。详见 [FEATURE_头像裁剪与存储配置增强.md](cc/FEATURE_头像裁剪与存储配置增强.md)
- 存储配置后台化与个人信息编辑重组：两个独立子任务——①个人信息编辑重组+地区（头像/地区收进编辑对话框、地区 seed 移出菜谱初始化解耦、数据维护中心更新模块）②图片存储配置后台化（StorageConfiguration 表 + BOOTSTRAP_ 前缀 + DB→.env→默认 三层优先级 + 四管理 API + 双向异步迁移 + 向导五步切换页）。详见 [FEATURE_存储配置后台化与个人信息编辑重组.md](cc/FEATURE_存储配置后台化与个人信息编辑重组.md)
- 禁用地图总开关：管理员「地图配置」页加 `map_enabled` 总开关（**新增字段**非复用 available_maps 空列表，语义正交、临时关闭不丢配置）。禁用后——前端所有地图隐藏；商家经纬度不必填（禁用时 payload 不带坐标，靠后端 `exclude_unset` 保留原值：有坐标修改不受影响/新建 null/无坐标开启后修改不给默认值）；商家列表改卡片网格(桌面 v-row+v-col)/列表(移动 v-list)参考 [PricesView](frontend/src/views/prices/PricesView.vue)、定位按钮隐藏；商家详情地图双条件(`mapEnabled && 有坐标`，关了或无坐标都不显示)；常用地点入口隐藏+路由守卫+后端写接口403(读保留数据不删)+页面兜底防守卫竞态。抽 [useMapConfig](frontend/src/composables/useMapConfig.ts) 单例 composable 统一下发（失败回退 true、onMounted 读一次不实时推送 YAGNI）；[MapSettingsView](frontend/src/views/admin/MapSettingsView.vue) 配置区 CSS class 灰显（**不用 v-card disabled**——Vuetify3 透传不可靠）；后端 [places.py](backend/app/api/places.py) 抽 `_ensure_map_enabled` helper 四写端点拦截/GET 不拦。迁移 alembic+三引擎SQL（应用开发库由开发者手动）。subagent-driven 13 任务两阶段审查执行；TDD 10 新测试+4 回归 passed、build 通过、final 整体审查无 issue。详见 [FEATURE_禁用地图总开关.md](cc/FEATURE_禁用地图总开关.md)
- 黑名单分组快速导入（跟随菜谱初始化 + 管理页按钮）：两个相关问题——①[main.py](backend/app/main.py) lifespan 的 `ensure_allergen_groups` 原在 `FIRST_RUN_INIT_RECIPES` 判断**块外**，false 时也跑、原料没入库就建 13 **空壳分组**（当前开发库正是此状态）；②seed 函数无对应管理 API，空壳/未导入时管理员无处补。用户拍板「未导入=13 标准分组任一缺失或映射空」「UI=顶部 v-alert+按钮」。4 处改动：(1) [allergen_seed.py](backend/app/services/allergen_seed.py) 加 `upsert_allergen_groups`（按 name 查含软删避免撞 unique，已存在软删则复活不改 display_order；原料映射复用本会话 [export.py `_import_blacklist_groups`](backend/app/services/importer/importers/export.py) 范式 `(group,ingredient)` 查含软删→复活 or 新建）+ `need_allergen_seed`（基于 is_active=True 分组判断，软删算缺失）；(2) [main.py](backend/app/main.py) 把 ensure 调用移进 `first_run_init_recipes` else 块末尾（菜谱导入后），false 时不碰；(3) [blacklist_groups.py](backend/app/api/blacklist_groups.py) 加 `GET /admin/blacklist-groups/allergens-status` + `POST /admin/blacklist-groups/seed-allergens`（均 get_current_admin_user）；(4) [BlacklistGroupsView.vue](frontend/src/views/admin/BlacklistGroupsView.vue) 顶部 v-alert（needed 时显，文案点缺失/空映射数）+ 按钮，onMounted 并行 loadGroups+loadAllergenStatus。**关键设计**：lifespan 用 ensure（保守不改管理员数据）、按钮用 upsert（主动补全），职责分离；闭环——false 启动→不建→按钮导入 / 空壳库→按钮补映射 / 全映射→按钮不显示。验证 py_compile 三文件 + 临时库测 upsert（建13组/二次幂等0-0-0/软删复活 reactivated=1）+ need（空表/空壳/全映射三态）全断言过 + build 31.40s。无表结构变更。教训：lifespan seed 位置要看清是否在条件块内（块外无视开关永远执行）；seed 类初始化配 upsert 端点+显隐判断是闭环标配。详见 [FEATURE_黑名单分组快速导入.md](cc/FEATURE_黑名单分组快速导入.md)
- 价格记录跳转详情：[PricesView.vue](frontend/src/views/prices/PricesView.vue) 列表项整行/整卡原不可点，用户想看记录对应的商品/商家详情只能手动翻「数据管理」。加 `goToProduct`/`goToMerchant` 两方法（各带 id guard），整行 `@click` 跳商品明细 `/data/products/:id`、行内商家 `@click.stop` 跳商家详情 `/data/merchants/:id`；三层 `.stop` 隔离（整行 / 商家 / 6 处按钮「再记·编辑·删除」× 移动+桌面）。移动端商家 `v-if`/`v-else` 条件渲染（有商家蓝字 `span.text-primary.cursor-pointer` / 无商家灰字「未知商家」）、桌面 chip 动态 `:color`/`:variant`（`merchant_id ? 'primary':'default'` / `'tonal':'outlined'`）+ `@click.stop` 区分可点性。对齐 [MerchantsView](frontend/src/views/data/MerchantsView.vue) 既有范式（整行 @click + router.push 字符串路径 + 按钮 .stop）；交互方式「整行/整卡可点 / 新增查看按钮 / 商品名可点」三案用户拍板选整行可点。状态保持走既有 `syncToUrl` URL 同步（跳详情返回自动恢复分页/搜索）。subagent-driven 双审：spec 合规 9 条全过（审查员标 Extra 的 CLAUDE.md + vite.config.ts 经 `git diff` 核实为会话前既有 PWA/脱敏改动误报）、code quality Critical 无（reviewer 建议桌面 chip 改 v-if 条件渲染对齐移动端，经评估保持现状——桌面统一 chip 形态 + guard 兜底，改 v-if 反而 chip/纯文本形态不统一；移动 cursor-pointer 已加触屏无害）。build 通过 34.06s（precache 128 entries）。单文件 +42 行，无表结构/后端/依赖变更，未 commit。详见 [FEATURE_价格记录跳转详情.md](cc/FEATURE_价格记录跳转详情.md)
- 演示库脱敏脚本 [desensitize_demo_db.py](backend/scripts/desensitize_demo_db.py)：把 `backend/data/livecalc.db` 脱敏成演示版（admin/test 两用户、密码统一 123456）。探查发现 id=1 admin 持全部 4901 条业务引用、其余 11 用户零引用→硬删无孤儿、id=2 审计残留 3 条清理。**关键坑**：前端 [crypto.ts](frontend/src/utils/crypto.ts) 登录前先 crypto-js `SHA256().toString()`（hex）再传后端，库里存的是 `bcrypt(SHA256_hex(明文))` 而非 `bcrypt(明文)`——首轮直接 `bcrypt("123456")` 脚本自检全绿，但端到端 `POST /auth/login` 返 422 要 `password_hash` 字段才暴露链路，加 `_sha256_hex()` 对齐 crypto-js 修正（自检/验证都改 SHA256 口径防假绿）。教训：改密码类脱敏必须端到端登录验证，`bcrypt.checkpw` 通过 ≠ 能登录（中间隔着前端预处理）。脚本幂等带时间戳备份 + 原子事务（`with con` 自动回滚）+ 验证（bcrypt verify(sha256) + 67 条真用户引用列残留复查 0 + VACUUM）。用户拍板：直接改 livecalc.db（非另存 demo）、test 保持空账号、id=2 一并清；token_version 归 0 致 admin 旧 session 失效需重新登录。端到端 admin/test 均 HTTP 200 + JWT bearer。本次纯 DML 数据脱敏非 DDL，无需 alembic/四引擎 SQL。注意 `.env` 当前是 sqlite 非 PG（记忆 [backend-uses-postgresql] 过时）。详见 [FEATURE_演示库脱敏.md](cc/FEATURE_演示库脱敏.md)
- PWA 快捷方式图标（manifest.shortcuts 96×96）：[vite.config.ts](frontend/vite.config.ts) 7 个 shortcuts（今日推荐/价格/菜谱/商品/原料/商家/个人中心）DevTools 提示缺 96×96 图标。新增 [scripts/generate_pwa_shortcuts.py](scripts/generate_pwa_shortcuts.py)（PEP 723 自声明 fontTools 依赖，`uv run` 直接跑、不污染 venv）——从 `@mdi/font` ttf 提侧边栏同款字形 path（[AppLayout.vue](frontend/src/components/layout/AppLayout.vue) prepend-icon 一一对应：silverware-fork-knife/currency-cny/book-open-variant/package-variant/leaf/store/account），fontTools `SVGPathPen` 提 path → 按各自 glyph bounds 几何居中（`unitsPerEm=512`，各字形 center 偏离 (256,256) 尤其 cy 偏下 170~190，直接 em box 映射会偏下，故算 tx/ty 落中心 + `scale(1,-1)` 翻 y；保持原大不缩放维持 MDI 视觉重量平衡）→ 绿底 #558B2F + 米白 #FDFCF8 SVG（配色对齐 [logo](frontend/public/logo.svg)）→ sharp `density:600` 转 96×96 PNG 落 [public/shortcuts/](frontend/public/shortcuts/)。vite.config.ts 每项加 `icons:[{src:"shortcuts/xxx.png",sizes:"96x96",type:"image/png"}]`。工具链全既有（@mdi/font/fontTools/sharp）零新依赖。踩坑：`node --input-type=module -e` 模式无脚本路径占 argv[1]，参数从 argv[1] 始，初版写 argv[2]/[3] 偏移一位致读空 outDir 静默退出 0（目录建了却空）。workbox globPatterns 含 png 自动 precache（114→128 entries），断网可用。build 通过（13.77s）。复跑改图标：`uv run scripts/generate_pwa_shortcuts.py`。详见 [FEATURE_PWA快捷方式图标.md](cc/FEATURE_PWA快捷方式图标.md)
- PWA 安装体验增强（screenshots/id/display_override/protocol_handlers）：Chrome devtools PWA 审计 4 条用户全选落实。[vite.config.ts](frontend/vite.config.ts) manifest 加 ①id '/' 显式应用身份（与 start_url 推导一致零破坏）②display_override ['window-controls-overlay','standalone'] 桌面安装版优先 WCO 回退 standalone ③screenshots 桌面 wide 2240×1400 + 移动 narrow 752×1631（用户手动截放 public/screenshots/，读 PNG IHDR offset16/20 回填真实尺寸非占位）④protocol_handlers [{protocol:'web+livecalc',url:'/?protocol-uri=%s'}]。WCO 全局 CSS 放 [App.vue](frontend/src/App.vue) style 不放 AppLayout（AppLayout 无顶部 bar 各 view 自带 v-app-bar，全局 header.v-app-bar 选择器一次覆盖，App.vue 原就在管该选择器固定定位聚合同类）：@media (display-mode: window-controls-overlay) 下 app-bar 设 -webkit-app-region: drag 可拖拽移动窗口 + .v-toolbar__content padding-right calc(100vw-env(titlebar-area-x)-env(titlebar-area-width)) 右侧避开系统控件区 + 交互元素(button/a/input/.v-btn/.v-field/[role=button]) no-drag 保持可点。protocol 解析 handleProtocolUri 挂 onMounted fetchUser 后（跳详情需登录态让守卫放行），正则 web+livecalc://(recipe|product|ingredient|merchant)/{id} 映射 /recipes、/data/products、/data/ingredients、/data/merchants 详情路由，router.replace 清 query。chrome-devtools 连不上 Edge（未开 9222 调试端口）改用户手动截图。无表结构变更，纯前端，build 通过（12.51s）+ PWA precache 112→114（screenshots 纳入，体积 4075→6921 KiB）。遗留：截图体积偏大（1.7MB/1.1MB）生产前可压（generator 自带 sharp 可复用）；protocol 演示性无真实链接源；WCO 仅桌面安装版生效。详见 [FEATURE_PWA安装体验增强.md](cc/FEATURE_PWA安装体验增强.md)
- PWA 图标跟随 logo 更新：用户换了新 [logo.svg](frontend/public/logo.svg)（绿底 #558b2f + 白色算盘计算器几何图案）后 6 个 PWA 图标仍显旧临时版（PWA 适配时基于旧 logo「生计二字转曲」生成、未跟随重跑）。排查发现 [package.json](frontend/package.json) 早装 `@vite-pwa/assets-generator` 1.0.2 + 有 `generate-pwa-assets` 脚本、[pwa-assets.config.ts](frontend/pwa-assets.config.ts) 一直在（首轮 Glob 用错名 `pwa-assets-generator.config.*` 没找到，正确约定名 `pwa-assets.config.ts`，README 示例确认），配置源已指向 public/logo.svg、maskable 已 padding 0——核心动作就是重跑生成器。顺手补配置遗漏：原配置只盖 maskable 漏 apple（apple 仍默认 padding 0.3 + 白底，iOS 主屏图标会缩小成小绿块漂白底），三类统一 padding 0 + maskable/apple 显式 background #558b2f 兜底。默认值依据 [index.mjs:13-17](frontend/node_modules/@vite-pwa/assets-generator/dist/index.mjs#L13) defaultPngOptions（maskable/apple 默认 padding 0.3 + white 即「露白翻车」根源）+ [index.mjs:38-45](frontend/node_modules/@vite-pwa/assets-generator/dist/index.mjs#L38) toResolvedAsset（`...asset` 后展开故 padding 可覆盖）。`npm run generate-pwa-assets` 重生成 6 文件（pwa-64/192/512 + maskable-512 + apple-touch-180 + favicon.ico），命名由 [index.mjs:46-55](frontend/node_modules/@vite-pwa/assets-generator/dist/index.mjs#L46) defaultAssetName 出，与 manifest/index.html 引用一字不差无需改引用。padding 0 生效决定性证据：maskable-icon-512 与 pwa-512 字节数完全相等（均 1313），若 padding 0.3+白底则像素内容不同字节必不同；logo 图案在中心 80% maskable 安全区内（边缘留白 ~12.5%>10%）padding 0 平台 mask 只裁绿边不伤白图。src/ 零硬编码图标引用（grep 确认），引用仅 index.html + vite.config.ts。无表结构变更，纯前端静态资源 + 一行配置补全，build 通过（19.97s）+ PWA precache 112 entries 一致。复跑：改 logo 后 `npm run generate-pwa-assets`。详见 [FEATURE_PWA图标跟随logo更新.md](cc/FEATURE_PWA图标跟随logo更新.md)
- 前端 PWA 最小集适配（可安装 + app shell 预缓存 + prompt 更新）：前端原零 PWA 配置（无 manifest/SW/logo，[public/](frontend/public/) 空），经 brainstorming 拍板最小集。**vite-plugin-pwa 1.3.0**（Workbox generateSW，registerType **prompt** 防丢表单 / devOptions.enabled / injectRegister auto）自动产 manifest+sw；workbox globPatterns 预缓存 app shell、navigateFallback index.html（断网开壳子）、/api/* 与地图瓦片 passthrough 不缓存（最小集边界）。思源黑体**转曲**生成路径化 logo.svg（[generate_pwa_logo.py](scripts/generate_pwa_logo.py) 用 fontTools `SVGPathPen` 把「生计」二字 dump 为 `<path>`，字体不入仓库 scripts/.fonts/ gitignore 用完即弃，规避微软雅黑版权）；**@vite-pwa/assets-generator 1.0.2**（antfu 维护，非 vite-pwa 官方 0.x；preset 用 `minimal2023Preset`）从 SVG 产全套图标（pwa-64/192/512+maskable-512+apple-touch-180+favicon，源放 public/ 因 generator 产物与源同目录、需根路径引用）；[PWAUpdatePrompt.vue](frontend/src/components/common/PWAUpdatePrompt.vue)（useRegisterSW+v-snackbar「发现新版本」）挂 [App.vue](frontend/src/App.vue)；[nginx 模板](deploy/nginx/default.conf.template) 加 `location = /sw.js`+`location = /manifest.webmanifest` no-cache（=精确匹配盖过 *.js 正则长缓存，防 SW 更新失效）。主题色 #558B2F/#FDFCF8 对齐 [vuetify.ts](frontend/src/plugins/vuetify.ts) lightTheme。执行纠偏：subagent 两次失败（模型不存在+中断）回退 inline；fontTools `SVGPathPen`（非 SvgPathPen，4.x API）；preset `minimal2023Preset`（非 plan 写的 minimal2021Preset）；logo 源 src/assets→public/；manifest icons 含 pwa-64；maskable padding 0（源已填满橄榄绿，加 padding 露白翻车）。**离线测试坑**：必须 build+preview 测（dev 模式 SW precache 不可靠致 ERR_INTERNET_DISCONNECTED，非 bug），首次访问后须 reload 一次（页面归 SW 控制）再勾 Offline。最终 build 34.30s（precache 112 条），待开发者 Lighthouse/安装/离线/prompt 验收。详见 [FEATURE_前端PWA适配.md](cc/FEATURE_前端PWA适配.md)
- 用户信息编辑：个人中心新增「用户信息编辑」（设置列表第一项，主题切换上方），自助改用户名/邮箱/手机/密码。后端新增 `PUT /auth/me/account`（[auth.py](backend/app/api/auth.py) `update_my_account`，字段全可选传了才改）+ schema `UserAccountUpdate`/`UserAccountResponse`（[schemas/auth.py](backend/app/schemas/auth.py)）；抽 [`_user_to_response`](backend/app/api/auth.py) helper 让 get_me/patch_me/新端点三处共用（DRY，消除三份 UserResponse 构造）；改密码需 `current_password`（verify_password 校验）→ 成功后 `token_version+=1` 并签发新 access+refresh token 返回（发起方无缝续登、其他设备旧 token 失效），不改密码则不发 token、不动 token_version；用户名/邮箱/手机查重排除自己（`User.id != current_user.id`）。前端 [ProfileView.vue](frontend/src/views/profile/ProfileView.vue) 用户信息编辑对话框（基本信息）+「修改密码」按钮打开独立修改密码对话框+ [user.ts](frontend/src/stores/user.ts) 加 `setTokens`（login/register 复用）+ `hashPassword` 复用。验证：[test_account_update.py](backend/tests/test_account_update.py) 7 绿（uuid 唯一名避残留）+ test_update_other_fields 回归证 helper 未坏 get_me + build 30.23s。无表结构变更。profile.md 同步新章节+账户安全改写。详见 [FEATURE_用户信息编辑.md](cc/FEATURE_用户信息编辑.md)
- 文档勘误修订（docs/ 正文，对照代码审查）：[deploy.md](docs/admin/deploy.md)「没有 Docker」改口+补 Docker 部署章（容器化早做完）；[config-init.md](docs/admin/config-init.md) 删伪环境变量 `REGISTRATION_REQUIRE_INVITE_CODE`（实为 system_config 表、后台「邀请码管理」开关控制）+ `JWT_ACCESS_TOKEN_EXPIRE_MINUTES` 15→10080（对齐 [config.py](backend/app/config.py) 默认 7 天）；[review.md](docs/admin/review.md) 覆盖清单按 [bootstrap.py](backend/app/services/proposals/bootstrap.py) 补全（原料含合并/商品含合并拆分/商品营养/USDA 原料+商品匹配/商家含合并/菜谱发布+编辑）+「菜谱图片删除」举例改；[recommend.md](docs/recommend.md) 黑名单空小节补正文；[admin-pages.md](docs/admin/admin-pages.md) 地图配置补启用开关+开头补 Dashboard；[ingredients.md](docs/ingredients.md) 第 80 行图片笔误补 `!`；幽灵文件坏链 `users-units-map.md`（不存在）3 处全修（profile/merchants-map/config-init）；[docs/README.md](docs/README.md)+[admin/README.md](docs/admin/README.md) 索引补漏列的 review/data-maintenance/agent 三篇 + admin/README 菜谱特权「创建即发布」改为「默认私有」。K 线正文 docs 无（前端 5 文件残骸未接路由，清不清另议）。详见 [DOC_文档勘误修订.md](cc/DOC_文档勘误修订.md)
- 过敏原seed接入初始化：过敏原黑名单分组（GB 7718-2025 的 13 组）此前往手动脚本 + SQL 形式存在、**从未接初始化流程**（PG 实测两表 0 行）。根因四层：[init_default_data](backend/app/main.py#L46) 只塞单位/分类、lifespan 不调 seed、[alembic 0001](backend/alembic/versions/20260626_0001_add_blacklist_and_allergen_groups.py) 只建表无 INSERT 且项目走 create_all、[seed_allergen_groups.py](backend/scripts/seed_allergen_groups.py) 是独立 `__main__` 脚本无人 import。方案：抽 [app/services/allergen_seed.py](backend/app/services/allergen_seed.py) 作唯一数据源（`ALLERGEN_GROUPS` + `_create_groups(db)` 纯创建 + `ensure_allergen_groups(db)` 幂等包装「count>0 跳过」），[main.py](backend/app/main.py) lifespan 批量建商品之后（:286→USDA 索引前）接入 ensure（空表才建、不耦合 `first_run_init_recipes` 默认 False），[scripts/seed_allergen_groups.py](backend/scripts/seed_allergen_groups.py) 改壳复用、保留命令行清空重建。顺手修 `group_data.pop("ingredient_names")` 破坏模块级常量 → `.get` 非破坏性（抽离复用必要前置，否则第二次起匹配落空）。无表结构变更（表早建好，无需 alembic/SQL）。验证：4 单测 passed（空库创建/已有跳过/双调幂等/缺失容错）+ compileall + 后端 reload 后 PG 实测 13 组 166 映射（166/168，2 个原料名库内无对应、计入 not_found）+ 幂等（独立进程再调 ensure 输出「已存在跳过」）。spec [2026-07-11-过敏原seed接入初始化-design.md](docs/superpowers/specs/2026-07-11-过敏原seed接入初始化-design.md) + plan [2026-07-11-过敏原seed接入初始化.md](docs/superpowers/plans/2026-07-11-过敏原seed接入初始化.md)。详见 [FEATURE_过敏原seed接入初始化.md](cc/FEATURE_过敏原seed接入初始化.md)
- 菜谱发布默认私有：自创菜谱默认私有（含管理员），点详情页「发布」按钮才公开（走审核），发布对作者不可逆（管理员审核台留 revert 救场）。根因 `source` 默认 `'custom'` 非空、被全系统当「公共」（列表 [recipes.py:181](backend/app/api/recipes.py#L181)、前端 [RecipeDetail.vue:559](frontend/src/views/recipes/RecipeDetail.vue#L559) `is_public||source`）致创建即发布；方案 A 改为「公共/已发布」只看 `is_public`、`source` 回归来源标识。改动：[create_recipe](backend/app/api/recipes.py#L111) 去管理员即发布；5 导入创建点补 `is_public=True`（recipe_import/howtocook/export/enhanced/json_recipe）；9 处可见性判断 `source`→`is_public`（recipes.py 列表/详情/update/upload/merchant-costs/3 可见性校验 + nutrition.py 批量）+ 变量名 `is_public_or_source`→`is_public_recipe`；导入菜谱数据迁移补 `is_public=true`（[alembic 20260709_0001](backend/alembic/versions/20260709_0001_recipe_import_default_public.py) + 三引擎 SQL，存量 custom 不动）；前端 `isPublished` 去 source、状态 chip（已发布不显示、未发布/审核中才显示、三态互斥）、发布加二次确认 v-dialog、RecipesView 未发布 chip 去 source。顺手修 [get_recipe_detail](backend/app/api/recipes.py#L451) `except Exception` 吞 `HTTPException(404)` 包 500 → 加 `except HTTPException: raise` 透传（既有 bug，被可见性测试撞破）。验证：5 新测试 passed（创建普通/管理员私有、导入 is_public、私有对他人不可见、公共可见）+ test_recipe_edit_proposals 全过 + build 32.37s + compileall；5 预存失败无关（auth/USDA/merchant/default_unit_id）。无表结构变更（纯数据 UPDATE）。遗留：迁移待开发者跑（开发库 PG，跑 [postgresql 脚本](backend/scripts/sql/20260709_recipe_import_default_public_postgresql.sql)，不跑则现有导入菜谱变私有）；subagent 本环境模型坏回退 inline。详见 [FEATURE_菜谱发布默认私有.md](cc/FEATURE_菜谱发布默认私有.md)
- 容器化部署：一份根 [Dockerfile](Dockerfile) multi-stage 三 target（frontend/backend/all-in-one）+ [.dockerignore](.dockerignore) + 统一/分开两份 compose（[docker-compose.yml](docker-compose.yml)/[docker-compose.split.yml](docker-compose.split.yml)）+ [deploy/](deploy/) 下 nginx 模板/supervisord/两个 entrypoint。统一部署单容器 nginx+uvicorn（supervisord 编排），分开部署独立镜像便于负载均衡。默认 SQLite（DATABASE_URL 走 .env，换库改一行，requirements 已含 PG/MySQL 驱动）。前端运行时注入 BACKEND_URL（VITE_API_URL=/api/v1 相对路径 + entrypoint envsubst 限定替换避开 $host 误伤），构建一次到处部署。.env 用 env_file 注入（非 volume——避「宿主文件不存在→被当目录挂载→pydantic 炸」坑），data/static/logs 才 bind volume。开箱即用：Settings 字段默认值 + Dockerfile mkdir + entrypoint 默认 BACKEND_URL，docker run -p 80:80 即验。stage 顺序 all-in-one 排 frontend 前（经典 builder 构 all-in-one 不需 nginx:alpine）；entrypoint sed 去 CRLF；supervisord 开 unix socket 供 supervisorctl。验证：三 target build 通过（all-in-one 372MB/backend 314MB/frontend 69.6MB）+ 点火测试（前端 / + /api 反代 + supervisorctl nginx/uvicorn RUNNING 全绿）。遗留：首启 FIRST_RUN_INIT_RECIPES 默认联网拉菜谱；生产必改 JWT 密钥；多副本需切 PG；**既有后端 bug register_all 'Session' not defined 与容器化无关需另查**；Docker Hub 国内不稳需配代理/mirror + 本地有镜像后 DOCKER_BUILDKIT=0 --pull=false 避联网核对。设计稿 [docs/plans/2026-07-08-容器化部署-design.md](docs/plans/2026-07-08-容器化部署-design.md)。详见 [FEATURE_容器化部署.md](cc/FEATURE_容器化部署.md)
- 自定义前后端端口：原端口硬编码（前端 [vite.config.ts](frontend/vite.config.ts) `port:5173` + proxy `target:http://localhost:8000`；后端靠 uvicorn `--port` 无配置项）。改环境变量驱动：前端 vite.config.ts port 读 `VITE_DEV_PORT`（默认 5173）/proxy target 读 `VITE_DEV_BACKEND_URL`（默认 `http://localhost:8000`）+ 前端 .env/.env.example 各加两变量；后端 [config.py](backend/app/config.py) Settings 加 `app_host`(默认 0.0.0.0)/`app_port`(默认 8000) + [main.py](backend/app/main.py) 末尾加 `if __name__=="__main__"` 块（`uv run python -m app.main` 读 settings 调 uvicorn.run reload=True）+ 后端 .env/.env.example 加 APP_HOST/APP_PORT。改端口：后端 APP_PORT 与前端 VITE_DEV_BACKEND_URL 必须同步（前后端联通命脉），env 仅启动时读取须重启生效。原 `uvicorn app.main:app --reload` 仍可用但端口要手动 --port。后端 CORS 全开（allow_origins=["*"]）、前端 src/ 零硬编码（全走 VITE_API_URL=/api/v1 经 vite proxy），故改端口无需动白名单/业务码；生产前后端分离部署需把 VITE_API_URL 改后端完整地址。无表结构变更。验证：前端 build 通过 + 后端 py_compile。详见 [FEATURE_自定义前后端端口.md](cc/FEATURE_自定义前后端端口.md)
- 商品加权价格机制：原料多商品价格悬殊（有机/普通）影响原料价计算与展示。引入商品级两层加权平均（商品内均价→商品间按 0-100 权重加权，默认50），取代原 latest-price 记录级简单平均（修正记录数偏置）与菜谱成本「取第一个商品」。`products.price_weight`（全局，仅管理员改）+ `user_product_weight_overrides`（用户覆盖）+ 统一服务 [ingredient_price_service.py](backend/app/services/ingredient_price_service.py)（纯聚合 `_aggregate_weighted` + `_resolve_weight` 优先级 + 编排 `get_weighted_ingredient_price` as_of/recent 双模式 + 成本 helper `resolve_direct_weighted_for_cost`）。覆盖 latest-price（target 口径，逐条折算+加权）/菜谱成本 cost·as_of·range_as_of·trend（standard 口径）/sparklines 原料加权。全局权重仅管理员（[update_product](backend/app/api/products_entity.py#L369) 剔除普通用户 price_weight）、用户覆盖所有人（`/products/{id}/my-weight`）。前端 ProductDetail 基本信息与 IngredientDetail 商品编辑两处配置 + 原料详情加权明细折叠。15 单测全过（含记录数偏置修正、用户覆盖、管理员全局、不同用户不同价）+ 前端 build 通过。遗留：range_trend 批量趋势函数（单商品 source 缓存+二分逐天，加权需重写批量算法）未改，菜谱 sparkline/成本区间趋势暂是单商品口径；cost breakdown 的 weighted_participants 字段未加（成本 tooltip 暂不展示加权来源）；default_product_id 死字段保留不动。subagent 本环境不可用（模型配置坏）回退 inline。详见 [FEATURE_商品加权价格机制.md](cc/FEATURE_商品加权价格机制.md)
- 用户级默认单位配置：User 加 4 列单位偏好（default_energy_unit/default_mass_unit_id/default_volume_unit_id/default_price_unit_id）+ 删 Ingredient.default_unit_id（含 grep 补全 recipes/products_entity/merchants/export/product_split 等下游引用，运行时折算统一走既有「回退斤」）。/auth/me 返回 unit_preferences（后端 join units 解析单位名）+ PATCH 类型校验（mass/volume/price 单位类型匹配、energy ∈ {kcal,kJ}、支持 null 清除）；前端 [useUserUnits](frontend/src/composables/useUserUnits.ts) composable（含 kcal↔kJ ×4.184 转换）+ [nutrientDefinitions](frontend/src/composables/nutrientDefinitions.ts) DRY 抽离（buildNutrientDefinitions 参数化能量行，消除两份重复 30 项定义）+ 三详情页 coreNutritionItems 跟随 energyUnit；记价默认 8 文件（含 pastePriceParser 参数化 defaultUnit）+ 价格趋势图表（massUnitName）+ 菜谱原料点击转换（[RecipeIngredientCard](frontend/src/components/recipes/RecipeIngredientCard.vue) 复用 POST /units/convert，from/to 缩写，跨类走原料密度）+ daily_calorie_target 跟随能量单位（库存仍 kcal）。附带修 PATCH /me detached user 写入失效潜伏 bug（[security.py:119](backend/app/core/security.py#L119) resolve_user_from_token 用独立 SessionLocal + finally close 返回 detached，原 setattr+commit 不写库、refresh 抛 InvalidRequestError——致 nutrition_goals/budget 修改一直未持久化，改为本请求 db 重新加载 user）。含 alembic 20260705_0001 + 三引擎 SQL + 开发库直接补（不走 alembic 惯例）。Phase F 原派 4 subagent 全因「模型不存在」API 错误失败，改手工。详见 [FEATURE_用户级默认单位配置.md](cc/FEATURE_用户级默认单位配置.md)
- USDA 匹配接入审核框架：放开 USDA 原料/商品匹配两端点（[usda.py](backend/app/api/usda.py) match_ingredient/match_product，原锁管理员 403），接入 change_proposals 框架（治理总表「有数据→人工」：有数据 manual / 没数据补空 auto_approve + 管理员 apply_as_admin 直写即时）。5 task：matcher 去 commit（match_ingredient/match_product commit→flush，调用方 commit）；service.submit 加可选 `policy_override` 参数（补空 auto 的正确实现——不用 nutrition 那套 apply 内 set_policy 权宜，那个其实没生效因 submit 在 validate 前读 policy）；两执行器 UsdaIngredientMatchExecutor（替换语义：snapshot 旧 NutritionData 全行→调 matcher 清空+写新→revert 删新+插旧）+ UsdaProductMatchExecutor（覆盖 custom_nutrition_data）+ bootstrap 注册全 manual；usda.py 两端点分流（判断有无数据→manual/auto policy_override）；前端 UsdaMatchDialog 按角色/status 提示（管理员成功/普通用户 pending 蓝色不误报/补空 success，pending 不刷新）+ entityTypeLabel 补 USDA 原料/商品匹配 + 清 dead import。关键技术：替换语义 snapshot/revert（区别 CRUD update）+ `_restore_row`（全行重插时 DateTime isoformat 转回 datetime——任何全列 snapshot 重插的执行器都踩 SQLite DateTime 拒收 string 的坑）。80 passed、build 通过。遗留：nutrition 端点补空 auto 仍未真正生效（apply 内 set_policy 权宜），可后续迁移 policy_override。续：商品营养端点 edit_product_nutrition 放开走审核（原锁管理员 403）——新建 ProductNutritionExecutor（写 custom_nutrition_data + source，同构 UsdaProductMatchExecutor）+ bootstrap 注册 + 端点分流（保留 structured_nutrients 构建，走提议 + policy_override 有数据 manual/补空 auto）+ ProductDetail saveNutritionEdit 按审核状态提示，与原料营养对齐，19 passed + build。续修 PUT 清空路径（update_product_nutrition 全删除时同款分流 + 前端按审核状态），21 passed。详见 [FEATURE_USDA匹配审核框架.md](cc/FEATURE_USDA匹配审核框架.md) 与 [FEATURE_原料商品改删审核框架.md](cc/FEATURE_原料商品改删审核框架.md) 增强 7
- 原料/商品改删+层级提示接入审核框架：放开普通用户对原料、商品的 update+delete（原 created_by 限制 403），接入 change_proposals 框架（全 manual + 管理员 apply_as_admin 直写即时 + delete 完整反级联）。5 task：新建 [ProductExecutor](backend/app/services/proposals/executors/product.py)（继承 CrudExecutorBase，delete 覆写：唯一商品检查 + 软删 Product + 级联软删 ProductRecord + snapshot + revert 反级联）+ [IngredientExecutor](backend/app/services/proposals/executors/ingredient.py) delete 覆写加级联（菜谱引用检查 + 软删 ingredient + 级联 Product + IngredientHierarchy parent/child + snapshot + revert 反级联）；bootstrap 注册 ProductExecutor（全 manual）；4 端点分流去 created_by（商品 [update/delete](backend/app/api/products_entity.py) + 原料 [update ingredient_extended](backend/app/api/ingredient_extended.py) / [delete nutrition.py](backend/app/api/nutrition.py) 前端实际调用）；前端 7 处按角色提示（[IngredientDetail](frontend/src/views/ingredients/IngredientDetail.vue) 基本信息/删除/层级 + [ProductDetail](frontend/src/views/products/ProductDetail.vue) 基本信息/删除，含层级关系前端提示 bug 修复）。update 只基本信息走提议（nutrition 走独立 NutritionExecutor，update_ingredient nutrition 段保留）；delete 双层业务检查（唯一商品/菜谱引用：端点提交时拒 + 执行器 apply 时再查）；snapshot 用 _json_safe + 在置 is_active=False 之前（对齐基类 _crud_base.py:81）；两个 soft_delete_ingredient（nutrition + ingredient_extended）都接入分流对齐消除漂移。90 passed（含 11 e2e 端到端 改/删→待审→审核生效→回滚反级联）、前端 build 通过。遗留：nutrition.py:482 旧 update_ingredient 影子路由（前端不用）。**后续修复（2026-06-30 用户反馈）**：① IngredientDetail 商品编辑/删除按角色提示遗漏（Task 5 只改 ProductDetail，漏了原料详情页的关联商品编辑入口 saveProduct/deleteProduct，普通用户误报「已更新/已删除」+ deleteProduct 提前移除列表；按 userStore.is_admin 分支修复）；② 审核台变更内容改 diff——CrudExecutorBase 加 build_snapshot + service.submit 预填 proposal.snapshot（getattr 兜底特殊执行器）+ ProposalResponse 返回 snapshot + 前端 ProposalsView before→after 字段对比（delete 显示原内容/update 旧→新高亮/create 标新增），解决审核 delete 提议看不到原内容（payload 空 + snapshot 仅 apply 时填）；③ 审核 diff 时 entity_type/entity_id 看不出具体实体（如 entity_unit_override 的 entity_id 是哪个原料），加执行器 entity_label（目标实体可读名称——CrudExecutorBase 默认查 name/title、entity_unit_override/density/hierarchy/nutrition/merge 等特殊执行器覆写）+ ProposalResponse.entity_label + proposals 序列化填充 + 审核台列表 payloadSummary 前置 + 详情顶部醒目显示；④ 商品/原料详情页密度数值不显示——根因 EntityDensityResponse.density 是 Decimal、Pydantic v2 序列化为 string（`{"density":"2254.273"}`）、前端 displayDensityValue 严格 `typeof !== 'number'` 永远返空，修复前端 loadDensity/displayDensityValue/openDensityDialog 用 Number() 兜底（两文件各 3 处）；⑤ 营养/商家编辑前端提示遗漏——普通用户编辑原料营养（manual 审核）/商家时前端固定「已保存」误报成功，修 IngredientDetail saveNutritionEdit（读后端 message：pending→info 待审不刷新）、MerchantsView saveItem（按 userStore.is_admin 区分）；排查确认层级/商品实体/合并/拆分/商家删除已正确、单位/USDA admin 页 admin-only；遗留商品营养端点 edit_product_nutrition 锁管理员（普通用户 403，需单独改造放开）。详见 [FEATURE_原料商品改删审核框架.md](cc/FEATURE_原料商品改删审核框架.md)
- 实体单位覆盖/密度接入审核框架：放开原料、商品的自定义单位（实体单位覆盖 `entity_unit_overrides`）与密度（`entity_densities`）的 5 个写端点给普通用户，接入 change_proposals 提议-审核框架（全 manual 审核 + 管理员 apply_as_admin 直写即时生效 + is_active 软删回滚）。6 task：两表加 is_active（含 server_default=sa.text("1") 防 task_templates/migrate_from_bak 原始 INSERT 路径在 create_all 库崩）+ 拆唯一约束（SQLite 匿名 UNIQUE 只能表重建，drop_constraint/drop_index 均失效）+ alembic/三引擎 SQL + 补开发库（258 覆盖/1 密度保留）；两执行器 EntityUnitOverrideExecutor/EntityDensityExecutor 继承 CrudExecutorBase 复用软删/复活、覆写 validate（两种 entity_type 区分：框架注册名 vs 业务 ingredient/product）；bootstrap 全 manual 注册；32 个下游读取点加 is_active 过滤（漏一处成本算错，plan 原清单漏 units.py 7 处管理读取点靠 grep 全量比对抓出补全，packaging 共用 _query 条件分支精准过滤不影响其他 13 模型）；units.py 5 端点分流（管理员 apply_as_admin/普通用户 submit）；两详情页前端按角色提示。Decimal 三处 JSON 序列化（payload model_dump(mode=json) / 占位补 confidence / snapshot）。⚠️ 顺带修 P0：CrudExecutorBase 的 update/delete snapshot 把 ORM Decimal 塞进 change_proposals.snapshot JSON 列致管理员直写 update/delete 带 Numeric 字段必 500（潜伏 P1 框架代码、既有执行器测试没碰 Numeric 字段 update/delete 故未暴露、Task 5 让管理员直写走执行器才激活），加 _json_safe（Decimal→str、datetime/date→isoformat）应用到两处 snapshot，revert 时 str 经 Numeric 列 setter 自动转回无损，修一处惠及所有 CRUD 执行器（ingredient/unit/hierarchy/merchant/override/density）。subagent-driven 6 task 两阶段 review，全程不 commit。77 passed（含 4 e2e 端到端 submit→pending→review→applied→revert，下游 pending 隔离/applied 生效）、前端 build 通过。详见 [FEATURE_实体单位密度审核框架.md](cc/FEATURE_实体单位密度审核框架.md)
- 多用户权限系统：完整的多用户权限体系（六桶数据分类 + 通用提议-审核框架 + 审核策略三档 + 管理员超级权限）。P0 堵漏（收紧共享写/越权读/统一鉴权，含路由遮蔽与 HTTPException 被吞两个 bug 修复）+ P1 框架（change_proposals 表 + 类型执行器 + submit/review/revert/apply_as_admin API + ProposalAutoReviewer 预留接口）+ P2 共享转型（商家共享池+收藏、价格去标识 latest-price 跨用户公开、菜谱 is_public+发布、5 业务执行器含合并 snapshot/revert+分流改造、前端适配）+ P3 增强（商家合并执行器、反垃圾批量回退）。管理员超级权限：所有写操作直写 apply_as_admin、菜谱创建即发布（单用户零负担）。29 commit 在 `feat/multi-user-permissions` 分支（未合并，待 review）。新增 51 个权限/框架/共享测试。详见 [MULTI_USER_PERMISSIONS.md](cc/MULTI_USER_PERMISSIONS.md)
- 用户重置密码与 token 失效：后台「用户管理」编辑表单去掉密码字段，改为「重置密码」按钮 + 独立对话框输两次新密码（仅编辑态，新增态密码框保留）；后端给 users 加 `token_version` 列，JWT 签发带 `ver`、`resolve_user_from_token` 与 `/auth/refresh` 校验比对版本，`update_user` 改密码时 `token_version += 1` 作废该用户所有 access/refresh token（存量用户默认 0 不受影响、老 token 无 ver 取 0 兼容、前端 401 自动 refresh 闭环零改动）；bump 仅限改密码，改其他字段不踢人。详见 [FEATURE_重置密码与token失效.md](cc/FEATURE_重置密码与token失效.md)
- 商品拆分为原料：在商品详情页添加「拆分为原料」按钮，将商品从当前原料中拆分为独立同名原料，迁移营养数据 mixin，同名冲突时支持重命名。详见 [FEATURE_PRODUCT_SPLIT_TO_INGREDIENT.md](cc/FEATURE_PRODUCT_SPLIT_TO_INGREDIENT.md)
- 详情页宽屏响应式布局：菜谱、原料、商品三个详情页实现宽屏（≥960px）双栏布局，行对齐区域用 CSS Grid，不对齐区域用 Flexbox。详见 [FEATURE_WIDESCREEN_DETAIL_LAYOUT.md](cc/FEATURE_WIDESCREEN_DETAIL_LAYOUT.md)
- 原料分类显示与编辑：原料详情页基本信息区显示分类，编辑表单支持选择分类（复用 GET /ingredients/categories）；后端 get_ingredient 详情接口补充返回 category_id/category。详见 [FEATURE_原料分类显示与编辑.md](cc/FEATURE_原料分类显示与编辑.md)
- 快速填写：价格记录页 app-bar 新增闪电入口，进入独立批量填写页面。选商家后自动列出历史商品（按拼音/字母顺序排序），逐行填价格（数量/单位默认 1/斤，点击可改），可新增行搜索商品。只保存填了价格的商品，近 1h 内填过的按商家独立隐藏（sessionStorage）。纯前端，后端复用现有 API。详见 [FEATURE_QUICK_FILL.md](cc/FEATURE_QUICK_FILL.md)
- 半成品菜谱成本传递：原料可指向其制作菜谱（激活 recipes.result_ingredient_id + 新增 ingredients.serving_weight），成本计算时商品无价则由制作菜谱推导每克单价（份×每份重桥接），支持递归套娃与循环检测。菜谱页「成品产出」与原料页「自制来源」双入口，成本明细 tooltip 显示 recipe_chain。附带修复 alembic 坏链与 as_of 的 Decimal×float 隐患。详见 [FEATURE_半成品菜谱成本传递.md](cc/FEATURE_半成品菜谱成本传递.md)
- 数据导出：个人中心「数据导出」，两档（全量 `full` / 仅我的 `mine`，mine 含外键可达性遍历保证引用完整）；菜谱/食材/营养/单位 HowToCook 兼容 + id 扩展，扩展知识库与账户交易数据独立规格，图片打包 zip 同步流式下载（`GET /api/v1/export/data?scope=`）；`services/export/` 分层（serializers/reachability/packaging）。详见 [FEATURE_数据导出.md](cc/FEATURE_数据导出.md)
- 快速填写粘贴导入：快速填写导航栏「粘贴导入」按钮，对话框粘贴文本（`名称 价格[/[数量]单位]` 四种格式）→ 解析预览 → 未匹配商品支持「关联已有 / 创建同名原料+商品 / 挂靠已有原料」三种手动处理 → 并发导入（`record_type=price` 不计入支出，并发数 5，进度+失败明细）；后端 `POST /products` 加可选 `ingredient_id` 支持「挂靠已有原料」创建商品。详见 [FEATURE_快速填写粘贴导入.md](cc/FEATURE_快速填写粘贴导入.md)
- USDA 营养素匹配：给原料/商品手动匹配 USDA 营养素。下载 USDA 原始数据（Foundation+SR Legacy）入库去重；6 后端翻译食材名（AI: Claude Code/OpenAI/Anthropic 兼容，机翻: 百度/阿里云/DeepL），营养素名走 172 条预设映射表（不耗 AI）；内存 OR 搜索（空格分词任意命中）；原料清空写 USDA、商品复制原料骨架设 0 再 USDA 覆盖；后台 AI/机翻/USDA 三管理页；前端匹配对话框接入原料/商品详情页营养编辑区。详见 [FEATURE_USDA营养素匹配.md](cc/FEATURE_USDA营养素匹配.md)
- Agent 维护任务台：管理员按按钮触发 claude code CLI 驱动 Agent 自主读写 SQLite 批量维护数据（方案 B：受控只读 MCP `db_read`/`describe`/`list_tables`，Agent 无写工具；写操作在 ` ```sql ` 代码块输出，经 `sql_guard` 判定 safe 自动执行 / dangerous 审批）。后端 `agent_sessions`/`agent_messages`/`agent_approvals` 三表 + `ClaudeCodeRunner`（subprocess stream-json + 线程桥接规避 Windows 事件循环 + `--strict-mcp-config`/`--resume`/`CLAUDECODE` pop）+ SSE 流式（query token 鉴权、subscribe 前置 seq 去重、终态兜底）+ 写操作多轮 loop（提取 SQL → 守卫 → 执行或审批 → `--resume` 回流）；前端任务台 UI（按钮 / 对话流 / 插话 / 审批卡 / 刷新重放，响应式）。阶段 1 实现「补单位质量」，实测 252 条脏数据修正 247 条（98%）。详见 [FEATURE_Agent维护任务台.md](cc/FEATURE_Agent维护任务台.md)
- langchain Agent 集成（OpenAI/Anthropic）：给 OpenAI 兼容/Anthropic 兼容两 provider 集成 langchain Agent，四项 AI 后处理（食材翻译/营养素翻译/模糊量推测/密度推测）从分批串行/逐条改成 Agent 自主批量（省时省 token）。新增 `LangChainRunner`（langchain 1.x `create_agent`，与 `ClaudeCodeRunner` 平级，复用 AgentRunner 协议 + run_agent_loop + sql_guard + 5 个 task_template）；只读 `@tool`（db_read/describe/list_tables，进程内 SQLAlchemy inspect 跨库，复用 db_query.check_read_only）；写库走 SQL 文本 + 守卫（不引入结构化写工具）；事件映射过滤消息类型（只 AI 消息产 text_delta）；resume 靠 AgentMessage 回放 chat_history；`run_agent_loop(unattended=True)` 老路径无人值守（dangerous 自动跳过）；任务台选 provider（runner_factory 分流）+ 老路径（TranslateTask/TranslateNutrientsTask/AIInferrer）引擎替换（建 `[后台]` AgentSession + 保留 UsdaTask/AIInferrer 载体 + ai_caller 回退 + async/同步桥接）。详见 [FEATURE_langchain_Agent集成.md](cc/FEATURE_langchain_Agent集成.md)
- 后台管理 UI 调整：合并 AI 配置与机翻配置为单页「AI 与机翻配置」（`/admin/ai-config`，`v-expansion-panels` 双面板默认全展 + 统一保存；抽 `components/admin/ProviderCard.vue` 子组件按 `fields` 配置数组渲染 6 个 provider，消除原两页重复的 `setField`/`testProvider`/`save` 逻辑；`/admin/mt-config` 重定向到 `/admin/ai-config`）；移除 USDA 数据页（功能已由数据维护中心 100% 覆盖，删 `UsdaDataView.vue` + 路由 + 导航项）；删 `MtConfigView.vue`（并入合并页）；后台导航重排为 邀请码→单位→地图→AI 与机翻配置→数据维护中心→Agent 任务台。后端无改动。详见 [FEATURE_后台管理UI调整.md](cc/FEATURE_后台管理UI调整.md)
- 价格趋势「全部」区间：三详情页（菜谱/商品/原料）价格趋势在「周/月/季/年」后加「全部」标签看完整历史；商品/原料图表由「分页 10 条」改按时间区间累积取数（复用 `/products` 已有 `start_date/end_date`）；菜谱「全部」循环分批（每批 90 天 + `loadCostHistory` 放宽 timeout 30s，纳入 `costHistoryQueue` 串行）、商品/原料「全部」单请求 `timeout` 放宽 30s 规避前端 10s 超时；纯前端、后端零改动。详见 [FEATURE_价格趋势全部区间.md](cc/FEATURE_价格趋势全部区间.md)
- 价格记录编辑与删除：商品详情页新增编辑功能（添加/编辑共用对话框，列表每行加编辑按钮）；原料详情页新增编辑和删除功能（新对话框含价格/数量/单位/商家，列表每行加编辑+删除按钮）。两页统一调用后端已有 `PUT/DELETE /products/{record_id}`。详见 [FEATURE_价格记录编辑删除.md](cc/FEATURE_价格记录编辑删除.md)
- 商家管理定位按钮：商家管理列表每项新增「定位」按钮（`mdi-crosshairs-gps`），点击使右侧地图居中显示该商家并加红色特殊标记（复用 `MerchantMapView` 已有 `selectedMerchant` 机制：watch → setCenter + setZoom(15) + selected 红标，与商家详情页一致，地图组件零改动）。`@click.stop` 阻止冒泡不触发跳详情；无坐标时按钮禁用并提示「未设置位置」；移动端点完 `nextTick` + `scrollIntoView(block:center)` 滚到地图。仅改 `MerchantsView.vue`。详见 [FEATURE_商家管理定位按钮.md](cc/FEATURE_商家管理定位按钮.md)
- 商家地图默认范围（常用地点聚焦）：商家管理地图默认范围由「框住当前页商家」（SDK 引擎甚至只跳第一个）改为「聚焦用户常用地点周边约 5km」。新增 `user_places` 表（家/公司/自定义 kind，带 is_default）+ `/places` CRUD（含设默认联动）+ `/merchants/coordinates` 全集坐标接口；`MerchantMapView` 加 `initialCenter`/`allCoordinates` props 与地点切换器（控件区下拉），翻页/搜索保持地点视角不飞回全国，补 SDK 真 fitBounds（高德 `setBounds`/百度 `setViewport`/腾讯 `fitBounds`），定位圆 1km→5km；`UserPlacesView` 子页（个人中心入口 + `/profile/places` 路由）管理地点，复用 `MapPicker`；没设地点时显示全部商家分布（allCoordinates 算 bounds、标记仍画当前页）；切换记 localStorage。清理遗留死代码 `FavoriteMerchant` 表 + `POST /merchants/route` 端点 + `map_service.calculate_route` 孤儿（前端从未接入），导出/导入链路 `favorite_merchants`→`user_places` 全迁移。详见 [FEATURE_商家地图默认范围.md](cc/FEATURE_商家地图默认范围.md)
- 时间口径统一为用户本地日：系统「把记录归到某一天」原三口径并存（用户本地日仅支出报告、服务器本地日用于成本趋势/今日推荐、UTC 日用于价格展示/最新价/sparkline），根因 recorded_at 存 UTC naive 部分代码直接 .date()。统一为**用户本地日**：前端 axios 拦截器注入 `X-Timezone`（IANA 名）+ 后端 `get_timezone` 依赖校验（缺失/非法 400，用 Header(None)+手动 raise 非 422）+ `date_range_utils` 改 `zoneinfo`（加 tzdata 依赖，Windows 必需）+ 策略丙按场景混合（逐日走 local_date_range_to_utc_range 切 SQL 区间、归日走 Python utc_datetime_to_local_date、as_of 前向填充不变，**SQL 层永不碰时区函数**三库安全）。整条成本计算链（recipe_service + ingredient_price_service 10+ 函数）+ 菜谱成本趋势/sparklines/nutrition latest-price/products_entity/meals/report 端点接入 Depends(get_timezone) + meal_recommender（含后台线程 args 透传）全改；删废弃老 price-trend；默认 UTC 兜底、不兼容旧 timezone_offset。无表结构变更/alembic/SQL 脚本。验证：后端 compileall 过、全量 pytest 482 passed（+11 新 tz 测试，25 failed 经抽查全预存非引入：default_unit_id 弃用/鉴权/表缺/网络下载等）、前端 build 过、grep 零残留。conftest 模块级 override get_timezone→UTC（既有端点测试免逐个带头，deps 校验测试用独立 app 不受影响）。设计 [spec](docs/superpowers/specs/2026-07-07-时间口径统一为用户本地日-design.md) + [plan](docs/superpowers/plans/2026-07-08-时间口径统一为用户本地日.md)。详见 [FEATURE_时间口径统一为用户本地日.md](cc/FEATURE_时间口径统一为用户本地日.md)
- 系统文档：原文档零散过时（根 README 功能特性只列 4 条、backend/README 写 Poetry+alembic 错误、docs/run.md 用 pip、cc/DEPLOYMENT 编造 Docker），重构整合为按**功能分**（非角色）的完整文档：docs/ 下 1 总导航 + concepts.md（核心概念七节：实体模型/原料关系/价格体系/时间口径/聚合计算/营养/数据归属）+ 9 篇功能手册（入门/价格/菜谱/原料/单位/营养/推荐/地图/个人）+ admin/ 8 篇（导航/部署/配置/升级/审核/Agent/数据维护/用户单位地图）。讲究：概念文档先行（DRY，功能篇链过去）、功能内用差异表标注「普通用户审核/管理员直写」、无 emoji、复用 docs/img 不补图、concepts 时间口径写统一版（因 [[FEATURE_时间口径统一为用户本地日]] 已完成不返工）。旧文档处置：根 README 精简门面、backend/README 极简指向 deploy、删 docs/conf.md+run.md（并入 admin/config-init+deploy）、cc/ 不动（开发者笔记甄别后未抄编造内容如 Docker）、忽略 docs/plans+docs/superpowers。18 篇正文，未 commit。详见 [FEATURE_系统文档.md](cc/FEATURE_系统文档.md)
