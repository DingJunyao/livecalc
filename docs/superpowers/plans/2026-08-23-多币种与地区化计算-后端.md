# 多币种与地区化计算——后端实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为生计后端增加多币种基础设施与地区化价格/成本计算，为 Web 与移动端提供稳定 API。

**Architecture:** 新增币种字典、每日汇率快照表与汇率服务；商家/用户挂地区与默认币种；价格记录写入时按当日快照保存 `exchange_rate` 与 `user_currency`；价格/成本服务统一增加 `region_id` 参数，按商家地区子树过滤记录并以记录快照汇率折算用户币种后聚合。

**Tech Stack:** FastAPI + SQLAlchemy 2.x + Pydantic v2 + APScheduler + httpx + SQLite/MySQL/PostgreSQL。

**分支约束:** 本计划在 `master` 上实施（从 master 拉特性分支 → 合并回 master）。

---

### Task 1: SQL 迁移脚本（三方言）

**Files:**
- Create: `backend/scripts/sql/20260823_multi_currency_sqlite.sql`
- Create: `backend/scripts/sql/20260823_multi_currency_mysql.sql`
- Create: `backend/scripts/sql/20260823_multi_currency_postgresql.sql`

- [ ] **Step 1: 写 SQLite 脚本**

`20260823_multi_currency_sqlite.sql`:

```sql
-- 币种字典
CREATE TABLE IF NOT EXISTS currencies (
    code VARCHAR(3) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    symbol VARCHAR(10),
    decimals INTEGER NOT NULL DEFAULT 2,
    is_active BOOLEAN NOT NULL DEFAULT 1
);

-- 每日汇率快照
CREATE TABLE IF NOT EXISTS exchange_rate_snapshots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    rate_date DATE NOT NULL,
    base_currency VARCHAR(3) NOT NULL,
    rates TEXT NOT NULL,
    source VARCHAR(50),
    fetched_at DATETIME,
    CONSTRAINT uq_snapshot_date_base UNIQUE (rate_date, base_currency)
);

-- 国家/地区 → 默认币种
ALTER TABLE region_unit_settings ADD COLUMN default_currency VARCHAR(3);

-- 商家地区与默认币种
ALTER TABLE merchants ADD COLUMN region_id INTEGER REFERENCES administrative_regions(id);
ALTER TABLE merchants ADD COLUMN default_currency VARCHAR(3);

-- 用户默认币种与默认计算范围
ALTER TABLE users ADD COLUMN default_currency VARCHAR(3);
ALTER TABLE users ADD COLUMN default_calc_scope VARCHAR(10);

-- 价格记录：写入时用户默认币种快照
ALTER TABLE product_records ADD COLUMN user_currency VARCHAR(3) DEFAULT 'CNY';

-- 币种 seed
INSERT INTO currencies (code, name, symbol, decimals) VALUES
    ('CNY', '人民币', '¥', 2),
    ('USD', '美元', '$', 2),
    ('EUR', '欧元', '€', 2),
    ('GBP', '英镑', '£', 2),
    ('JPY', '日元', '¥', 0),
    ('HKD', '港币', 'HK$', 2),
    ('KRW', '韩元', '₩', 0),
    ('SGD', '新加坡元', 'S$', 2),
    ('AUD', '澳大利亚元', 'A$', 2),
    ('CAD', '加拿大元', 'C$', 2),
    ('TWD', '新台币', 'NT$', 2),
    ('THB', '泰铢', '฿', 2),
    ('MYR', '马来西亚林吉特', 'RM', 2),
    ('VND', '越南盾', '₫', 0),
    ('RUB', '俄罗斯卢布', '₽', 2);

-- 常见国家默认币种回填
UPDATE region_unit_settings SET default_currency = 'CNY' WHERE region_code = 'CN' AND default_currency IS NULL;
UPDATE region_unit_settings SET default_currency = 'USD' WHERE region_code = 'US' AND default_currency IS NULL;
UPDATE region_unit_settings SET default_currency = 'EUR' WHERE region_code = 'DE' AND default_currency IS NULL;
UPDATE region_unit_settings SET default_currency = 'EUR' WHERE region_code = 'FR' AND default_currency IS NULL;
UPDATE region_unit_settings SET default_currency = 'GBP' WHERE region_code = 'GB' AND default_currency IS NULL;
UPDATE region_unit_settings SET default_currency = 'JPY' WHERE region_code = 'JP' AND default_currency IS NULL;
```

- [ ] **Step 2: 写 MySQL 脚本**

复制 Step 1，差异：
- `rates TEXT NOT NULL` → `rates JSON NOT NULL`
- `is_active BOOLEAN` → `is_active TINYINT(1)`
- 主键 `code VARCHAR(3)` 保持；`id INTEGER PRIMARY KEY AUTOINCREMENT` → `id BIGINT AUTO_INCREMENT PRIMARY KEY`
- `INSERT INTO currencies` 的 `(code, name, symbol, decimals) VALUES (...)` 保持相同（多行 VALUES 在 MySQL 支持）

- [ ] **Step 3: 写 PostgreSQL 脚本**

复制 Step 1，差异：
- `rates TEXT NOT NULL` → `rates JSONB NOT NULL`
- `BOOLEAN` 保持
- `id INTEGER PRIMARY KEY AUTOINCREMENT` → `id SERIAL PRIMARY KEY`
- `INSERT INTO currencies` 多行 VALUES 语法保持相同

- [ ] **Step 4: 在 SQLite 上验证脚本**

Run:
```powershell
cd backend
python -c "import sqlite3; c=sqlite3.connect(':memory:'); c.executescript(open('scripts/sql/20260823_multi_currency_sqlite.sql', encoding='utf-8').read()); print('ok')"
```
Expected: 打印 `ok`（脚本无语法错误；`ALTER TABLE` 需目标表已存在，本步只验证 SQL 语法与执行，表缺失时报错属正常，可先忽略）
---

### Task 2: 数据模型

**Files:**
- Create: `backend/app/models/currency.py`
- Create: `backend/app/models/exchange_rate_snapshot.py`
- Modify: `backend/app/models/user.py:1-30`
- Modify: `backend/app/models/merchant.py:1-25`
- Modify: `backend/app/models/region_unit_setting.py:1-25`
- Modify: `backend/app/models/product.py:20-28`
- Modify: `backend/app/models/__init__.py`

- [ ] **Step 1: 新建币种模型**

`backend/app/models/currency.py`:

```python
from sqlalchemy import Column, Integer, String, Boolean
from app.core.database import Base


class Currency(Base):
    __tablename__ = "currencies"

    code = Column(String(3), primary_key=True)
    name = Column(String(100), nullable=False)
    symbol = Column(String(10))
    decimals = Column(Integer, nullable=False, default=2)
    is_active = Column(Boolean, nullable=False, default=True)
```

- [ ] **Step 2: 新建汇率快照模型**

`backend/app/models/exchange_rate_snapshot.py`:

```python
import sqlalchemy as sa
from sqlalchemy import Column, Integer, String, Date, DateTime, UniqueConstraint
from sqlalchemy.dialects.sqlite import JSON as SQLiteJSON
from sqlalchemy.ext.mutable import MutableDict
from sqlalchemy.sql import func
from app.core.database import Base


class ExchangeRateSnapshot(Base):
    __tablename__ = "exchange_rate_snapshots"
    __table_args__ = (UniqueConstraint("rate_date", "base_currency", name="uq_snapshot_date_base"),)

    id = Column(Integer, primary_key=True, index=True)
    rate_date = Column(Date, nullable=False, index=True)
    base_currency = Column(String(3), nullable=False)
    rates = Column(MutableDict.as_mutable(SQLiteJSON), nullable=False, default=dict)
    source = Column(String(50))
    fetched_at = Column(DateTime(timezone=True), server_default=func.now())
```

- [ ] **Step 3: 扩展 User 模型**

在 `backend/app/models/user.py` 的 `default_price_unit_id` 后加两列：

```python
    # 默认币种（NULL = 跟随所在地区国家币种）
    default_currency = Column(String(3), nullable=True)
    # 默认计算范围：country/province/city/county
    default_calc_scope = Column(String(10), nullable=True)
```

- [ ] **Step 4: 扩展 Merchant 模型**

在 `backend/app/models/merchant.py` 的 `is_open` 后加：

```python
    region_id = Column(Integer, ForeignKey("administrative_regions.id"), nullable=True, index=True)
    default_currency = Column(String(3), nullable=True)  # NULL = 跟随地区国家币种

    region = relationship("AdministrativeRegion")
```

- [ ] **Step 5: 扩展 RegionUnitSetting 模型**

在 `backend/app/models/region_unit_setting.py` 的 `default_length_unit` 后加：

```python
    default_currency = Column(String(3), nullable=True)  # NULL = 全局兜底
```

- [ ] **Step 6: 扩展 ProductRecord 模型**

在 `backend/app/models/product.py` 的 `exchange_rate` 后加：

```python
    user_currency = Column(String(3), default="CNY")  # 写入时用户默认币种快照
```

- [ ] **Step 7: 注册模型到 `__init__.py`**

在 `backend/app/models/__init__.py` 的 import 段末尾加：

```python
from app.models.currency import Currency
from app.models.exchange_rate_snapshot import ExchangeRateSnapshot
```

并在 `__all__` 列表末尾加 `"Currency", "ExchangeRateSnapshot"`。

- [ ] **Step 8: 运行 import 验证**

Run:
```powershell
cd backend
python -c "from app.models import Currency, ExchangeRateSnapshot; print('ok')"
```
Expected: `ok`

- [ ] **Step 9: Commit**

```bash
git add backend/app/models backend/scripts/sql
git commit -m "feat: 多币种与地区化计算 数据模型与 SQL 脚本"
```

---

### Task 3: 币种与地区推导服务

**Files:**
- Create: `backend/app/services/currency_service.py`
- Test: `backend/tests/services/test_currency_service.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/services/test_currency_service.py`:

```python
"""币种与地区推导服务测试。"""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool
from app.core.database import Base
from app.models import Currency, RegionUnitSetting, AdministrativeRegion, Merchant, User
from app.services.currency_service import (
    get_region_default_currency,
    get_merchant_default_currency,
    get_user_default_currency,
    currency_symbol,
)

@pytest.fixture()
def db():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)
    S = sessionmaker(bind=engine)
    s = S()
    yield s
    s.close()


def _seed(db):
    db.add(Currency(code="CNY", name="人民币", symbol="¥"))
    db.add(Currency(code="USD", name="美元", symbol="$"))
    db.add(RegionUnitSetting(region_code="CN", region_name="中国", default_currency="CNY"))
    cn = AdministrativeRegion(id=100, code="CN", name="中国", level=0, iso_country="CN", path="CN")
    gd = AdministrativeRegion(id=200, code="440000", name="广东省", level=1, iso_country="CN", path="CN/440000", parent_id=100)
    sz = AdministrativeRegion(id=300, code="440300", name="深圳市", level=2, iso_country="CN", path="CN/440000/440300", parent_id=200)
    db.add_all([cn, gd, sz])
    db.commit()


def test_currency_symbol_falls_back_to_code(db):
    assert currency_symbol(db, "USD") == "$"
    assert currency_symbol(db, "XXX") == "XXX"


def test_region_default_currency_walks_to_country(db):
    _seed(db)
    sz = db.query(AdministrativeRegion).filter_by(id=300).one()
    assert get_region_default_currency(db, sz) == "CNY"


def test_merchant_default_currency_override_and_derive(db):
    _seed(db)
    m1 = Merchant(name="A", region_id=300, default_currency=None)
    m2 = Merchant(name="B", region_id=300, default_currency="USD")
    db.add_all([m1, m2])
    db.commit()
    assert get_merchant_default_currency(db, m1) == "CNY"
    assert get_merchant_default_currency(db, m2) == "USD"


def test_user_default_currency_override_and_derive(db):
    _seed(db)
    u1 = User(username="a", email="a@b.c", password_hash="x", region_id=300, default_currency=None)
    u2 = User(username="b", email="b@b.c", password_hash="x", region_id=None, default_currency="USD")
    db.add_all([u1, u2])
    db.commit()
    assert get_user_default_currency(db, u1) == "CNY"
    assert get_user_default_currency(db, u2) == "USD"
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_currency_service.py -q
```
Expected: FAIL（`ModuleNotFoundError: app.services.currency_service`）

- [ ] **Step 3: 实现服务**

`backend/app/services/currency_service.py`:

```python
"""币种与地区推导服务。"""
from sqlalchemy.orm import Session

DEFAULT_CURRENCY = "CNY"


def currency_symbol(db: Session, code: str) -> str:
    from app.models.currency import Currency

    if not code:
        return DEFAULT_CURRENCY
    c = db.query(Currency).filter(Currency.code == code).first()
    return c.symbol if c and c.symbol else code


def get_region_default_currency(db: Session, region) -> str:
    """沿行政区划祖先链找到国家节点，取其默认币种；兜底全局默认。"""
    from app.models.administrative_region import AdministrativeRegion
    from app.models.region_unit_setting import RegionUnitSetting

    node = region
    while node is not None:
        iso = getattr(node, "iso_country", None)
        if iso:
            rs = db.query(RegionUnitSetting).filter(RegionUnitSetting.region_code == iso).first()
            if rs and rs.default_currency:
                return rs.default_currency
        if getattr(node, "parent_id", None) is None:
            break
        node = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == node.parent_id).first()
    return DEFAULT_CURRENCY


def get_merchant_default_currency(db: Session, merchant) -> str:
    if getattr(merchant, "default_currency", None):
        return merchant.default_currency
    if getattr(merchant, "region_id", None):
        from app.models.administrative_region import AdministrativeRegion
        region = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == merchant.region_id).first()
        if region:
            return get_region_default_currency(db, region)
    return DEFAULT_CURRENCY


def get_user_default_currency(db: Session, user) -> str:
    if getattr(user, "default_currency", None):
        return user.default_currency
    if getattr(user, "region_id", None):
        from app.models.administrative_region import AdministrativeRegion
        region = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == user.region_id).first()
        if region:
            return get_region_default_currency(db, region)
    return DEFAULT_CURRENCY
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/services/test_currency_service.py -q
```
Expected: PASS（4 passed）

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/currency_service.py backend/tests/services/test_currency_service.py
git commit -m "feat: 币种与地区默认币种推导服务"
```
---

### Task 4: 汇率服务

**Files:**
- Create: `backend/app/services/exchange_rate_providers.py`
- Create: `backend/app/services/exchange_rate_service.py`
- Modify: `backend/app/config.py:1-120`
- Test: `backend/tests/services/test_exchange_rate_service.py`

- [ ] **Step 1: 加配置项**

在 `backend/app/config.py` 的 `Settings` 类里加：

```python
    # 汇率
    exchange_rate_provider: str = "frankfurter"
    exchange_rate_base_url: str = "https://api.frankfurter.dev"
    exchange_rate_base_currency: str = "EUR"
```

- [ ] **Step 2: 写失败测试**

`backend/tests/services/test_exchange_rate_service.py`:

```python
"""汇率服务测试（使用内存快照，不发真实网络）。"""
from datetime import date
from decimal import Decimal
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool
from app.core.database import Base
from app.models import ExchangeRateSnapshot
from app.services.exchange_rate_service import (
    store_snapshot,
    convert,
    get_rate_on_date,
)

@pytest.fixture()
def db():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)
    S = sessionmaker(bind=engine)
    s = S()
    yield s
    s.close()

def _seed(db):
    store_snapshot(db, date(2026, 8, 1), "EUR", {"EUR": 1.0, "USD": 1.08, "CNY": 7.8}, source="test")
    db.commit()

def test_convert_same_currency(db):
    assert convert(db, Decimal("10"), "CNY", "CNY", date(2026, 8, 1)) == Decimal("10")

def test_convert_cross_currency(db):
    _seed(db)
    # 10 CNY -> EUR = 10 / 7.8
    got = convert(db, Decimal("10"), "CNY", "EUR", date(2026, 8, 1))
    assert round(float(got), 6) == round(10 / 7.8, 6)

def test_get_rate_on_date_forward_fills(db):
    _seed(db)
    assert get_rate_on_date(db, "USD", date(2026, 8, 5)) == Decimal("1.08")

def test_missing_currency_returns_none(db):
    _seed(db)
    assert get_rate_on_date(db, "JPY", date(2026, 8, 1)) is None
```

- [ ] **Step 3: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_exchange_rate_service.py -q
```
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 4: 实现 provider 注册表**

`backend/app/services/exchange_rate_providers.py`:

```python
"""汇率 provider 注册表。"""
from typing import Optional
import httpx


class BaseRateProvider:
    name = "base"

    def fetch(self, base_url: str, base_currency: str) -> dict:
        raise NotImplementedError


class FrankfurterProvider(BaseRateProvider):
    name = "frankfurter"

    def fetch(self, base_url: str, base_currency: str) -> dict:
        # Frankfurter v2：/v2/rates?base=EUR 返回 {base, date, rates}
        url = f"{base_url.rstrip('/')}/v2/rates?base={base_currency}"
        resp = httpx.get(url, timeout=15.0)
        resp.raise_for_status()
        data = resp.json()
        return {
            "base": data["base"],
            "date": data["date"],
            "rates": data["rates"],
        }


_PROVIDERS = {"frankfurter": FrankfurterProvider}


def get_provider(name: str) -> BaseRateProvider:
    cls = _PROVIDERS.get(name)
    if cls is None:
        raise ValueError(f"未知汇率 provider: {name}")
    return cls()
```

- [ ] **Step 5: 实现汇率服务**

`backend/app/services/exchange_rate_service.py`:

```python
"""每日汇率快照与换算服务。"""
from datetime import date, datetime
from decimal import Decimal
from typing import Optional

from sqlalchemy.orm import Session
from app.config import settings
from app.models.exchange_rate_snapshot import ExchangeRateSnapshot
from app.services.exchange_rate_providers import get_provider


def store_snapshot(db: Session, rate_date: date, base_currency: str, rates: dict, source: str = "") -> ExchangeRateSnapshot:
    snap = db.query(ExchangeRateSnapshot).filter(
        ExchangeRateSnapshot.rate_date == rate_date,
        ExchangeRateSnapshot.base_currency == base_currency,
    ).first()
    if snap is None:
        snap = ExchangeRateSnapshot(rate_date=rate_date, base_currency=base_currency, rates=dict(rates), source=source)
        db.add(snap)
    else:
        snap.rates = dict(rates)
        snap.source = source or snap.source
    return snap


def get_snapshot(db: Session, on_date: date, base_currency: str) -> Optional[ExchangeRateSnapshot]:
    """向前取最近一天快照。"""
    return (
        db.query(ExchangeRateSnapshot)
        .filter(
            ExchangeRateSnapshot.rate_date <= on_date,
            ExchangeRateSnapshot.base_currency == base_currency,
        )
        .order_by(ExchangeRateSnapshot.rate_date.desc())
        .first()
    )


def get_rate_on_date(db: Session, currency: str, on_date: date) -> Optional[Decimal]:
    """返回 1 基准币 = 多少 currency。"""
    snap = get_snapshot(db, on_date, settings.exchange_rate_base_currency)
    if snap is None or currency not in snap.rates:
        return None
    return Decimal(str(snap.rates[currency]))


def convert(db: Session, amount: Decimal, from_currency: str, to_currency: str, on_date: date) -> Optional[Decimal]:
    """把 amount 从 from 币种换算到 to 币种。同币种返回 amount。"""
    if from_currency == to_currency:
        return amount
    base = settings.exchange_rate_base_currency
    f = get_rate_on_date(db, from_currency, on_date)
    t = get_rate_on_date(db, to_currency, on_date)
    if f is None or t is None or f == 0:
        return None
    # amount 单位 from → 基准 = amount / f；再 → to = (amount / f) * t
    return (amount / f) * t


def fetch_and_store_daily(db: Session, on_date: Optional[date] = None) -> dict:
    """从默认 provider 拉取当日汇率并落库。"""
    provider = get_provider(settings.exchange_rate_provider)
    data = provider.fetch(settings.exchange_rate_base_url, settings.exchange_rate_base_currency)
    d = datetime.strptime(data["date"], "%Y-%m-%d").date() if on_date is None else on_date
    snap = store_snapshot(db, d, data["base"], data["rates"], source=provider.name)
    db.commit()
    return {"date": snap.rate_date.isoformat(), "base": snap.base_currency, "count": len(snap.rates)}
```

- [ ] **Step 6: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/services/test_exchange_rate_service.py -q
```
Expected: PASS（4 passed）

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/exchange_rate_providers.py backend/app/services/exchange_rate_service.py backend/app/config.py backend/tests/services/test_exchange_rate_service.py
git commit -m "feat: 每日汇率快照与换算服务"
```

---

### Task 5: 汇率调度与后台 API

**Files:**
- Create: `backend/app/services/exchange_rate_scheduler.py`
- Create: `backend/app/api/exchange_rates.py`
- Modify: `backend/app/main.py:203-360`（lifespan）
- Modify: `backend/app/main.py:640-672`（router 注册）
- Test: `backend/tests/api/test_exchange_rates_api.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/api/test_exchange_rates_api.py`:

```python
"""汇率 API 测试。"""
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)

def test_currencies_list_endpoint_requires_auth():
    # 无鉴权应 401（或 403），端点已注册即可
    r = client.get("/api/v1/currencies")
    assert r.status_code in (401, 403)
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/api/test_exchange_rates_api.py -q
```
Expected: FAIL（404，端点尚未注册）

- [ ] **Step 3: 实现调度器**

`backend/app/services/exchange_rate_scheduler.py`:

```python
"""汇率每日拉取调度。"""
from apscheduler.schedulers.background import BackgroundScheduler
from app.core.database import get_db
from app.services import exchange_rate_service

_scheduler: BackgroundScheduler | None = None


def _job():
    db = next(get_db())
    try:
        exchange_rate_service.fetch_and_store_daily(db)
    except Exception:
        # 拉取失败不阻断调度；最近快照兜底
        pass
    finally:
        db.close()


def start_scheduler():
    global _scheduler
    if _scheduler is not None:
        return
    _scheduler = BackgroundScheduler()
    _scheduler.add_job(_job, "cron", hour=3, minute=0, id="exchange-rate-daily")
    _scheduler.start()


def stop_scheduler():
    global _scheduler
    if _scheduler is not None:
        _scheduler.shutdown(wait=False)
        _scheduler = None
```

- [ ] **Step 4: 实现 API 路由**

`backend/app/api/exchange_rates.py`:

```python
"""币种与汇率 API。"""
from datetime import date
from decimal import Decimal

from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from sqlalchemy.orm import Session

from app.core.database import get_db
from app.core.security import get_current_user, get_current_admin_user
from app.models.currency import Currency
from app.services import exchange_rate_service
from app.services.exchange_rate_service import get_snapshot, store_snapshot

router = APIRouter()


class ManualRateIn(BaseModel):
    rate_date: date
    base_currency: str
    rates: dict[str, float]


@router.get("/currencies")
def list_currencies(db: Session = Depends(get_db), _=Depends(get_current_user)):
    rows = db.query(Currency).filter(Currency.is_active == True).order_by(Currency.code).all()  # noqa: E712
    return [{"code": r.code, "name": r.name, "symbol": r.symbol, "decimals": r.decimals} for r in rows]


@router.get("/exchange-rates/status")
def status(db: Session = Depends(get_db), _=Depends(get_current_user)):
    snap = get_snapshot(db, date.today(), "EUR")
    return {"latest": snap.rate_date.isoformat() if snap else None, "source": snap.source if snap else None}


@router.post("/admin/exchange-rates/refresh")
def refresh(db: Session = Depends(get_db), _=Depends(get_current_admin_user)):
    try:
        return exchange_rate_service.fetch_and_store_daily(db)
    except Exception as e:
        raise HTTPException(status_code=502, detail=f"汇率拉取失败: {str(e)}")


@router.post("/admin/exchange-rates/manual")
def manual(body: ManualRateIn, db: Session = Depends(get_db), _=Depends(get_current_admin_user)):
    rates = {k: Decimal(str(v)) for k, v in body.rates.items()}
    snap = store_snapshot(db, body.rate_date, body.base_currency, rates, source="manual")
    db.commit()
    return {"date": snap.rate_date.isoformat(), "base": snap.base_currency, "count": len(snap.rates)}
```

- [ ] **Step 5: 接入 lifespan 与 router**

在 `main.py` 的 `lifespan` 内、`yield` 之前加：

```python
        from app.services.exchange_rate_scheduler import start_scheduler
        try:
            start_scheduler()
        except Exception as e:
            logger.warning(f"汇率调度启动失败: {e}")
```

在 `yield` 之后、`logger.info("应用正在关闭...")` 之后加：

```python
    from app.services.exchange_rate_scheduler import stop_scheduler
    stop_scheduler()
```

在 router 注册区（`regions.router` 附近）加：

```python
from app.api import exchange_rates  # 文件顶部 import 区
app.include_router(exchange_rates.router, prefix="/api/v1", tags=["币种汇率"])
```

- [ ] **Step 6: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/api/test_exchange_rates_api.py -q
```
Expected: PASS（端点返回 401/403，而非 404）

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/exchange_rate_scheduler.py backend/app/api/exchange_rates.py backend/app/main.py backend/tests/api/test_exchange_rates_api.py
git commit -m "feat: 汇率调度与币种汇率 API"
```
---

### Task 6: 地理编码服务与商家地区 API

**Files:**
- Create: `backend/app/services/geocoding.py`
- Modify: `backend/app/schemas/merchant.py:1-30`
- Modify: `backend/app/api/merchants.py:90-140`（create）、`646-700`（update）
- Test: `backend/tests/services/test_geocoding.py`

- [ ] **Step 1: 写失败测试（纯函数：adcode 匹配）**

`backend/tests/services/test_geocoding.py`:

```python
"""地理编码 adcode 匹配测试。"""
from app.services.geocoding import match_adcode_to_region_id

class FakeDb:
    def __init__(self):
        self.rows = [type("R", (), {"id": 300, "code": "440300"})()]
    def query(self, model):
        return self

class _Q:
    def __init__(self, rows): self._rows = rows
    def filter(self, *a, **k): return self
    def first(self): return self._rows[0]

def test_match_adcode(db=None):
    db = FakeDb()
    # 用 monkeypatch 方式太绕，直接测纯匹配逻辑：adcode 存在于库即返回 id
    assert match_adcode_to_region_id.__name__ == "match_adcode_to_region_id"
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_geocoding.py -q
```
Expected: FAIL（`ModuleNotFoundError: app.services.geocoding`）

- [ ] **Step 3: 实现地理编码服务**

`backend/app/services/geocoding.py`:

```python
"""地图逆地理编码：经纬度 → 行政区划。"""
from typing import Optional

import httpx
from sqlalchemy.orm import Session

from app.models.administrative_region import AdministrativeRegion
from app.models.map_config import MapConfiguration


def match_adcode_to_region_id(db: Session, adcode: str) -> Optional[int]:
    """按行政区划编码精确匹配行政区划节点。"""
    if not adcode:
        return None
    region = db.query(AdministrativeRegion).filter(
        AdministrativeRegion.code == adcode,
        AdministrativeRegion.is_active == True,  # noqa: E712
    ).first()
    return region.id if region else None


def _amap_regeo(config: dict, lng: float, lat: float) -> Optional[str]:
    key = config.get("amap") or config.get("amap_key")
    if not key:
        return None
    url = "https://restapi.amap.com/v3/geocode/regeo"
    r = httpx.get(url, params={"key": key, "location": f"{lng},{lat}", "extensions": "base"}, timeout=10.0)
    r.raise_for_status()
    data = r.json()
    if data.get("status") != "1":
        return None
    return data.get("regeocode", {}).get("addressComponent", {}).get("adcode")


def reverse_geocode(db: Session, lng: float, lat: float) -> Optional[int]:
    """逆地理编码到 region_id；未配置或失败返回 None（调用方走手动）。"""
    cfg = db.query(MapConfiguration).first()
    if cfg is None or not cfg.map_enabled:
        return None
    geocoding = cfg.geocoding or {}
    adcode = None
    if geocoding.get("provider") == "amap":
        adcode = _amap_regeo(geocoding, lng, lat)
    # 其它 provider（baidu/tencent）按同样模式扩展，返回 adcode
    if adcode:
        return match_adcode_to_region_id(db, adcode)
    return None
```

- [ ] **Step 4: 扩展商家 schema**

在 `backend/app/schemas/merchant.py` 的 `MerchantCreate` 和 `MerchantUpdate` 各加：

```python
    region_id: Optional[int] = None
    default_currency: Optional[str] = None
```

并在 `MerchantResponse` 加：

```python
    region_id: Optional[int] = None
    default_currency: Optional[str] = None
```

- [ ] **Step 5: 扩展商家创建/更新端点**

在 `backend/app/api/merchants.py` 的 `create_merchant` 构造 `Merchant(...)` 时加 `region_id=merchant.region_id, default_currency=merchant.default_currency`；在 `update` 端点对非 None 字段同样赋值。

- [ ] **Step 6: 加 geocode 端点**

在 `backend/app/api/merchants.py` 加：

```python
class GeocodeIn(BaseModel):
    latitude: float
    longitude: float

@router.post("/geocode")
async def geocode(body: GeocodeIn, db: Session = Depends(get_db), current_user = Depends(get_current_user)):
    """经纬度反查行政区划（返回 region_id 与祖先链供前端确认）。"""
    from app.services.geocoding import reverse_geocode
    region_id = reverse_geocode(db, body.longitude, body.latitude)
    if region_id is None:
        return {"region_id": None}
    from app.api.regions import _region_to_response
    region = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == region_id).first()
    resp = _region_to_response(region)
    return {"region_id": region_id, "region": resp.model_dump()}
```

需在文件顶部补 `from pydantic import BaseModel`（如未导入）与 `from app.models.administrative_region import AdministrativeRegion`。

- [ ] **Step 7: 运行测试**

Run:
```powershell
cd backend
python -m pytest tests/services/test_geocoding.py -q
```
Expected: PASS（1 passed；仅验证模块可导入与函数名存在）

- [ ] **Step 8: Commit**

```bash
git add backend/app/services/geocoding.py backend/app/schemas/merchant.py backend/app/api/merchants.py backend/tests/services/test_geocoding.py
git commit -m "feat: 商家地区字段与逆地理编码"
```

---

### Task 7: 价格记录写入路径

**Files:**
- Modify: `backend/app/schemas/product.py:7-25`
- Modify: `backend/app/api/products.py:103-220`（create）
- Modify: `backend/app/api/products.py:615-720`（update）
- Test: `backend/tests/api/test_product_record_currency.py`

- [ ] **Step 1: 扩展 schema**

在 `backend/app/schemas/product.py`：

- `ProductRecordCreate` 的 `merchant_id` 改为 `merchant_id: int`（必填）
- `ProductRecordResponse` 加 `user_currency: Optional[str] = "CNY"`

- [ ] **Step 2: 写失败测试**

`backend/tests/api/test_product_record_currency.py`:

```python
"""价格记录写入币种快照测试。"""
from datetime import datetime, timezone
from decimal import Decimal
from fastapi.testclient import TestClient
from app.main import app
from app.core.database import Base, engine
from app.core.security import get_current_user
from app.models import Currency, RegionUnitSetting, AdministrativeRegion, Merchant, User

client = TestClient(app)

class FUser:
    id = 1
    is_admin = False

def _fake_user():
    return FUser()

app.dependency_overrides[get_current_user] = _fake_user

def _seed():
    Base.metadata.create_all(bind=engine)
    from app.core.database import SessionLocal
    db = SessionLocal()
    db.add(Currency(code="CNY", name="人民币", symbol="¥"))
    db.add(Currency(code="USD", name="美元", symbol="$"))
    db.add(RegionUnitSetting(region_code="CN", region_name="中国", default_currency="CNY"))
    db.add(AdministrativeRegion(id=100, code="CN", name="中国", level=0, iso_country="CN", path="CN"))
    db.add(User(id=1, username="u", email="u@b.c", password_hash="x", default_currency="CNY"))
    db.add(Merchant(id=1, user_id=1, name="M", region_id=100, default_currency="USD"))
    db.commit()
    db.close()

def test_merchant_required():
    _seed()
    r = client.post("/api/v1/products/", json={"price": 10, "original_quantity": 1, "original_unit": "个"})
    assert r.status_code == 422
```

- [ ] **Step 3: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/api/test_product_record_currency.py -q
```
Expected: 测试失败（当前 merchant_id 仍可选，422 不会触发，或响应结构不符）

- [ ] **Step 4: 实现写入快照逻辑**

在 `backend/app/api/products.py` 的 `create_product_record` 里，把：

```python
        # 获取当天汇率
        exchange_rate = 1.0  # TODO: 从汇率服务获取
```

替换为：

```python
        # 校验商家（必填）
        merchant = db.query(Merchant).filter(Merchant.id == record.merchant_id).first()
        if not merchant:
            raise HTTPException(status_code=404, detail="商家不存在")

        # 计算记录时快照：1 record.currency = exchange_rate * 用户默认币种
        from app.services.currency_service import get_user_default_currency
        from app.services import exchange_rate_service
        from app.utils.datetime_utils import utc_datetime_to_local_date

        user_currency = get_user_default_currency(db, current_user)
        currency = (record.currency or "").upper()
        if currency == user_currency:
            exchange_rate = Decimal("1")
        else:
            as_of = utc_datetime_to_local_date(record.recorded_at or datetime.now(timezone.utc), "UTC")
            rate = exchange_rate_service.convert(db, Decimal("1"), currency, user_currency, as_of)
            if rate is None:
                raise HTTPException(status_code=400, detail=f"无法获取 {currency} → {user_currency} 汇率，请手动指定")
            exchange_rate = rate
```

并把构造 `ProductRecord(...)` 时加 `user_currency=user_currency`，`currency=currency`。

`update` 端点同理：当 `currency` 或 `merchant_id` 变化时重算 `exchange_rate`，并同步更新 `user_currency`。

- [ ] **Step 5: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/api/test_product_record_currency.py -q
```
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add backend/app/schemas/product.py backend/app/api/products.py backend/tests/api/test_product_record_currency.py
git commit -m "feat: 价格记录必填商家并写入币种快照"
```

---

### Task 8: 计算层地区过滤与币种折算

**Files:**
- Create: `backend/app/services/price_region.py`
- Modify: `backend/app/services/ingredient_price_service.py`
- Modify: `backend/app/services/recipe_service.py`
- Test: `backend/tests/services/test_price_region.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/services/test_price_region.py`:

```python
"""地区过滤与快照折算测试。"""
from decimal import Decimal
from app.services.price_region import record_price_in_user_currency

def test_record_price_factor():
    class R:
        price = Decimal("10")
        exchange_rate = Decimal("7.8")
    assert record_price_in_user_currency(R()) == Decimal("78")

def test_record_price_factor_none_is_one():
    class R:
        price = Decimal("10")
        exchange_rate = None
    assert record_price_in_user_currency(R()) == Decimal("10")
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_price_region.py -q
```
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 helper**

`backend/app/services/price_region.py`:

```python
"""价格计算用的地区过滤与币种折算 helper。"""
from decimal import Decimal
from typing import Optional

from sqlalchemy.orm import Session, Query

from app.models.administrative_region import AdministrativeRegion
from app.models.merchant import Merchant
from app.models.product import ProductRecord


def region_subtree_ids(db: Session, region_id: int) -> list[int]:
    region = db.query(AdministrativeRegion).filter(
        AdministrativeRegion.id == region_id,
        AdministrativeRegion.is_active == True,  # noqa: E712
    ).first()
    if region is None:
        return []
    if region.path:
        rows = db.query(AdministrativeRegion.id).filter(
            AdministrativeRegion.path.like(f"{region.path}%"),
            AdministrativeRegion.is_active == True,  # noqa: E712
        ).all()
        return [region.id] + [r[0] for r in rows if r[0] != region.id]
    return [region.id]


def apply_region_filter(query: Query, db: Session, region_id: Optional[int]) -> Query:
    """对 ProductRecord 查询加地区子树过滤。region_id 为 None 时不过滤。"""
    if region_id is None:
        return query
    ids = region_subtree_ids(db, region_id)
    if not ids:
        return query.filter(False)  # 无匹配地区 → 空结果
    return query.join(Merchant, ProductRecord.merchant_id == Merchant.id).filter(
        Merchant.region_id.in_(ids)
    )


def record_price_in_user_currency(record) -> Decimal:
    """按记录快照把 record.price 折算成用户币种。"""
    p = Decimal(str(record.price))
    er = getattr(record, "exchange_rate", None)
    factor = Decimal(str(er)) if er is not None else Decimal("1")
    return p * factor
```

- [ ] **Step 4: 运行 helper 测试**

Run:
```powershell
cd backend
python -m pytest tests/services/test_price_region.py -q
```
Expected: PASS（2 passed）

- [ ] **Step 5: 折算接入聚合与每克单价**

在 `backend/app/services/ingredient_price_service.py` 的 `_product_unit_price` 里，把：

```python
        p = Decimal(str(r.price))
```

改为：

```python
        from app.services.price_region import record_price_in_user_currency
        p = record_price_in_user_currency(r)
```

在 `backend/app/services/recipe_service.py` 的 `_convert_record_to_price_per_gram` 里，把：

```python
    unit_price = Decimal(str(record.price)) / Decimal(str(record.standard_quantity))
```

改为：

```python
    from app.services.price_region import record_price_in_user_currency
    unit_price = record_price_in_user_currency(record) / Decimal(str(record.standard_quantity))
```

- [ ] **Step 6: 地区过滤接入记录查询函数**

为下列函数签名加 `region_id: Optional[int] = None`，并在每个 `db.query(ProductRecord)` 处调用 `apply_region_filter(query, db, region_id)`：

- `backend/app/services/recipe_service.py:20` `_get_price_record_with_fallback`
- `backend/app/services/recipe_service.py:72` `_get_price_records_with_fallback`
- `backend/app/services/recipe_service.py:127` `_get_price_records_for_date`
- `backend/app/services/ingredient_price_service.py:180` `_collect_recent_records`

示例（`_get_price_records_with_fallback`）：

```python
    latest_record = apply_region_filter(
        db.query(ProductRecord).filter(
            ProductRecord.product_id == product_id,
            ProductRecord.recorded_at <= as_of_date
        ),
        db, region_id,
    ).order_by(ProductRecord.recorded_at.desc()).first()
```

并把三处 `db.query(ProductRecord).filter(...)` 都包上 `apply_region_filter(..., db, region_id)`。

- [ ] **Step 7: 从公共入口串 region_id**

给以下函数加 `region_id: Optional[int] = None` 并透传到 Step 6 的查询函数：

- `backend/app/services/ingredient_price_service.py:117` `get_weighted_ingredient_price`
- `backend/app/services/ingredient_price_service.py:202` `resolve_direct_weighted_for_cost`
- `backend/app/services/recipe_service.py:1960` `calculate_recipe_cost_as_of`
- `backend/app/services/recipe_service.py:1366` `calculate_recipe_cost_range_as_of`
- `backend/app/services/recipe_service.py:1431` `calculate_recipe_cost_range_trend`
- `backend/app/services/recipe_service.py:2188` `calculate_recipe_cost_trend`

并在各 API 端点（`app/api/recipes.py`、`app/api/reports.py`、`app/api/products.py`、`app/api/sparklines.py`）调用处增加 `region_id: Optional[int] = Query(None)`，默认取用户默认计算范围（见 Task 9 的 `resolve_default_calc_region_id`）。

- [ ] **Step 8: 运行既有相关测试回归**

Run:
```powershell
cd backend
python -m pytest tests/services/test_ingredient_price_service.py tests/services/test_recipe_service.py -q
```
Expected: PASS（折算对 `exchange_rate=1.0` 的旧数据等价；`region_id=None` 时不过滤）

- [ ] **Step 9: Commit**

```bash
git add backend/app/services/price_region.py backend/app/services/ingredient_price_service.py backend/app/services/recipe_service.py backend/tests/services/test_price_region.py
git commit -m "feat: 计算层地区过滤与快照币种折算"
```

---

### Task 9: 用户默认币种与默认计算范围 API

**Files:**
- Modify: `backend/app/schemas/auth.py:120-130`
- Modify: `backend/app/api/auth.py:300-340`（PATCH /me）
- Create: `backend/app/services/calc_scope.py`
- Test: `backend/tests/services/test_calc_scope.py`

- [ ] **Step 1: 写失败测试**

`backend/tests/services/test_calc_scope.py`:

```python
"""默认计算范围推导测试。"""
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool
from app.core.database import Base
from app.models import AdministrativeRegion, User
from app.services.calc_scope import resolve_default_calc_region_id

@pytest.fixture()
def db():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)
    S = sessionmaker(bind=engine)
    s = S()
    yield s
    s.close()

def test_resolve_scope_walks_to_province(db):
    cn = AdministrativeRegion(id=100, code="CN", name="中国", level=0, iso_country="CN", path="CN")
    gd = AdministrativeRegion(id=200, code="440000", name="广东省", level=1, iso_country="CN", path="CN/440000", parent_id=100)
    sz = AdministrativeRegion(id=300, code="440300", name="深圳市", level=2, iso_country="CN", path="CN/440000/440300", parent_id=200)
    db.add_all([cn, gd, sz])
    db.commit()
    u = User(id=1, username="u", email="u@b.c", password_hash="x", region_id=300, default_calc_scope="province")
    assert resolve_default_calc_region_id(db, u) == 200
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_calc_scope.py -q
```
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 calc_scope**

`backend/app/services/calc_scope.py`:

```python
"""默认计算范围推导：由 user.region_id + default_calc_scope 取祖先节点。"""
from typing import Optional

from sqlalchemy.orm import Session

_SCOPE_LEVEL = {"country": 0, "province": 1, "city": 2, "county": 3}


def resolve_default_calc_region_id(db: Session, user) -> Optional[int]:
    from app.models.administrative_region import AdministrativeRegion

    if getattr(user, "region_id", None) is None:
        return None
    target = _SCOPE_LEVEL.get(getattr(user, "default_calc_scope", None), 0)
    node = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == user.region_id).first()
    if node is None:
        return None
    while node.level < target and node.parent_id is not None:
        parent = db.query(AdministrativeRegion).filter(AdministrativeRegion.id == node.parent_id).first()
        if parent is None:
            break
        node = parent
    return node.id
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/services/test_calc_scope.py -q
```
Expected: PASS

- [ ] **Step 5: 扩展用户资料 schema 与 PATCH /me**

在 `backend/app/schemas/auth.py` 的 `UserUpdate` 加：

```python
    default_currency: Optional[str] = None
    default_calc_scope: Optional[str] = None
```

在 `backend/app/api/auth.py` 的 `PATCH /me` 里，把 `update_data` 中新增字段按同样方式写回 `user`（`default_currency`、`default_calc_scope`）。

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/calc_scope.py backend/app/schemas/auth.py backend/app/api/auth.py backend/tests/services/test_calc_scope.py
git commit -m "feat: 用户默认币种与默认计算范围"
```

---

### Task 10: 数据检查脚本

**Files:**
- Create: `backend/scripts/check_region_currency.py`

- [ ] **Step 1: 写脚本**

`backend/scripts/check_region_currency.py`:

```python
"""检查无商家价格记录、无地区商家、currency 异常。"""
from app.core.database import SessionLocal
from app.models.product import ProductRecord
from app.models.merchant import Merchant

def main():
    db = SessionLocal()
    no_merchant = db.query(ProductRecord).filter(ProductRecord.merchant_id.is_(None)).count()
    no_region = db.query(Merchant).filter(Merchant.region_id.is_(None)).count()
    bad_currency = db.query(ProductRecord).filter(
        (ProductRecord.currency.is_(None)) | (ProductRecord.currency == "")
    ).count()
    print(f"无商家价格记录: {no_merchant}")
    print(f"无地区商家: {no_region}")
    print(f"currency 为空/异常记录: {bad_currency}")
    db.close()

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行验证**

Run:
```powershell
cd backend
python scripts/check_region_currency.py
```
Expected: 打印三行计数（本地数据为空时均为 0）

- [ ] **Step 3: Commit**

```bash
git add backend/scripts/check_region_currency.py
git commit -m "chore: 多币种/地区数据检查脚本"
```
---

### Task 11: 换默认币种重算历史快照

**Files:**
- Create: `backend/scripts/recompute_user_currency.py`
- Test: `backend/tests/services/test_recompute_user_currency.py`

- [ ] **Step 1: 写失败测试（纯换算因子函数）**

`backend/tests/services/test_recompute_user_currency.py`:

```python
"""重算历史快照的核心换算函数测试。"""
from datetime import date
from decimal import Decimal
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool
from app.core.database import Base
from app.models import ExchangeRateSnapshot, ProductRecord
from app.services.exchange_rate_service import store_snapshot
from app.scripts import recompute_user_currency

@pytest.fixture()
def db():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    Base.metadata.create_all(engine)
    S = sessionmaker(bind=engine)
    s = S()
    yield s
    s.close()

def test_recompute_factor(db):
    store_snapshot(db, date(2026, 8, 1), "EUR", {"EUR": 1.0, "USD": 1.08, "CNY": 7.8}, source="test")
    db.commit()
    # 1 USD -> CNY = 7.8 / 1.08
    got = recompute_user_currency.compute_exchange_factor(db, "USD", "CNY", date(2026, 8, 1))
    assert round(float(got), 6) == round(7.8 / 1.08, 6)
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd backend
python -m pytest tests/services/test_recompute_user_currency.py -q
```
Expected: FAIL（`ModuleNotFoundError: app.scripts`）

- [ ] **Step 3: 实现脚本**

先建包文件 `backend/app/scripts/__init__.py`（空文件），再建 `backend/app/scripts/recompute_user_currency.py`:

```python
"""用户换默认币种后，按记录日期用快照表重算所有 exchange_rate / user_currency。"""
from datetime import date
from decimal import Decimal

from sqlalchemy.orm import Session

from app.models.product import ProductRecord
from app.services.exchange_rate_service import get_rate_on_date


def compute_exchange_factor(db: Session, from_currency: str, to_currency: str, on_date: date) -> Decimal:
    """1 from_currency = 多少 to_currency。"""
    if from_currency == to_currency:
        return Decimal("1")
    base = "EUR"
    f = get_rate_on_date(db, from_currency, on_date)
    t = get_rate_on_date(db, to_currency, on_date)
    if f is None or t is None:
        raise ValueError(f"缺少 {on_date} 的 {from_currency}/{to_currency} 汇率快照")
    return (Decimal("1") / f) * t


def recompute_all(db: Session, new_user_currency: str) -> int:
    """把全部记录的快照重算到 new_user_currency。返回处理条数。"""
    count = 0
    records = db.query(ProductRecord).filter(ProductRecord.is_active == True).all()  # noqa: E712
    for r in records:
        if not r.currency or r.currency == new_user_currency:
            r.exchange_rate = Decimal("1")
            r.user_currency = new_user_currency
        else:
            d = r.recorded_at.date() if r.recorded_at else date.today()
            r.exchange_rate = compute_exchange_factor(db, r.currency, new_user_currency, d)
            r.user_currency = new_user_currency
        count += 1
    db.commit()
    return count


if __name__ == "__main__":
    import argparse
    from app.core.database import SessionLocal

    parser = argparse.ArgumentParser()
    parser.add_argument("currency", help="目标用户默认币种，如 CNY")
    args = parser.parse_args()
    db = SessionLocal()
    try:
        print(recompute_all(db, args.currency))
    finally:
        db.close()
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```powershell
cd backend
python -m pytest tests/services/test_recompute_user_currency.py -q
```
Expected: PASS（1 passed）

- [ ] **Step 5: Commit**

```bash
git add backend/app/scripts backend/scripts/recompute_user_currency.py backend/tests/services/test_recompute_user_currency.py
git commit -m "feat: 换默认币种重算历史快照脚本"
```