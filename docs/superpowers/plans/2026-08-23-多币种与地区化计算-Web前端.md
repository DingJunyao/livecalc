# 多币种与地区化计算——Web 前端实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Web 前端落地多币种与地区化计算：价格记录表单币种联动、原币种+换算展示、商家地区/默认币种、用户默认币种与默认计算范围、后台币种汇率管理、本地模式对齐。

**Architecture:** 新增共享类型、货币工具与 `RegionSelect` 级联组件、币种汇率 API；改造价格记录/商家/个人中心/详情/后台视图；本地模式同步实现等价逻辑。所有金额展示统一走 `formatMoney` 与 `useUserCurrency`。

**Tech Stack:** Vue 3 + Vuetify 3 + Pinia + TypeScript + Vite；本地模式用 IndexedDB。

**分支约束:** 本计划在 `master` 上实施（依赖后端计划已完成合并）。

---

### Task 1: 类型与共享货币工具

**Files:**
- Modify: `frontend/src/types/index.ts`
- Create: `frontend/src/utils/currency.ts`
- Create: `frontend/src/composables/useUserCurrency.ts`
- Test: `frontend/src/utils/currency.spec.ts`（用 Vitest 若已配置；否则建独立纯函数并 `vitest`）

- [ ] **Step 1: 扩展类型**

在 `frontend/src/types/index.ts` 加：

```ts
export interface Currency {
  code: string
  name: string
  symbol: string | null
  decimals: number
}
```

给 `User` 加 `default_currency: string | null` 和 `default_calc_scope: string | null`。新增：

```ts
export interface Merchant {
  id: number
  name: string
  address?: string | null
  latitude?: number | null
  longitude?: number | null
  is_open: boolean
  region_id?: number | null
  default_currency?: string | null
  pending_proposal?: Record<string, any> | null
}
```

给 `PriceRecord` 加 `currency: string`、`exchange_rate: number`、`user_currency: string`、`merchant_id?: number | null`。

- [ ] **Step 2: 写失败测试**

`frontend/src/utils/currency.spec.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { convertAmount, symbolFromIntl } from './currency'

describe('currency utils', () => {
  it('convertAmount multiplies by exchange rate', () => {
    expect(convertAmount(10, 7.8)).toBeCloseTo(78)
  })
  it('symbolFromIntl falls back to code', () => {
    expect(symbolFromIntl('CNY')).toMatch(/¥|CNY|CN/)
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

Run:
```powershell
cd frontend
npx vitest run src/utils/currency.spec.ts
```
Expected: FAIL（模块不存在）

- [ ] **Step 4: 实现 currency.ts**

`frontend/src/utils/currency.ts`:

```ts
import type { Currency } from '@/types'
import { api } from '@/api'

let cache: Currency[] | null = null

export async function loadCurrencies(force = false): Promise<Currency[]> {
  if (!force && cache) return cache
  const res = await api.get('/currencies')
  cache = Array.isArray(res) ? res : ((res as any)?.items || [])
  return cache
}

export function symbolFromIntl(code: string): string {
  try {
    const parts = new Intl.NumberFormat(undefined, { style: 'currency', currency: code, currencyDisplay: 'narrowSymbol' }).formatToParts(0)
    const p = parts.find((x) => x.type === 'currency')
    return p?.value || code
  } catch {
    return code
  }
}

export async function currencySymbol(code: string): Promise<string> {
  const list = await loadCurrencies()
  const hit = list.find((c) => c.code === code)
  return hit?.symbol || symbolFromIntl(code)
}

export function formatMoney(amount: number, code: string): string {
  try {
    return new Intl.NumberFormat(undefined, { style: 'currency', currency: code }).format(amount)
  } catch {
    return `${amount.toFixed(2)} ${code}`
  }
}

export function convertAmount(amount: number, exchangeRate: number): number {
  return amount * (exchangeRate || 1)
}
```

- [ ] **Step 5: 实现 useUserCurrency**

`frontend/src/composables/useUserCurrency.ts`:

```ts
import { computed } from 'vue'
import { useUserStore } from '@/stores/user'

export function useUserCurrency() {
  const store = useUserStore()
  const currency = computed(() => store.user?.default_currency || 'CNY')
  return { currency }
}
```

- [ ] **Step 6: 运行测试确认通过**

Run:
```powershell
cd frontend
npx vitest run src/utils/currency.spec.ts
```
Expected: PASS（2 passed）

- [ ] **Step 7: Commit**

```bash
git add frontend/src/types/index.ts frontend/src/utils/currency.ts frontend/src/composables/useUserCurrency.ts frontend/src/utils/currency.spec.ts
git commit -m "feat(web): 币种类型与共享货币工具"
```

---

### Task 2: 币种与汇率 API 封装

**Files:**
- Create: `frontend/src/api/currencies.ts`
- Test: `frontend/src/api/currencies.spec.ts`（mock api）

- [ ] **Step 1: 写失败测试**

`frontend/src/api/currencies.spec.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'
import { api } from './client'
vi.mock('./client', () => ({ api: { get: vi.fn(), post: vi.fn() } }))
import { listCurrencies, refreshRates, manualRate, ratesStatus } from './currencies'

describe('currencies api', () => {
  it('listCurrencies calls GET /currencies', async () => {
    ;(api.get as any).mockResolvedValue([])
    const r = await listCurrencies()
    expect(api.get).toHaveBeenCalledWith('/currencies')
    expect(r).toEqual([])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run:
```powershell
cd frontend
npx vitest run src/api/currencies.spec.ts
```
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

`frontend/src/api/currencies.ts`:

```ts
import { api } from './client'
import type { Currency } from '@/types'

export function listCurrencies(): Promise<Currency[]> {
  return api.get('/currencies')
}

export function ratesStatus(): Promise<{ latest: string | null; source: string | null }> {
  return api.get('/exchange-rates/status')
}

export function refreshRates(): Promise<any> {
  return api.post('/admin/exchange-rates/refresh')
}

export function manualRate(payload: { rate_date: string; base_currency: string; rates: Record<string, number> }): Promise<any> {
  return api.post('/admin/exchange-rates/manual', payload)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```powershell
cd frontend
npx vitest run src/api/currencies.spec.ts
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add frontend/src/api/currencies.ts frontend/src/api/currencies.spec.ts
git commit -m "feat(web): 币种与汇率 API 封装"
```
---

### Task 3: 地区级联选择组件

**Files:**
- Create: `frontend/src/components/common/RegionSelect.vue`

- [ ] **Step 1: 写组件**

`frontend/src/components/common/RegionSelect.vue`:

```vue
<template>
  <v-row dense>
    <v-col v-for="(level, idx) in levels" :key="idx" cols="6" sm="3">
      <v-select
        v-model="selected[idx]"
        :items="options[idx]"
        :label="level.label"
        item-title="name"
        item-value="id"
        variant="outlined"
        density="compact"
        hide-details
        clearable
        @update:model-value="onLevelChange(idx)"
      />
    </v-col>
  </v-row>
</template>

<script setup lang="ts">
import { ref, watch, onMounted } from 'vue'
import { api } from '@/api'

interface Props {
  modelValue: number | null
}
const props = defineProps<Props>()
const emit = defineEmits<{ (e: 'update:modelValue', v: number | null): void }>()

const levels = [
  { label: '国家/地区', code: 0 },
  { label: '省份', code: 1 },
  { label: '城市', code: 2 },
  { label: '区县', code: 3 },
]
const selected = ref<(number | null)[]>([null, null, null, null])
const options = ref<any[][]>([[], [], [], []])

async function loadChildren(level: number, parentId: number | null) {
  const params = parentId != null ? { parent_id: parentId } : level === 0 ? {} : { level }
  const res = await api.get('/regions', { params })
  options.value[level] = Array.isArray(res) ? res : ((res as any)?.items || [])
  for (let i = level + 1; i < levels.length; i++) {
    selected.value[i] = null
    options.value[i] = []
  }
}

function currentRegionId(): number | null {
  for (let i = levels.length - 1; i >= 0; i--) {
    if (selected.value[i] != null) return selected.value[i]
  }
  return null
}

function onLevelChange(level: number) {
  const pid = selected.value[level]
  if (level < levels.length - 1) {
    if (pid == null) {
      for (let i = level + 1; i < levels.length; i++) { selected.value[i] = null; options.value[i] = [] }
    } else {
      void loadChildren(level + 1, pid)
    }
  }
  emit('update:modelValue', currentRegionId())
}

watch(() => props.modelValue, (v) => {
  // 编辑回填：仅当外部值变化且内部为空时跳过（简化：由父组件首次挂载时设置）
  if (v == null) return
})

onMounted(async () => {
  await loadChildren(0, null)
  if (props.modelValue != null) {
    // 回填祖先链（详情接口返回 ancestors 供父组件传入；此处用 path 兜底由父组件处理）
  }
})
</script>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/components/common/RegionSelect.vue
git commit -m "feat(web): 地区级联选择组件"
```

---

### Task 4: 价格记录表单币种联动

**Files:**
- Modify: `frontend/src/views/prices/QuickFillView.vue`
- Modify: `frontend/src/components/prices/PriceRecordForm.vue`
- Modify: `frontend/src/components/prices/QuickPriceRecordDialog.vue`

- [ ] **Step 1: QuickFillView 加币种状态**

在 `QuickFillView.vue` 的 `Merchant` interface 加 `default_currency: string | null`；在状态区加：

```ts
const currencies = ref<any[]>([])
const recordCurrency = ref<string>('CNY')
const currencySymbolText = ref<string>('¥')
```

并在 `onMounted` 里加载币种（同时 `loadMerchants` 返回商家含 `default_currency`）。

- [ ] **Step 2: 商家切换时带出币种符号**

在 `onMerchantChange` 内、设置 `historyRows` 之后加：

```ts
  const m = merchants.value.find((x) => x.id === val)
  recordCurrency.value = m?.default_currency || 'CNY'
  currencySymbolText.value = await currencySymbol(recordCurrency.value)
```

- [ ] **Step 3: 价格输入框前缀改为币种符号 + 可点下拉**

把模板里两处 `placeholder="¥0.00"` 的价格输入改为带 `prepend-inner-icon` 的菜单触发器：

```vue
<v-menu :close-on-content-click="true" location="bottom">
  <template #activator="{ props: menuProps }">
    <v-btn variant="text" size="x-small" class="pa-0" v-bind="menuProps" :aria-label="'选择币种'">
      {{ currencySymbolText }}
    </v-btn>
  </template>
  <v-list density="compact">
    <v-list-item
      v-for="c in currencies"
      :key="c.code"
      :title="`${c.symbol || c.code} ${c.name}`"
      :active="recordCurrency === c.code"
      @click="recordCurrency = c.code; currencySymbolText = c.symbol || symbolFromIntl(c.code)"
    />
  </v-list>
</v-menu>
```

并把价格字段 `placeholder="¥0.00"` 改为 `:placeholder="currencySymbolText + '0.00'"`。

- [ ] **Step 4: 保存时带上 currency**

在 `saveAll` 构造 payload 处，给每条价格记录加 `currency: recordCurrency.value`。

- [ ] **Step 5: PriceRecordForm / QuickPriceRecordDialog 同步**

这两处同样：商家必填（`PriceRecordForm` 的 `merchant_name` 改为商家 Autocomplete 且必填、置于商品字段前）、价格输入前缀用商家默认币种符号、点符号弹币种下拉、保存时带 `currency`。按 Step 1-4 同模式改造。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/views/prices/QuickFillView.vue frontend/src/components/prices/PriceRecordForm.vue frontend/src/components/prices/QuickPriceRecordDialog.vue
git commit -m "feat(web): 价格记录表单商家必填与币种联动"
```

---

### Task 5: 价格记录原币种 + 换算展示

**Files:**
- Create: `frontend/src/components/prices/PriceWithConvert.vue`
- Modify: `frontend/src/views/prices/PricesView.vue`
- Modify: `frontend/src/views/products/ProductDetail.vue`
- Modify: `frontend/src/views/ingredients/IngredientDetail.vue`

- [ ] **Step 1: 写展示组件**

`frontend/src/components/prices/PriceWithConvert.vue`:

```vue
<template>
  <span class="price-with-convert">
    <span class="price-native">{{ formatMoney(price, currency) }}</span>
    <span v-if="converted != null" class="price-converted text-caption text-medium-emphasis">
      ≈ {{ formatMoney(converted, userCurrency) }}
    </span>
  </span>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { formatMoney, convertAmount } from '@/utils/currency'
import { useUserCurrency } from '@/composables/useUserCurrency'

const props = defineProps<{
  price: number
  currency: string
  exchangeRate?: number | null
}>()
const { currency: userCurrency } = useUserCurrency()
const converted = computed(() => {
  if (!props.exchangeRate || props.currency === userCurrency.value) return null
  return convertAmount(props.price, props.exchangeRate)
})
</script>

<style scoped>
.price-with-convert { display: inline-flex; align-items: baseline; gap: 6px; }
</style>
```

- [ ] **Step 2: 接入价格列表**

在 `PricesView.vue` 的价格列，用 `<PriceWithConvert :price="item.price" :currency="item.currency || 'CNY'" :exchange-rate="item.exchange_rate" />` 替换原来的 `¥{{ item.price }}`（或等效文本）。

- [ ] **Step 3: 接入商品/原料详情**

在 `ProductDetail.vue` 与 `IngredientDetail.vue` 的价格展示处，同样替换为 `PriceWithConvert`（原币种为主，旁边小字换算用户币种）。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/prices/PriceWithConvert.vue frontend/src/views/prices/PricesView.vue frontend/src/views/products/ProductDetail.vue frontend/src/views/ingredients/IngredientDetail.vue
git commit -m "feat(web): 价格记录原币种与换算展示"
```
---

### Task 6: 商家表单地区、默认币种与地图反查

**Files:**
- Modify: `frontend/src/views/data/MerchantsView.vue`
- Modify: `frontend/src/views/merchants/MerchantDetail.vue`

- [ ] **Step 1: 商家表单加地区与默认币种字段**

在 `MerchantsView.vue` 的商家编辑对话框表单（`openEditDialog` 对应模板）加：

```vue
<RegionSelect v-model="form.region_id" class="mb-4" />

<v-select
  v-model="form.default_currency"
  :items="currencies"
  item-title="name"
  item-value="code"
  label="默认币种（留空跟随地区）"
  variant="outlined"
  density="compact"
  clearable
  hide-details
  class="mb-4"
/>
```

并在表单状态对象加 `region_id: null`、`default_currency: null`；创建/更新请求体带上这两个字段。

- [ ] **Step 2: 加载币种选项**

在 `MerchantsView.vue` 的 `onMounted` 加：

```ts
currencies.value = await loadCurrencies()
```

- [ ] **Step 3: 地图选点后自动反查地区**

在现有地图选点回调（`MapPicker` 的 `@update:model-value` 或等效处）加：

```ts
async function onMapPick(lat: number, lng: number) {
  form.latitude = lat
  form.longitude = lng
  try {
    const res = await api.post('/merchants/geocode', { latitude: lat, longitude: lng })
    if (res?.region_id) form.region_id = res.region_id
  } catch { /* 反查失败保持手动 */ }
}
```

并在 `openEditDialog` 里 `form` 增加 `latitude`/`longitude`/`region_id` 字段绑定。

- [ ] **Step 4: 商家详情页同构**

在 `MerchantDetail.vue` 的编辑表单做同样改造（地区 + 默认币种 + 地图反查）。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/data/MerchantsView.vue frontend/src/views/merchants/MerchantDetail.vue
git commit -m "feat(web): 商家地区默认币种与地图反查"
```

---

### Task 7: 个人中心默认币种与默认计算范围

**Files:**
- Modify: `frontend/src/views/profile/ProfileView.vue`
- Modify: `frontend/src/stores/user.ts`
- Modify: `frontend/src/api/currencies.ts`

- [ ] **Step 1: user store 本地模式加默认币种字段**

在 `stores/user.ts` 的 `localUserData` 加 `default_currency: 'CNY'`、`default_calc_scope: 'country'`。

- [ ] **Step 2: 个人中心加两个设置项**

在 `ProfileView.vue` 的「设置列表」卡片内加：

```vue
<v-list-item @click="currencyDialog = true">
  <template #prepend><v-icon>mdi-currency-usd</v-icon></template>
  <v-list-item-title>默认币种</v-list-item-title>
  <v-list-item-subtitle>{{ user?.default_currency || '跟随所在地区' }}</v-list-item-subtitle>
  <template #append><v-icon>mdi-chevron-right</v-icon></template>
</v-list-item>

<v-list-item @click="scopeDialog = true">
  <template #prepend><v-icon>mdi-map-marker-radius</v-icon></template>
  <v-list-item-title>默认计算范围</v-list-item-title>
  <v-list-item-subtitle>{{ scopeLabel }}</v-list-item-subtitle>
  <template #append><v-icon>mdi-chevron-right</v-icon></template>
</v-list-item>
```

- [ ] **Step 3: 实现两个选择对话框**

在 `ProfileView.vue` 加两个 `v-dialog`：

```vue
<v-dialog v-model="currencyDialog" max-width="420">
  <v-card>
    <v-card-title>默认币种</v-card-title>
    <v-card-text>
      <v-select v-model="currencyValue" :items="currencies" item-title="name" item-value="code" clearable label="留空 = 跟随所在地区" />
    </v-card-text>
    <v-card-actions>
      <v-spacer /><v-btn variant="text" @click="currencyDialog=false">取消</v-btn>
      <v-btn color="primary" @click="saveCurrency">保存</v-btn>
    </v-card-actions>
  </v-card>
</v-dialog>

<v-dialog v-model="scopeDialog" max-width="420">
  <v-card>
    <v-card-title>默认计算范围</v-card-title>
    <v-card-text>
      <v-select v-model="scopeValue" :items="scopeOptions" item-title="title" item-value="value" />
    </v-card-text>
    <v-card-actions>
      <v-spacer /><v-btn variant="text" @click="scopeDialog=false">取消</v-btn>
      <v-btn color="primary" @click="saveScope">保存</v-btn>
    </v-card-actions>
  </v-card>
</v-dialog>
```

- [ ] **Step 4: 保存到后端**

`saveCurrency` 调 `api.patch('/auth/me', { default_currency: currencyValue || null })`；`saveScope` 调 `api.patch('/auth/me', { default_calc_scope: scopeValue })`；保存后 `await useUserStore().fetchUser()`。

`scopeOptions = [{title:'国家/地区', value:'country'}, {title:'省级', value:'province'}, {title:'地级', value:'city'}, {title:'县级', value:'county'}]`。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/profile/ProfileView.vue frontend/src/stores/user.ts
git commit -m "feat(web): 用户默认币种与默认计算范围"
```

---

### Task 8: 聚合结果用户币种与地区选择器

**Files:**
- Modify: `frontend/src/views/recipes/RecipeDetail.vue`
- Modify: `frontend/src/views/recipes/RecipeAnalysisView.vue`
- Modify: `frontend/src/views/recipes/RecipesView.vue`
- Modify: `frontend/src/views/products/ProductDetail.vue`
- Modify: `frontend/src/views/ingredients/IngredientDetail.vue`

- [ ] **Step 1: 建地区选择 composable**

`frontend/src/composables/useCalcRegion.ts`:

```ts
import { ref, computed } from 'vue'
import { useUserStore } from '@/stores/user'

export function useCalcRegion() {
  const store = useUserStore()
  const regionId = ref<number | null>(null)
  const effective = computed(() => regionId.value ?? null)
  return { regionId, effective }
}
```

（后端 API 在调用成本/价格接口时传 `region_id` 查询参数，缺省由后端按用户默认计算范围解析。）

- [ ] **Step 2: 菜谱/商品/原料价格请求带上 region_id**

在这些视图调用成本/价格接口的 `params` 里统一加 `region_id: effective.value`（`null` 时不传或传空）。

- [ ] **Step 3: 顶部加地区选择器**

在 `RecipeDetail.vue`、`RecipeAnalysisView.vue`、`ProductDetail.vue`、`IngredientDetail.vue` 的工具栏加：

```vue
<RegionSelect v-model="regionId" class="mb-3" />
```

- [ ] **Step 4: 聚合金额用用户币种格式化**

把这些视图里硬编码的 `¥{{ cost }}` / `mdi-currency-cny` 相关金额改为 `{{ formatMoney(cost, userCurrency) }}`（`useUserCurrency().currency`），并移除/替换 `mdi-currency-cny` 图标为 `mdi-currency-{{ userCurrency.toLowerCase() }}` 或直接去掉图标。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/composables/useCalcRegion.ts frontend/src/views/recipes/RecipeDetail.vue frontend/src/views/recipes/RecipeAnalysisView.vue frontend/src/views/recipes/RecipesView.vue frontend/src/views/products/ProductDetail.vue frontend/src/views/ingredients/IngredientDetail.vue
git commit -m "feat(web): 聚合结果用户币种与地区选择器"
```
---

### Task 9: 后台币种与汇率管理页

**Files:**
- Create: `frontend/src/views/admin/CurrencyAdminView.vue`
- Create: `frontend/src/views/admin/ExchangeRateAdminView.vue`
- Modify: `frontend/src/router/index.ts`
- Modify: `frontend/src/views/admin/AdminDashboard.vue`

- [ ] **Step 1: 写币种管理页**

`frontend/src/views/admin/CurrencyAdminView.vue`:

```vue
<template>
  <v-container fluid>
    <v-card elevation="0">
      <v-card-title>币种管理</v-card-title>
      <v-card-text>
        <v-table>
          <thead><tr><th>代码</th><th>名称</th><th>符号</th><th>小数位</th><th>启用</th></tr></thead>
          <tbody>
            <tr v-for="c in currencies" :key="c.code">
              <td>{{ c.code }}</td>
              <td>{{ c.name }}</td>
              <td>{{ c.symbol }}</td>
              <td>{{ c.decimals }}</td>
              <td><v-icon v-if="c.is_active !== false" color="success">mdi-check</v-icon></td>
            </tr>
          </tbody>
        </v-table>
      </v-card-text>
    </v-card>
  </v-container>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { listCurrencies } from '@/api/currencies'
const currencies = ref<any[]>([])
onMounted(async () => { currencies.value = await listCurrencies() })
</script>
```

（增补/停用币种的写接口后续随后端 admin 能力补齐；本页先只读展示，符合当前后端只读 `GET /currencies`。）

- [ ] **Step 2: 写汇率管理页**

`frontend/src/views/admin/ExchangeRateAdminView.vue`:

```vue
<template>
  <v-container fluid>
    <v-card elevation="0">
      <v-card-title>汇率管理</v-card-title>
      <v-card-text>
        <v-alert type="info" variant="tonal" class="mb-4">
          最新快照：{{ status.latest || '暂无' }}（{{ status.source || '-' }}）
        </v-alert>
        <v-btn color="primary" :loading="loading" @click="onRefresh">立即刷新</v-btn>
      </v-card-text>
    </v-card>
  </v-container>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { ratesStatus, refreshRates } from '@/api/currencies'
const status = ref<{ latest: string | null; source: string | null }>({ latest: null, source: null })
const loading = ref(false)
onMounted(async () => { status.value = await ratesStatus() })
async function onRefresh() {
  loading.value = true
  try { await refreshRates(); status.value = await ratesStatus() } finally { loading.value = false }
}
</script>
```

（手动补改汇率在后续需要时再加表单，当前后端已提供 `POST /admin/exchange-rates/manual`。）

- [ ] **Step 3: 注册路由**

在 `frontend/src/router/index.ts` 的管理员路由区加：

```ts
{ path: '/admin/currencies', name: 'admin-currencies', component: () => import('@/views/admin/CurrencyAdminView.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
{ path: '/admin/exchange-rates', name: 'admin-exchange-rates', component: () => import('@/views/admin/ExchangeRateAdminView.vue'), meta: { requiresAuth: true, requiresAdmin: true } },
```

- [ ] **Step 4: 挂到管理首页**

在 `AdminDashboard.vue` 的管理卡片/列表里加两个入口链接（`mdi-currency-usd` 币种管理、`mdi-swap-horizontal` 汇率管理）。

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/admin/CurrencyAdminView.vue frontend/src/views/admin/ExchangeRateAdminView.vue frontend/src/router/index.ts frontend/src/views/admin/AdminDashboard.vue
git commit -m "feat(web): 后台币种与汇率管理页"
```

---

### Task 10: 本地模式对齐

**Files:**
- Modify: `frontend/src/api/local/database.ts`
- Modify: `frontend/src/api/local/handlers/merchants.ts`
- Modify: `frontend/src/api/local/handlers/products.ts`
- Modify: `frontend/src/api/local/handlers/recipes.ts`
- Create: `frontend/src/api/local/handlers/currencies.ts`

- [ ] **Step 1: 本地 schema 加字段**

在 `database.ts` 的商家与价格记录对象上加 `region_id`、`default_currency`、`currency`、`exchange_rate`、`user_currency` 字段，默认值同后端（`currency: 'CNY'`、`exchange_rate: 1`、`user_currency: 'CNY'`）。

- [ ] **Step 2: 本地币种/汇率 handler**

`frontend/src/api/local/handlers/currencies.ts`:

```ts
import type { Currency } from '@/types'

const CURRENCIES: Currency[] = [
  { code: 'CNY', name: '人民币', symbol: '¥', decimals: 2 },
  { code: 'USD', name: '美元', symbol: '$', decimals: 2 },
  { code: 'EUR', name: '欧元', symbol: '€', decimals: 2 },
  { code: 'JPY', name: '日元', symbol: '¥', decimals: 0 },
]

export function localGetCurrencies(): Currency[] {
  return CURRENCIES
}

export function localRatesStatus() {
  return { latest: null, source: 'local-static' }
}
```

并把 `/currencies`、`/exchange-rates/status` 两个 GET 路由注册进 `local/proxy.ts`（或 `handlers/` 路由表），返回上述结果。

- [ ] **Step 3: 本地商家 handler 支持地区/默认币种**

在 `handlers/merchants.ts` 的 create/update 里透传 `region_id`、`default_currency`；`/merchants/geocode` 本地直接返回 `{ region_id: null }`（本地不依赖地图逆地理）。

- [ ] **Step 4: 本地价格记录 handler 写快照**

在 `handlers/products.ts` 写价格记录时：`currency = payload.currency || 'CNY'`、`exchange_rate = 1`、`user_currency = 'CNY'`（本地固定 CNY，同币种倍率 1）。

- [ ] **Step 5: 本地成本计算用用户币种**

在 `local/business/costCalculator.ts` 与 `priceWeighted.ts` 中，对每条记录用 `convertAmount(record.price, record.exchange_rate || 1)` 后再聚合（与后端折算口径一致）；地区过滤同后端语义：按 `merchant.region_id` 子树过滤（本地 `regions` 已提供层级）。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/api/local
git commit -m "feat(web): 本地模式多币种与地区对齐"
```