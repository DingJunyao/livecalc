# 多币种与地区化计算——移动端 Flutter 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Flutter 移动端落地多币种与地区化计算，对齐 Web 端：价格记录币种联动、原币种+换算展示、商家地区/默认币种、用户默认币种与默认计算范围、聚合结果用户币种。

**Architecture:** 扩展数据模型与 repository，新增币种工具与地区级联 widget，改造价格记录/商家/个人中心/详情视图，金额展示统一走 `CurrencyFmt`。

**Tech Stack:** Flutter + Riverpod + GoRouter。

**分支约束:** 本计划在 `feat/mobile-app` 上实施（后端/Web 计划先并入 master，再合并进本分支）。

---

### Task 1: 模型字段与币种工具

**Files:**
- Modify: `mobile/lib/features/merchants/models/merchant.dart`
- Modify: `mobile/lib/features/prices/models/price_record.dart`
- Create: `mobile/lib/shared/utils/currency_fmt.dart`

- [ ] **Step 1: Merchant 模型加字段**

在 `Merchant` 构造与 `fromJson` 加 `regionId`、`defaultCurrency`：

```dart
final int? regionId;
final String? defaultCurrency;
```

`fromJson` 里：

```dart
regionId: (json['region_id'] as num?)?.toInt(),
defaultCurrency: json['default_currency'] as String?,
```

- [ ] **Step 2: PriceRecord 模型加字段**

加 `currency`、`exchangeRate`、`userCurrency`：

```dart
final String currency;
final double? exchangeRate;
final String? userCurrency;
```

`fromJson` 里：

```dart
currency: (json['currency'] as String?) ?? 'CNY',
exchangeRate: json['exchange_rate'] == null ? null : _toDouble(json['exchange_rate']),
userCurrency: json['user_currency'] as String?,
```

- [ ] **Step 3: 建币种工具**

`mobile/lib/shared/utils/currency_fmt.dart`:

```dart
import 'package:intl/intl.dart';

/// 货币代码 → 符号（intl 提供大部分；缺失时回退代码）。
String currencySymbol(String code) {
  try {
    final fmt = NumberFormat.simpleCurrency(locale: Intl.getCurrentLocale(), name: code);
    final s = fmt.symbols.CURRENCY_SYMBOL;
    return s.isEmpty ? code : s;
  } catch (_) {
    return code;
  }
}

String formatMoney(num amount, String code) {
  try {
    return NumberFormat.simpleCurrency(name: code).format(amount);
  } catch (_) {
    return '${amount.toStringAsFixed(2)} $code';
  }
}

num convertAmount(num amount, num? exchangeRate) {
  return amount * (exchangeRate ?? 1);
}
```

- [ ] **Step 4: Commit**

```bash
git add mobile/lib/features/merchants/models/merchant.dart mobile/lib/features/prices/models/price_record.dart mobile/lib/shared/utils/currency_fmt.dart
git commit -m "feat(mobile): 多币种模型字段与币种工具"
```

---

### Task 2: repository 支持币种/地区/汇率

**Files:**
- Modify: `mobile/lib/features/merchants/repositories/merchant_repository.dart`
- Modify: `mobile/lib/features/prices/repositories/price_repository.dart`
- Modify: `mobile/lib/features/auth/repositories/auth_repository.dart`

- [ ] **Step 1: 商家 repository**

`createMerchant` / `updateMerchant` 增加 `regionId`、`defaultCurrency` 命名参数并写入 body；新增 `geocode(latitude, longitude)` 调 `POST /merchants/geocode` 返回 `int? regionId`。

- [ ] **Step 2: 价格 repository**

写价格记录 body 增加 `currency` 字段；解析响应时保留 `currency/exchange_rate/user_currency`（模型已支持）。

- [ ] **Step 3: auth repository**

`updateSettings`（或等效）增加 `defaultCurrency`、`defaultCalcScope` 透传字段。

- [ ] **Step 4: Commit**

```bash
git add mobile/lib/features/merchants/repositories/merchant_repository.dart mobile/lib/features/prices/repositories/price_repository.dart mobile/lib/features/auth/repositories/auth_repository.dart
git commit -m "feat(mobile): repository 支持币种地区汇率"
```

---

### Task 3: 地区级联选择 widget

**Files:**
- Create: `mobile/lib/shared/widgets/region_select_field.dart`

- [ ] **Step 1: 写 widget**

`mobile/lib/shared/widgets/region_select_field.dart`（核心逻辑）：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../features/merchants/repositories/merchant_repository.dart';

class RegionSelectField extends ConsumerStatefulWidget {
  final int? value;
  final ValueChanged<int?> onChanged;
  const RegionSelectField({super.key, required this.value, required this.onChanged});

  @override
  ConsumerState<RegionSelectField> createState() => _RegionSelectFieldState();
}

class _RegionSelectFieldState extends ConsumerState<RegionSelectField> {
  static const _levels = [('国家/地区', 0), ('省份', 1), ('城市', 2), ('区县', 3)];
  late final List<List<Map<String, dynamic>>> _options;
  late final List<int?> _sel;

  @override
  void initState() {
    super.initState();
    _options = List.generate(4, (_) => const []);
    _sel = [null, null, null, null];
    _load(0, null);
  }

  Future<void> _load(int level, int? parentId) async {
    final repo = MerchantRepository();
    final rows = await repo.listRegions(parentId: parentId, level: parentId == null && level == 0 ? 0 : null);
    if (!mounted) return;
    setState(() {
      _options[level] = rows.map((r) => {'id': r.id, 'name': r.name}).toList();
      for (var i = level + 1; i < 4; i++) { _sel[i] = null; _options[i] = const []; }
    });
  }

  int? get _current => _sel.reversed.firstWhere((v) => v != null, orElse: () => null);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        for (var i = 0; i < 4; i++)
          DropdownButtonFormField<int?>(
            initialValue: _sel[i],
            items: [
              const DropdownMenuItem<int?>(value: null, child: Text('请选择')),
              for (final o in _options[i]) DropdownMenuItem<int?>(value: o['id'] as int, child: Text(o['name'] as String)),
            ],
            decoration: InputDecoration(labelText: _levels[i].$1, border: const OutlineInputBorder()),
            onChanged: (v) {
              setState(() => _sel[i] = v);
              if (v != null && i < 3) { _load(i + 1, v); }
              widget.onChanged(_current);
            },
          ),
      ],
    );
  }
}
```

（`MerchantRepository.listRegions` 需要在 Task 2 中补充 `GET /regions` 封装。）

- [ ] **Step 2: Commit**

```bash
git add mobile/lib/shared/widgets/region_select_field.dart
git commit -m "feat(mobile): 地区级联选择 widget"
```

---

### Task 4: 价格记录表单币种联动

**Files:**
- Modify: `mobile/lib/features/prices/screens/price_record_form_screen.dart`
- Modify: `mobile/lib/features/prices/screens/quick_fill_screen.dart`

- [ ] **Step 1: 表单状态加币种**

在 `_PriceRecordFormScreenState` 加：

```dart
String _currency = 'CNY';
String _currencySymbol = '¥';
List<dynamic> _currencies = const [];
```

`initState` 里加载币种列表（`GET /currencies`）。

- [ ] **Step 2: 商家切换带出符号**

在 `_merchantController` 的 `onChanged` 或 Autocomplete 的 `onSelected` 里，根据选中的 `Merchant.defaultCurrency` 设 `_currency` 与 `_currencySymbol`。

- [ ] **Step 3: 价格输入前缀 + 点击弹币种下拉**

价格 `TextField` 的 `prefixText` 用 `_currencySymbol`，并在其前方放一个 `PopupMenuButton<String>`，点击弹币种列表切换 `_currency`（symbol 同步）。价格字段保持必填校验。

- [ ] **Step 4: 保存带 currency**

`_save` 里把 `_currency` 写入价格记录 body。

- [ ] **Step 5: quick_fill_screen 同步**

`quick_fill_screen.dart` 同样处理：商家必填 + 币种符号 + 币种下拉 + 保存带 `currency`。

- [ ] **Step 6: Commit**

```bash
git add mobile/lib/features/prices/screens/price_record_form_screen.dart mobile/lib/features/prices/screens/quick_fill_screen.dart
git commit -m "feat(mobile): 价格记录币种联动"
```

---

### Task 5: 价格列表原币种 + 换算

**Files:**
- Modify: `mobile/lib/features/prices/screens/price_list_screen.dart`

- [ ] **Step 1: 展示原币种与换算**

价格单元格改为两行：

```dart
Text(formatMoney(r.price, r.currency))
if (r.exchangeRate != null && r.currency != userCurrency)
  Text('≈ ${formatMoney(convertAmount(r.price, r.exchangeRate), userCurrency)}',
      style: Theme.of(context).textTheme.bodySmall)
```

`userCurrency` 从用户信息（auth provider）取，缺省 `'CNY'`。

- [ ] **Step 2: Commit**

```bash
git add mobile/lib/features/prices/screens/price_list_screen.dart
git commit -m "feat(mobile): 价格列表原币种与换算展示"
```

---

### Task 6: 商家表单地区/默认币种/地图反查

**Files:**
- Modify: `mobile/lib/features/merchants/screens/merchant_form_screen.dart`

- [ ] **Step 1: 表单加地区与默认币种**

在 `_MerchantFormScreenState` 加 `int? _regionId; String? _defaultCurrency;`，build 里加：

```dart
RegionSelectField(value: _regionId, onChanged: (v) => setState(() => _regionId = v)),
DropdownButtonFormField<String?>(
  initialValue: _defaultCurrency,
  items: [const DropdownMenuItem(value: null, child: Text('跟随地区')), for (final c in _currencies) DropdownMenuItem(value: c.code, child: Text('${c.symbol} ${c.name}'))],
  decoration: const InputDecoration(labelText: '默认币种', border: OutlineInputBorder()),
  onChanged: (v) => setState(() => _defaultCurrency = v),
),
```

- [ ] **Step 2: 地图选点后反查**

在 `MapPointPicker` 回调里：

```dart
final rid = await repository.geocode(lat, lng);
if (rid != null) setState(() => _regionId = rid);
```

- [ ] **Step 3: 保存带字段**

`createMerchant`/`updateMerchant` 传 `regionId`、`defaultCurrency`。

- [ ] **Step 4: Commit**

```bash
git add mobile/lib/features/merchants/screens/merchant_form_screen.dart
git commit -m "feat(mobile): 商家地区默认币种与地图反查"
```

---

### Task 7: 个人中心默认币种与默认计算范围

**Files:**
- Modify: `mobile/lib/features/profile/screens/profile_screen.dart`

- [ ] **Step 1: 加两个入口**

在 `ProfileScreen` 设置列表加「默认币种」「默认计算范围」两个 `ListTile`，副标题分别显示当前值（`跟随地区` / `国家/地区|省级|地级|县级`）。

- [ ] **Step 2: 弹选择对话框**

分别弹 `showDialog` + 下拉选择，保存时调 auth repository `updateSettings` 传 `default_currency` / `default_calc_scope`，成功后刷新 auth provider。

- [ ] **Step 3: Commit**

```bash
git add mobile/lib/features/profile/screens/profile_screen.dart
git commit -m "feat(mobile): 用户默认币种与默认计算范围"
```

---

### Task 8: 聚合结果用户币种 + 地区选择器

**Files:**
- Modify: `mobile/lib/features/recipes/providers/recipe_provider.dart`
- Modify: `mobile/lib/features/recipes/repositories/recipe_repository.dart`
- Modify: `mobile/lib/features/ingredients/screens/ingredient_detail_screen.dart`
- Modify: `mobile/lib/features/products/screens/product_detail_screen.dart`

- [ ] **Step 1: 请求带 region_id**

recipe/价格 repository 的 `params` 加 `region_id`（来自当前地区选择状态，缺省 null 由后端按用户默认计算范围解析）。

- [ ] **Step 2: 加地区选择器**

在 `RecipeDetail`（菜谱详情/成本分析）、`IngredientDetail`、`ProductDetail` 顶部加 `RegionSelectField`，选择后触发重新加载。

- [ ] **Step 3: 金额用用户币种格式化**

把这些页面里 `Icons.currency_yen` / `¥` 金额替换为 `formatMoney(cost, userCurrency)`，并移除/替换日元图标。

- [ ] **Step 4: Commit**

```bash
git add mobile/lib/features/recipes/providers/recipe_provider.dart mobile/lib/features/recipes/repositories/recipe_repository.dart mobile/lib/features/ingredients/screens/ingredient_detail_screen.dart mobile/lib/features/products/screens/product_detail_screen.dart
git commit -m "feat(mobile): 聚合结果用户币种与地区选择器"
```